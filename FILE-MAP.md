# Framework file map — the EXHAUSTIVENESS net

> ⚠️ EXTRACTED from the skill on 31/07/2026 (progressive disclosure): this list weighed
> 24,625 characters, 48% of the skill, and pushed the whole thing far above the emission
> budget — the skill was therefore EVICTED from the frame, i.e. ABSENT from context.
> ⚠️ ROLE UNCHANGED: this is the exhaustiveness net. A FILE OUTSIDE THIS LIST IS A
> HOLE, by definition — never a judgement of importance. Sealed by check ② of
> `coverage-gate.test.js`, which reads THIS file and the skill.
> ⚠️ ON-DEMAND reading: the skill only keeps a pointer to it. Do not fold it back in.

**Engine (`src/`)**:
- `FILE-MAP.md` — THIS file: the exhaustiveness net itself (1 line = 1 file + role).
- `src/hooks/legacy-mcp-inject.js` — RELIC (unwired 17/07/2026, the gate covers MCP): kept as the MCP differential's oracle + rollback. The doctor requires its ABSENCE from the wiring.
- `src/hooks/ctxroute-reset.js` — PreCompact hook. Empties the session stores at every compaction (doc-seen, legacy ctxroute-seen, turn-count — absolute reset, all modes).
- `src/hooks/turn-count.js` — TURN gate (UserPromptSubmit, wired 18/07/2026): increments the session's turn counter (store 'turn-count-'). SILENT by contract (UserPromptSubmit stdout = injected context). Sensor for `driftUnit: turn`.
- `src/lib-pure.js` — ALL the decision logic, zero I/O (fs/path/process forbidden). Mutated by Stryker.
- `src/lock.js` — cross-process lock (atomic `fs.mkdirSync`) protecting concurrent access to `state/`. 2 s fail-open timeout (prod, untouchable); env `CTXROUTE_LOCK_TIMEOUT_MS` reserved for concurrency tests (proving atomicity without load).
- `src/stdin-json.js` — stdin→JSON reading shared by the hooks (extracted after jscpd duplication detection).
- `src/paths.js` — SINGLE SOURCE of the paths (config/docs/state) + the HARNESS ROOTS registry (`harnessRoots()`: hook fleet, transcript corpus, skill store) + the isolation env vars reserved for tests/doctor (a carved COUNT drifts, so none is written here). No ad-hoc `path.join(__dirname,...)` nor `path.join(os.homedir(),...)` anywhere else.
- `tools/doctor.js` — dead-man switch: end-to-end probes (gates + write guard + reset), each proven by REAL EFFECT (context injected, store incremented, stores erased — never just exit 0, hole closed 19/07) + `settings.json` wiring file by file. Wired on SessionStart (`--quiet`), screams if dead.
- `src/deadline.js` — process DEADLINE (anti-zombie). Standalone, depends on nothing. Every stdin-reading hook MUST arm it before any I/O. Born from the 875 zombies of 15/07/2026 (Claude Code bug #68626). ⚠️ BEST-EFFORT by nature (lives INSIDE the targeted process): 502 zombies measured on 27/07/2026 DESPITE it (processes born under saturation, never got far enough to arm it). The external net that collects them is a PERSONAL maintainer tool — **outside this public repo: it targets hooks specific to their machine, nothing universal about it**.
- `vitest-projects.mjs` — **SUITE CLASSIFICATION, DERIVED FROM CONTENT** (spawn / env mutation ⇒ `integration`, else `unit`). It is what makes `isolate: false` SAFE on the fast lane. 212 s → 1.7 s. Sealed by `vitest-projects-gate.test.js`.
- `src/harness-profile.js` — **THE HARNESS DIALECT, AS DATA** (path keys, patch tools). Zero logic, zero I/O. Porting a harness = editing THIS file, never the engine. 🛑 No shell-tool list: a shell is DETECTED (presence of a `command`, measured on 7,553 calls). Sealed by `harness-profile-gate.test.js`.
- `test/sources-file.property.test.js` — **THE LAWS of `exclude`/`scope`** (fast-check): monotonicity · independence from PHRASING · group monotonicity · flat↔group parity. Each one SEEN RED on a sabotage. This is where the invariants live that a case test cannot cover.
- `src/language-spec.js` — **INDEPENDENT MODEL of the semantics** (∃/∀ per operator), written from INTENT and never copied from the engine. PURE, zero dependencies. The only judge of what the language MUST do; `spec-differential.test.js` confronts it EXHAUSTIVELY with the engine.
- `src/cadence-spec.js` — **INDEPENDENT MODEL of the CADENCE** (the twin of `language-spec.js`, for the other half of the language): 4-stage cascade with its 4 DECLARED asymmetries, delivery, drift, memory, alternation of the refusal, target filter. Written from INTENT, PURE, and OUTSIDE `mutate` (a model's judge is its differential, never a mutation score).
- `test/cadence-differential.test.js` — confronts `cadence-spec.js` with the REAL `gate.js`, **EXHAUSTIVELY: 11,346 cases** (cascade · delivery/drift · memory/alternation/filter), INVALID values included — they are what prove the total fallback. 4 engine sabotages as negative-check. Found on its first run: `threshold`'s validity rule differed per cascade stage, so `0` slipped through and silently turned `smart` into `dumb`.
- `src/sources/file.js` — "file" SOURCE: payload → which docs (frontmatters). PURE, exact replica of the historical injection engine. Consumed by the unified gate.
- `src/sources/tool.js` — "tool" SOURCE (19/07/2026): docs with a `tool:` key → EXACT (===) match on a native tool NAME (WebFetch/WebSearch — the no-path/no-mcp__ blind spot, closed). scope/exclude via file.shouldSkip (single source). PURE, 100% mutated. ⚠️ its adapter REUSES the corpus laid down by fileAdapter (file→tool order = dependency sealed by a test).
- `src/sources/mcp.js` — "MCP" SOURCE (merger 17/07/2026): payload → 'mcp/…' docs (3 levels, filters, subToolParam via lib-pure) + declFor (cadence: THE doc's frontmatter > global — JSON = global only since 17/07/2026). PURE, 100% mutated.
- `src/source-adapters.js` — source REGISTRY (extension point, 17/07/2026): adapter contract {id, collect, message} documented at the top, array order = concatenation order. New source = 1 pure module + 1 adapter HERE — doc-inject.js/gate.js are NEVER touched.
- `src/frontmatter.js` — PURE parser of the docs' frontmatter. Total: never throws. Triggers: `match:` (file), `mcp:` (server), `rules:` (per-entry JSON {pattern, scope?, exclude?} — for the 31 docs with divergent scopes, measured 16/07).
- `src/loader.js` — PURE: doc corpus (frontmatters) → flat ordered rules for `sources/file.js`. Sorted PER RULE (per-entry rank = interleaved docs). 100% mutated.
- `src/gate.js` — PURE: THE decision of the unified gate (per DOC: dumb/once/smart, foreign counters, docLabel). 100% mutated.
- `src/hooks/doc-inject.js` — UNIFIED GATE (✅ WIRED, LIVE 17/07/2026, single PreToolUse `*` hook): Claude Code shell = stdin + emit; common body in pretool-core.js.
- `tools/explain.js` — INTROSPECTION TOOL (31/07/2026): "for THIS action, what injects, and WHY (or why NOT)?". Read-only, ZERO state writes, fail-LOUD (exit 2). Consumes the SAME functions as the gate; the "why NOT" comes from PROBES that re-query the real sources — never a 2nd match logic. ⚠️ Draw it BEFORE concluding a doc is silent: re-implementing the engine to probe it once cost a whole session.
- `src/collect-core.js` — SHARED collection gate↔explain (single source of "which docs for this payload?"). Zero decision (gate.js decides). Duplicating it reopens the divergence `explain` exists to kill.
- `src/pretool-core.js` — PreToolUse GATE CORE (19/07/2026, multi-harness single source): registry collection → gate → EMISSION, run(data, emit, options). Any orchestration change lives HERE, never in a shell. ⚠️ It no longer orchestrates TRANSPORT: queue and splitting moved into `emission-core.js` on 05/08/2026 (⑯) — putting them back would make them optional for other emitters.
- `src/emission-core.js` — EMISSION LAYER (05/08/2026, ⑯): the ONLY way a context leaves. Receives segments, applies queue + splitting + seal, persists the remainder. I/O shell (never mutated); all decision is pure in budget.js. Queue SHARED by all emitters — that is what makes the session gate's remainder drainable.
- `test/store-purge-gate.test.js` — STORAGE-COUPLING GATE (07/08/2026, ㉕): every declared store is PURGED by `ctxroute-reset` (derived from BOTH sides, never a list) + inverse check (dead purge) + negative-check. Turns the prose instruction of reset.md ("every new store MUST be added here") into a machine. 🛑 It proves a store is KNOWN to the reset, NOT that a reader TOLERATES the purge — that part is semantic and stays covered by a case test.
- `test/emission-core.test.js` — EMISSION COUNTER suite (07/08/2026): the canary's denominator. Seals the CONDITIONAL increment (an empty pass does not count — otherwise accusing a healthy channel), per-agent-scope isolation, retro-compat (absent key = 0), totality (absurd value ⇒ 0, never a throw), and the fact that the counter does NOT break the queue it shares its store with.
- `test/emission-core-gate.test.js` — DERIVED GATE: "every file that writes `additionalContext` REACHES emission-core" (transitive traversal), + inverse check on stale exemptions + negative-check via IN-MEMORY sabotage; ② "no `*-core.js` calls `process.exit`" — the lifecycle belongs to the SHELL (that check is what found `guard-core`). They make the layers MANDATORY instead of optional.
- `test/english-only-gate.test.js` — GATE: the PUBLISHED mirror (`docs/framework/`) carries no non-English prose. NOT a French detector but a NOT-ENGLISH one (contributors are international). Dependency `eld` CHOSEN BY MEASUREMENT AGAINST THE MARKET LEADER: `franc` (1,374,671 downloads/month, 4,407 stars) produced 97 FALSE POSITIVES on this corpus, `eld` produced 0 and caught 2 real violations out of 2. `isReliable()` is load-bearing (the detector says ITSELF when the sample is too short); MIN_CHARS=90 measured. Born of the rule being broken 3 times in 4 days, one slip surviving a session that ended with "everything is clean".
- `test/state-write-under-lock-gate.test.js` — GATE: no state write escapes the cross-process lock. Born from the audit of work item 58 (2026-08-20), which ANSWERED its first question — the lock MUST exist, because the critical section is a LEADER ELECTION plus a memoized plan among N parallel frame processes, not a per-doc counter that could decompose into independent files. What the audit found instead was an invariant carried by PROSE: `emission.md` states "`emettre` IS CALLED UNDER LOCK" while the writer takes no lock itself — it is safe only because both callers wrap it, and `emission-core.js` is BY DESIGN the layer every future emitter crosses. Rule via `ast-grep` (never regex), DERIVED from the SHAPE of a write. It went RED twice while being built, the second time on an ALIAS that hid a write — flagged, not excused.
- `tools/scope-reach.js` — INSTRUMENT (read-only, informs, never gates): "would giving `scope` the universe of `exclude` touch the real fleet, and where?". Born of work item 59, whose evidence lived in a THROWAWAY script while the backlog promised it was "reproducible in 3 minutes" — a promise that rots. The tool names are DERIVED from the harness transcripts, ANCHORED on `"type":"tool_use"`: a hand-written list is defect ㊽, and matching every `"name":` counts SKILL and AGENT names too (measured: 343 false collisions against 1 real). Tri-state throughout — no corpus, or a corpus whose shape changed, answers NOT MEASURED, never 0.
- `test/scope-reach.test.js` — contract of the instrument above. Both wrong versions of the measurement are replayed as cases (the lucky hand-written list, the over-counting derivation) plus the two tri-state silences. A lucky probe teaches nothing: that is why the anchor is tested, not the answer.
- `src/derived-observables.js` — THE REGISTRY of the facts we DERIVE from what a harness sends (the directory a `cd X && …` designates · the paths it reconstructs word by word). Exists because ONE FIELD PER FACT is a tool: two hard-coded profile fields grew two near-identical engine branches in one evening, and every judge would have grown one too. Adding a fact = ONE entry, zero engine line — the reach table and the spec model both loop over it. Each entry carries a THUNK witness (a module-level literal is a static mutant, measured: 2 false survivors).
- `test/derived-observables.test.js` — the contract of that registry AS DATA (every entry addressable, its `from` naming a real key family, its witness really witnessing) plus the laws a parser owes: total, non-empty candidates (`includes("")` is always true — one empty candidate would fire every rule of the fleet), and no `cd` ⇒ no fact. Every law has its deterministic twin: fast-check is outside the Stryker runner.
- `src/scope-reach-pure.js` — the DECISION of that instrument, isolated from the I/O so it can be MUTATED. Holds the anchor on the tool-use entry (a bare name field also matches SKILL and AGENT names — 343 false collisions against 1 real), the `scope` flattening (an EMPTY pattern is dropped: it would make everything collide) and the collision predicate. TOTAL by contract: a third party owns the transcript format, so a malformed byte must never take the tool down.
- `test/scope-reach-pure.test.js` — deterministic cases AND the LAWS a scanner owes (total, round-trip, anchoring, dedup, growth never removes a name, patterns is a sub-sequence, norm idempotent). Properties are excluded from the Stryker runner, so every law also has its case here. Born of `/stack-audit` catching the logic shipped inside an I/O shell, hence unmutated.
- `test/mutation-floor-gate.test.js` — GATE: PER-FILE mutation floor. Stryker's `thresholds.break` is an AVERAGE ⇒ `canary.js` held at 89.23% with 7 survivors while the global showed 99.64%, green CI. Complements `break` (average) without replacing it. Floor 100, measured reachable by all mutated modules, ZERO exemption. Silent if `reports/mutation.json` is absent (npm test does not run Stryker). Negative-check with 6 parts on FABRICATED reports.
- `test/cascade-source-gate.test.js` — GATE: every caller of a cascade resolver (`modeForDoc`/`thresholdForDoc`/`driftUnitForDoc`/`enforceForDoc`) passes all 3 arguments, SOURCE included. Born from a REAL bug found by READING (09/08/2026): `pretool-core.js` and `explain.js` called with 2 arguments ⇒ cascade stage ② (`defaults.{source}`) invisible to them but applied by `gate.decide` ⇒ a `smart` doc in TURN units degenerated into `once`, SILENTLY. Check ① = behavior (the doc really comes back) · check ② = the CLASS (argument counting by parenthesis DEPTH, never `split(',')`) · negative-check including the nested-comma trap. Excludes `gate.js` (which defines the cascade) and tests (`gate.test.js` calls with 2 args to prove the fallback).
- `test/backlog-coherence-gate.test.js` — GATE: no heading of the maintainer's private work journal may announce OPEN while all its subsections are CLOSED. Skips cleanly when the journal is absent (public clone). Proves INTERNAL COHERENCE, never that a work item still exists (undecidable). IN-MEMORY negative-check + 2 anti-false-positive counter-proofs.
- `test/capacity-alarm.test.js` — CAPACITY ALARM: when an action's load exceeds the N frames, the badge SAYS so and names the setting (`frames`). REAL spawn (the alarm travels in `systemMessage`, the shell's dialect — an in-memory test would not prove it gets out). 3 parts: screams on the last frame · stays QUIET on intermediate frames (12 screams = unreadable alarm) · total silence when capacity suffices (real wiring). Closes the "nothing measures THROUGHPUT" hole: a deferral loses nothing but makes the agent ACT before the delivery ends, silently.
- `test/emission-duplicate.test.js` — ZERO DUPLICATES: an EMITTED segment never stays in the queue, and 6 successive actions never re-deliver a chunk. MIRROR of conservation (`budget.property`), which is blind to duplication — a segment delivered twice is perfectly "conserved". Born from the 06/08/2026 defect (chunk 7/8 delivered twice because of the parallel processes).
- `layers.json` — layer MANIFEST: the CAPABILITIES × LAYERS table (exit · stdout · env · argv) × (pure core · shared core · shell). Pure core DERIVED from stryker.conf.json. 🛑 Widening it is almost always the wrong answer — fix the file, not the table.
- `temporal-budget.json` — TIME BUDGET (20/08/2026): every temporal call of the repo DECLARED with its motive, of which only two are admissible (`distant` = no reachable authority · `undecidable` = alive-vs-frozen). Carries the POLICY, the RATCHET (declared count = measured count), the anti-inert witness per rule atom and the anti-vacuity floors. Measured: 9 calls over 5 files, all `undecidable`.
- `disk-writers.json` — DISK WRITER MANIFEST (21/08/2026): every runtime disk writer of `src/` DECLARED with its path (through `paths.js`, never a literal), its class, its budget and its eviction policy — or an explicit `policy: "none"` WITH a written reason and a work item, so an honest gap is LOUD instead of absent. Also carries the `fs`-method classification (the write set is DERIVED, never copied), the anti-inert witness per rule atom and the anti-vacuity floors. Measured: 10 writers over 57 runtime files, 15 distinct methods; ZERO declared gap left since WI-VENDOR-PATH closed on 2026-08-21 — every path now starts at a declared source, and the reachability check no longer DEMANDS that a gap exist (a floor that punishes the good behaviour gets disarmed).
- `rules/fs-call.yml` — ast-grep rule DETECTING a filesystem call, RECEIVER-anchored (`fs.$M`, `require('fs').$M`, the promise API and its usual alias). DETECTION ONLY, zero exemption. It matches READS too on purpose: it is the only half that sees an UNKNOWN method name, so the manifest must CLASSIFY every method met — that is what keeps the write set derived instead of a hand list that only knows the past. 🔴 Keyed on the receiver it was MEASURED fail-OPEN (21/08/2026: an aliased or double-quoted `require` went GREEN, i.e. SILENT), which is why the second, METHOD-anchored family is generated at gate time from the manifest's `write` entries.
- `rules/temporal-call.yml` — ast-grep rule DETECTING a temporal call (`setTimeout`/`setInterval`/`Atomics.wait`/`sleep`, bare or on a member). DETECTION ONLY, zero exemption: an `ignores:`/`files:` here would hide occurrences from the budget in silence (sealed by the gate). Option keys named `timeout` are deliberately out of scope — a BOUND is not a WAIT (40 occurrences measured) — and so is `setImmediate` since 21/08/2026: a TICK is not a WAIT, and no admissible motive fits it (1 occurrence, counted by the gate).
- `test/declfor-gate.test.js` — PROPAGATION GATE (06/08/2026): every decision key (DERIVED from gate.js's `xForDoc`) must be copied over by the `declFor` of `sources/mcp.js` AND `sources/skill.js`, which FILTER key by key. Born from `enforce` INERT for 24 h on the MCP channel — admitted by the validator, never transported. Distinct from the vocabulary symmetry gate: that one validates ADMISSION, this one PROPAGATION.
- `LANGUAGE.md` — PUBLIC language reference (English, root): AUTO blocks derived from the constants by `language-doc.js` (gate on drift), prose = judgement only.
- `HARNESS-CONTRACT.md` — the published harness CONTRACT (English, adopter-facing, repo root): required capabilities, optional ones + named degradations, self-serve proof recipe (`doctor --harness`).
- `src/harness-conformance.js` — ㊾ (15/08/2026): a harness's conformance from ONE real payload (required/optional/named degradations) + diagnosis of path-shaped keys unknown to the profile. PURE, mutated.
- `test/harness-conformance.test.js` — the module's deterministic suite (Stryker target).
- `test/language-atoms.test.js` — ATOM TABLE (㊻④, 15/08/2026): every source × projection × operator cell probed by BEHAVIOR on the real engine; a blind cell without a written justification = RED. ㊴/㊵ were empty cells of this table.
- `test/observable-reach-gate.test.js` — **THE SECOND FACTOR OF EXPRESSIVENESS**: can the language DISCRIMINATE on every fact of the harness contract, positively AND negatively? Observable list **DERIVED from `conformance({})`** (never written), cells probed by BEHAVIOUR, blind cell = written reason or RED, plus the inverse check on a stale cell. Born from the measurement that EVERY defect this project has had is a hole of the observable SET and none of the logic — the count is deliberately NOT written: an enumerable fact copied into prose is a copy, and a copy drifts. It did: 8 became 9. Found, before the need: the tool NAME is reachable negatively but not positively by substring.
- `test/language-completeness.test.js` — LANGUAGE EXPRESSIVENESS, COMPUTED (12/08/2026): enumerates ALL possible declarations over 3 atoms, computes each one's truth table by CALLING the real engine, closes by union. Measured fact and its characterization: the expressible set is EXACTLY `{f | f(empty action) = false}`. Born from a false promise ("any condition") that sat in the skill for 3 weeks: a language's expressiveness is COMPUTED, not proof-read. Part ④ anti-dormancy.
- `test/substrate-gate.test.js` — SUBSTRATE GATE (12/08/2026): which PROJECTIONS of the event does each source SEE? 2nd symmetry dimension, next to vocabulary — 2 of the 3 defects of 11-12/08 were SUBSTRATE blindnesses (㊵, ㊴ part ②), invisible to a gate that compares KEYS. PROBED by behavior (a unique witness placed in each projection), never by text. 4 parts: unjustified blindness · stale justification · every ADAPTERS source probed · discriminating witness (anti-lying-probe).
- `test/doc-drift-gate.test.js` — ANTI-DOC-LIE GATE (06/08/2026): every framework doc citing a `.js` file must prove that file EXISTS (repo · `src/sources/` · fleet). Closes the only DECIDABLE part of "the doc teaches the opposite of the code" — the silent rename. 3 parts: dead citations (+ anti-dormancy floor) · in-memory negative-check · anti-false-positive. Does NOT prove a doc tells the truth.
- `test/layers-gate.test.js` — the table's GATE, parsed by ast-grep (AST never regex: a `process.exit` in a comment is a false positive). 5 parts: violations · scan existence · stale justifications · layer sabotage · MENTION-vs-CALL sabotage. Does NOT cover imports (dependency-cruiser).
- `test/temporal-budget-gate.test.js` — TIME GATE (20/08/2026): every temporal call DECLARED with an admissible motive, ratchet as an EQUALITY (stale declaration = RED too). Perimeter DERIVED from `git ls-files` and files handed to ast-grep EXPLICITLY — a tree walk would honour `.gitignore` and could go blind without a word. 8 parts: budget · documented motives · anti-vacuity floors · anti-inert witness per atom DERIVED from the `.yml` · rule carries no exemption · IN-MEMORY sabotage of the 5 fault classes · MENTION-vs-CALL · the `setImmediate` OUT-OF-SCOPE clause (2026-08-21: a TICK is not a WAIT, so it is declared and COUNTED — 1 occurrence — instead of being enrolled under an invented motive; the atom must stay absent from the rule). Exports `verdict` (pure), which is what makes the sabotage possible without touching a real file.
- `src/temporal-budget-pure.js` — PURE CORE of the temporal gate (Stryker-mutated). The rule lives HERE, not in the test: Stryker does not mutate test code, a rule living there would be UNVERIFIABLE (same precedent and same remedy as `deps-criticality-pure.js`). Holds the CLOSED list of admissible motives, the ratchet as an equality, and the two-independent-`if` shape that keeps `>` → `>=` killable instead of equivalent.
- `src/disk-writers-pure.js` — PURE CORE of the disk-writer gate (Stryker-mutated). SPACE DECLARES ITSELF: whatever writes declares its ceiling and its eviction in the same gesture as its creation. Holds the CLOSED lists of classes and policies, the ratchet in BOTH directions (undeclared writer held at zero · stale declaration = dormant permit), and the rule that `policy: "none"` costs a written reason plus a work item. The rule lives HERE and not in the suite: Stryker does not mutate test code, so a rule written there would be UNVERIFIABLE.
- `test/disk-writers-gate.test.js` — the GATE: scans `src/` (perimeter derived from `git ls-files` through a `GIT_*`-scrubbing door, files named to ast-grep explicitly because it honours `.gitignore`) with TWO families — the receiver-anchored `rules/fs-call.yml` and a METHOD-anchored rule GENERATED from the manifest's `write` entries — then confronts the manifest. 10 parts: the verdict · four anti-vacuity floors plus a perimeter-identity check · a witness per rule atom · a witness FABRICATED per derived write primitive, bare and behind any receiver · the regression test of the three forms measured GREEN on 21/08 · no exemption in the rule · every declared gap reachable from the injectable doc · three IN-MEMORY sabotages (undeclared, stale, missing policy) · the AST-versus-mention counter-proof.
- `test/disk-writers-pure.test.js` — the module's DETERMINISTIC suite (Stryker target): both bounds of the 40- and 80-character justifications, zero as a legitimate budget, every legitimate class and policy, the pathSourceGap in all its broken forms, sorting, and the absent-table cases. Imported DIRECTLY (a re-export loses the `perTest` mapping), fixtures as THUNKS, expected messages hardcoded in full.
- `test/temporal-budget-pure.test.js` — the module's DETERMINISTIC suite (Stryker target): both sides of the 40-character bound, non-string justifications, several defects on one file, sort order, empty scan. Imported DIRECTLY (a re-export loses the `perTest` mapping = phantom survivors), fixtures as THUNKS (a module-level `const` is a static mutant), expected messages hardcoded in full.
- `quadratic-budget.json` — QUADRATIC BUDGET (third twin of TIME and SPACE): every nested traversal of the repo DECLARED with its class, from a CLOSED list (`O(N)` · `O(N log N)` · `DEBT`, which owes a NUMERIC impact · `INHERITED_DEBT`, exempt from justification ON PURPOSE — demanding one would produce INVENTED sentences). Keys are RELATIVE paths, never basenames (this repo holds homonyms). Carries the RATCHET (declared `max` = measured count), the anti-inert witness per rule atom and the anti-vacuity perimeter floor. 🛑 The baseline is GENERATED BY MEASUREMENT of THIS repository — an imported figure is an invented ratchet that protects nothing.
- `rules/no-undeclared-quadratic.yml` — ast-grep rule DETECTING a traversal nested inside a traversal: the 4 loop NODES (`for_statement`, `for_in_statement` = for..of AND for..in, `while_statement`, `do_statement`) **AND** the array methods (map/filter/find/includes/indexOf/some/every/reduce/sort…). DETECTION ONLY, zero exemption and zero glob exclusion. 🛑 NON-CAPTURING metavariables (`$_IT`, `$$$_A`): capturing, they unify across the two levels and `xs.map(x => ys.find(...))` goes green.
- `test/quadratic-gate.test.js` — COMPLEXITY GATE: every nested traversal DECLARED with an admissible class, ratchet as an EQUALITY (stale declaration and dormant permit = RED too). Perimeter DERIVED from `git ls-files` and files handed to ast-grep EXPLICITLY. 9 parts: budget · documented classes · anti-vacuity (perimeter floor + a CONTROL smuggled into a real scan, because ast-grep answers `[]` with exit 0 on an unresolved path) · anti-inert witness per atom DERIVED from the `.yml` · the metavariable trap · a linear search inside a loop · rule carries no exemption · IN-MEMORY sabotage · MENTION-vs-CODE. Witnesses and decoys in the OS tmpdir, never in the repo.
- `src/quadratic-budget-pure.js` — PURE CORE of the quadratic gate (Stryker-mutated). The rule lives HERE, not in the test: Stryker does not mutate test code, a rule living there would be UNVERIFIABLE (same precedent and remedy as `temporal-budget-pure.js`). Holds the CLOSED list of classes, the ratchet as an equality, the two-independent-`if` shape that keeps `>` → `>=` killable, and a `Set` membership so the judge is not its own first defendant.
- `test/quadratic-budget-pure.test.js` — the module's DETERMINISTIC suite (Stryker target): both sides of the 60-character and 40-character bounds, non-string justifications, the deliberate `INHERITED_DEBT` exemption, several defects on one file, sort order, homonym paths, empty scan. Imported DIRECTLY (a re-export loses the `perTest` mapping), fixtures as THUNKS, expected messages hardcoded in full.
- `docs/framework/quadratic-gate.md` — injectable doc of the five files above (fleet mirror, compared byte for byte by `mirror-sync-gate`).
- `test/budget-declare-gate.test.js` — GATE: the engine's budget FOLLOWS the limit declared to the harness. Reads the real Codex wiring and requires `additionalContextLimit` and `--budget` to be the SAME number, in the SAME block. Closes a LYING GREEN (11 actions instead of 1, all green). In-memory negative-check — never on the machine policy, which is in production.
- `src/budget.js` — EMISSION BUDGET, PURE, 100% mutated: **DELIVERS EVERYTHING** (03/08/2026). THREE paths — it fits ⇒ as-is; it overflows ⇒ FRAGMENTED (`fragment`) over N frames, RFC 2046/6455-style protocol; it overflows again ⇒ QUEUE (`orderSegments`, re-emitted at the next action). Undeliverability is impossible by construction; budget too small for the envelope ⇒ we UNSEAL and deliver. Knows NO harness threshold (the number comes from the shell).
- `test/budget.test.js` — budget.js's DETERMINISTIC suite (the one Stryker mutates: property tests are excluded from `vitest.stryker.config.mjs`, one flaky run per mutant would make the score lie). Anchors the EXACT texts of the header/announcement (they ARE the contract the agent reads) and the inclusive bound.
- `src/canary.js` — INJECTION CANARY (03/08/2026), PURE, mutated: decides `vivant`/`mort`/`indecidable` (alive/dead/undecidable) from what REALLY landed in the harness transcript. The ONLY witness that watches the other end of the pipe — everything else tests itself and would stay green if the harness changed its contract.
- `src/hooks/canary-check.js` — the canary's I/O shell (UserPromptSubmit, SILENT by contract): reads the transcript's TAIL (2 MB, never the whole — 104 MB measured in the fleet) and writes `state/canary.json`. On error: writes nothing, the previous verdict survives.
- `test/canary.test.js` — the canary's DETERMINISTIC suite (the one Stryker mutates): verdict, threshold boundary, totality, counting, negative-check.
- `test/canary-check.test.js` — REAL-SPAWN suite of the shell: healthy channel, DEAD channel, stdout muteness, bounded reading, verdict preservation on error.
- `src/leak-pure.js` — DECIDES whether a text carries personal data (PURE, mutated). Contains NO data to protect: patterns are built from the outside (OS account, home folder, private list outside the repo). Word boundaries mandatory — a short first name is a subword of common words. Also carries `forgottenRoots`/`normalizePath` (10/08/2026): which client roots does the gate REALLY look at? Every root seen must be DECIDED (derived or ignored with its reason).
- `test/leak-pure.test.js` — the decision's DETERMINISTIC suite (the one Stryker mutates): word boundaries, dedup, allowed RFC ranges, CGNAT block refused, totality, negative-check.
- `test/leak-gate.test.js` — THE GATE: scans TRACKED files (PUBLIC repository). Private terms DERIVED (client folders carrying their marker), never listed. Stays green on a pristine clone (generic mode). Negative-check via sabotage ON A COPY. **Part ② (10/08/2026)**: DISCOVERS the client roots on disk and requires each to be decided — born from a REAL leak (a social client invisible because only the SEO root was declared); it bit on its 1st run by finding a 3rd forgotten root.
- `test/deps-purity-gate.test.js` — META-GATE: checks that the `*-must-stay-pure` rules CAN redden. Born from a real bug (03/08/2026) where `lib-pure-must-stay-pure` was INERT — `includeOnly` kept the core modules out of the graph, so no rule could see them.
- `test/budget.property.test.js` — property-based (fast-check): CONSERVATION, bound, priority, determinism, seal, announcement + meta-test ⑦ COVERAGE (proves the generator reaches the mixed zone — two sabotages passed GREEN there before it).
- `src/hooks/codex-doc-inject.js` — CODEX PreToolUse shell (19/07/2026): pretool-core + Codex emit (WITHOUT permissionDecision outside deny).
- `test/codex-doc-inject.test.js` — spawn suite of the Codex shell (dialect only: degradation, key without agent_id, fail-open).
- `src/hooks/http-server.js` — HTTP shell (20/08/2026, ⛔ NOT WIRED ON PURPOSE): serves the SAME PreToolUse gate over a loopback socket, so a frame costs a local POST instead of a ~330 ms node startup. Frame coordinates travel in the URL, the response JSON comes from `doc-inject.output()` (never a twin). Claude Code only — Codex has no `http` handler. Listens on the INHERITED descriptor when the OS passes one (`inheritedFd`/`listenOn`, socket activation, Linux only — `LISTEN_PID` compared to our own pid because the variables are inherited), on the port otherwise, byte-identically to before. **Since 21/08/2026 it listens on BOTH transports**: the port (Claude Code's `http` handler takes a URL) AND the kernel rendezvous via `kernel-bind` (the client lane, and Codex which has no `http` handler). It listened only on the port before, while `state-client` knocked on the pipe — nobody was listening where the client knocks, and the three-OS proof could not see it because `state-daemon.test.js` forks its OWN test daemon.
- `test/http-daemon-lifecycle.test.js` — the daemon's three fears: watched set DERIVED from `require.cache`, the real-kernel proof that a DIRECTORY watch survives the rename-over a git checkout performs, non-zero stale-code exit code, and a NO-LEAK gate judged on the SLOPE (with its own SEEN RED against a deliberately leaky server).
- `test/http-lane-differential.test.js` — DIFFERENTIAL spawn lane ↔ HTTP lane: same payload, same frame, identical bytes (oracle = `doc-inject.js` REALLY spawned). Covers the multi-frame split, silence, `enforce`, fail-open, and carries its own SEEN-RED case (a shifted frame must be detected).
- `test/http-socket-activation.test.js` — the socket-activation contract, proven WITHOUT systemd (20/08/2026): a real listening socket really inherited on descriptor 3 by a real child, with `$LISTEN_FDS`/`$LISTEN_PID` set as sd_listen_fds(3) prescribes. Four layers: the DECISION (integers only, both variables required), the WIRING (a recording server proves `listen({fd})` vs the unchanged `listen(port, HOST)`), the REAL descriptor, and the FOREIGN-pid case proven twice — decision and end to end, because inherited variables make it the one defect that fails silently. SEEN RED on two sabotages sharing the criterion literally (an implementation ignoring `LISTEN_PID`, and one that never activates). ⚠️ The real-descriptor cases cannot run on Windows (Node: listening on an fd is unsupported there) — the CI matrix executes them on ubuntu and macos.
- `service/README.md` — the ONE place the three units' COMMON reasoning lives (20/08/2026, ⛔ nothing installed): the 5 things a unit must declare, the per-OS answer to "does exit 90 restart?", why Windows is a scheduled task and not a Windows Service, how each OS bounds the log, and the explicit list of what was NOT verified without a machine (no unit ever started, no Mac involved).
- `service/ctxroute-http.socket` — systemd USER SOCKET (20/08/2026): systemd owns the listening socket, so the daemon's stale-code exit 90 costs NO outage — connections queue in the kernel backlog and the next one starts a fresh instance, which is why the service no longer restarts. 🛑 `Accept=no` (`yes` would spawn one node process PER CONNECTION = the cost the whole lane deletes, and it is also what now guarantees a single instance), `ListenStream=127.0.0.1:` (the socket IS the boundary, there is no auth), `FlushPending=` deliberately ABSENT (it drops the queued connections that ARE the repair). Trigger burst sized in ACTIONS (16 frames), because hitting it makes the socket "not connectible anymore until restarted". Enable THIS, never the `.service`.
- `service/ctxroute-http.service` — systemd USER unit for `http-server.js` (`~/.config/systemd/user/`), SOCKET-ACTIVATED and `[Install]`-less since 20/08/2026. `Restart=no`: an eager restart was THE stale-code burst (a fresh instance every second, back into the running `git pull`) — re-adding `on-failure` re-arms it. What can still refuse a start is the start LIMIT, not the `failed` state. 🛑 `SuccessExitStatus=` deliberately ABSENT: systemd's own man page Example 1 declares 75 (`TEMPFAIL`) a CLEAN termination, which would silently disarm the stale-code restart. Log bounded by journald alone + a per-unit rate bound; start-limit burst raised because a `git pull` legitimately restarts it several times.
- `service/com.ctxroute.http.plist` — launchd USER AGENT (`~/Library/LaunchAgents/`, starts at login). `KeepAlive`/`SuccessfulExit=false` = restart ONLY on a non-zero exit ⇒ exit 90 restarts. 🛑 `StandardOutPath`/`StandardErrorPath` deliberately ABSENT (a plist log file is NEVER rotated — output goes to the OS log instead). Restates no launchd default: Apple's web sources are ARCHIVED, the authority is `man launchd.plist` on the target Mac.
- `service/ctxroute-http.task.xml` — Windows Task Scheduler definition (LogonTrigger, user scope, no elevation). 🔴 `RestartOnFailure` does NOT see the exit code ([MS-TSCH]: it covers failing to START) ⇒ the exit-90 restart is an `EventTrigger` on Operational event 201, UNVERIFIED without installing. Corrects three schema defaults that would kill a daemon SILENTLY (`ExecutionTimeLimit` PT72H, both battery flags).
- `service/install-linux.sh` — the SINGLE place the Linux procedure lives (21/08/2026): substitutes the machine-dependent `ExecStart` on the fly (nothing machine-specific ever enters a tracked file — the repo is public), enables the SOCKET and never the service, then DERIVES the port from the `ListenStream=` of the unit it just installed and prints it. Closes the duplication the README used to carry: a `systemctl` line copied into CI would be a second truth. Exits 78 (EX_CONFIG) when no systemd USER manager is reachable — "we could not MEASURE", never "the unit is wrong".
- `service/install-macos.sh` — same contract for launchd (`~/Library/LaunchAgents/`): substitutes both `ProgramArguments` BY SHAPE (matching today's placeholder text would stop substituting the day the plist is edited), `bootout` before `bootstrap` for idempotence, port DERIVED from the plist's `CTXROUTE_HTTP_PORT`. Distinguishes an unreachable DOMAIN (78, nothing was judged) from launchd REFUSING the job (1, a real defect) — the domain is a parameter because the machine decides it, not us.
- `service/install-windows.ps1` — same contract for Task Scheduler. 🔴 STRIPS the XML prolog before `Register-ScheduledTask` (PowerShell hands a UTF-16 string, the prolog declares UTF-8, the API refuses with `HRESULT 0x8004131a` — measured 21/08/2026); the file KEEPS its prolog because `schtasks /Create /XML` needs it. Enables the Operational channel WITH its ceiling first (a PREREQUISITE: disabled = the restart trigger is inert IN SILENCE) and reads that state through `Get-WinEvent`'s `IsEnabled` boolean, never through localised `wevtutil` text. No unit carries the port on Windows (the schema has no element for it) ⇒ it is read from the module's own `DEFAULT_PORT`, never re-typed.
- `service/verify-responds.sh` — "does the daemon ANSWER?", the ONE proof shared by the three OSes and by the operator after a manual install: a REAL POST on the hook lane whose body must parse as JSON — a process listing proves node runs, which nobody doubted. The port is RECEIVED, never re-typed. Its retry loop is a declared BOUND (no OS event says "the port is now bound"), never a health verdict; exhaustion fails loudly.
- `.github/workflows/service-units.yml` — the three units LOADED BY A REAL SUPERVISOR (21/08/2026): we own no Mac, GitHub rents one by the minute. Three independent jobs, each calling its OS's installer (zero command rewritten here), starting the service through its supervisor and then POSTing to it. `workflow_dispatch` + `push` on strict `paths:` only — a macOS runner bills ×10, and that filter is what makes the job payable. TRI-STATE: an installer's exit 78 becomes a LOUD, NAMED "precondition not met", never a silent green. Its header declares the four things NO runner can prove (logon trigger, reboot, wake from sleep, the exit-code restart end to end).
- `docs/framework/service-units.md` — injectable doc of the three service units (fleet mirror): the per-OS answer on exit 90, the two silent killers (systemd `TEMPFAIL`, Windows defaults), and the ban on any probe/heartbeat/PID file.
- `test/service-units-gate.test.js` — GATE (20/08/2026): the daemon's exit status stops living in 5 hand-written copies. DERIVED from the `EXIT_STALE_CODE` export (zero literal in the test) — every unit + README + injectable doc must name the LIVE code, and an ACTIVE `SuccessExitStatus=` in the systemd unit is RED (the prose warning that motivated 75 → 90, turned into a machine; comment lines are NOT flagged, or greening it would mean deleting the explanation). 🔴 Its first version was SEEN FAILING TO REDDEN: a retired code was tolerated ANYWHERE, so `exits 75` passed under cover of the legitimate manual quotation ⇒ a retired code is now legal ONLY in the quotation form `Exit status N`. `HISTORICAL` + INVERSE check (an allowance nothing quotes any more is RED), 3 anti-vacuity floors (the 5 files must EXIST — a dead path looks EXACTLY like 5 perfect units), and a test-only root env var so the negative-check runs on a sabotaged COPY in tmpdir, never on the repository. ⚠️ `FILE-MAP.md` is deliberately OUT of its corpus: it narrates every tool's exit codes (`exit 1`/`exit 2`…), so scanning it would flag unrelated ones — the 4 lines above are maintained by hand.
- `src/corpus.js` — shared I/O: recursive read of the fleet's .md files (ids identical to the docs' ids).
- `src/memory-store.js` — the state of a LIVING daemon: the SAME API as `session-store`, held in memory. I/O SHELL only (snapshot read/write); every decision lives in `memory-store-pure.js`. The snapshot is written by the SINGLE owner and read ONCE at startup, before the socket opens — so no lock, no window, no retry on contention. The write stays ATOMIC for the CRASH, not for a reader. No `fsync`: a DECISION (this state describes a living session, none survives a reboot).
- `src/memory-store-pure.js` — the DECISIONS of that state, isolated so Stryker can MUTATE them (an I/O file is never mutated: the LRU and the ceiling would have shipped measured by nothing — same precedent as `scope-reach-pure.js`). Holds the key, the LRU where READING counts as a use, the `MAX_SCOPES` ceiling applied in the same gesture as the write, and the SHAPE validation of a snapshot (`JSON.parse` succeeding is not the guardrail). **Two budgets since 21/08/2026** (`MAX_SCOPES` / `MAX_EPHEMERAL`): a `plan-` key is born at every tool call and dies with its action, a scope key lives as long as its agent — under one shared ceiling the ephemeral flood evicted the durable, i.e. one busy agent erasing the whole fleet's memory, silently and WORSE with scale.
- `src/kernel-endpoint.js` — WHERE the daemon and its clients meet, per kernel: named pipe (Windows, kernel object namespace), ABSTRACT socket (Linux, nothing on disk), socket file (macOS, the only one leaving an entry — declared, never emulated). Address DERIVED from the repository root and HASHED: two clones must never share a daemon, and a real path carries a user name (public repo).
- `src/kernel-bind.js` — taking the rendezvous address on a kernel that leaves traces. macOS keeps a real socket FILE that Node unlinks only on a clean `close()`, so a KILLED daemon blocks the next one with `EADDRINUSE` while nothing listens (found by CI, only there). Clears it — but NEVER on a guess: the kernel is asked whether anyone answers (`connect` succeeds = alive, `ECONNREFUSED` = dead). No probe, no delay as a verdict.
- `test/kernel-bind.test.js` — the five branches of taking the address, driven with an INJECTED probe and unlink: the decision is only ever REACHED on macOS, so testing it indirectly meant testing it on one kernel out of three. The branch that must do NOTHING (a living owner) is the dangerous one — deleting there is silent and undetectable.
- `src/hooks/state-client.js` — a spawned hook ASKS the daemon instead of writing a file. Same HTTP protocol as the network lane (`http.request({ socketPath })`), so Codex — which has no `http` handler — becomes a client and loses the files, the lock and the flaky. No daemon ⇒ an IMMEDIATE kernel fact (`ENOENT`/`ECONNREFUSED`), never a timeout, never a liveness probe.
- `src/client-core.js` — ONE AUTHORITY, OR NONE (21/08/2026, ⛔ NOT WIRED ON PURPOSE): a spawned frame ASKS the daemon; no daemon ⇒ it decides LOCALLY, delivers `injectLockless` (everything except `once`) and **writes NOTHING**. A fallback that wrote would make TWO memories, and a `once` delivered by one would be re-delivered by the other, in silence. The empty state here is a FACT (no file, no daemon), the exact opposite of the spawn lane where reading the file is mandatory. Zero timer: absence is an immediate kernel fact.
- `src/corpus-cache.js` — the daemon's RESIDENT corpus: one entry per root, invalidated by a kernel DIRECTORY watch (a file watch follows a dead inode after a git rename and goes silently deaf), ceiling 8 roots, OFF unless `corpus.enableCache()` is called — which is why every differential still runs on the unchanged path. Measured: the round trip is 41.49 ms of which 0.17 ms is transport; the 41 ms were this re-read.
- `src/state-eviction-pure.js` — decides WHICH state files disappear: age for the ephemeral class (derived from `deadline.DEFAULT_MS` x 10, so the bound follows the deadline instead of being a second number), a COUNT per class for the durable one, and FAIL-CLOSED on an unknown name — it is never deleted, only REPORTED. Pure, mutated.
- `src/state-eviction.js` — the shell: lists `state/`, unlinks what the pure module names, fail-open twice. Triggered from `turn-count.js` (once per human turn, one process), never from a state WRITE (16 frames x every action) and never a timer.
- `test/state-eviction-pure.test.js` — the deterministic cells of the eviction rule, in the Stryker runner.
- `test/state-eviction.test.js` — the real-directory cell: the EXACT survivors and the EXACT removals, because an eviction is proven by what it DELETES; plus the confrontation of the class list with the reset's own literal.
- `test/corpus-cache.test.js` — freshness (a doc changed on disk is served new on the next request, no restart), fail-open, ceiling, off-by-default.
- `test/fleet-hooks-path.test.js` — WI-VENDOR-PATH: the fleet root has ONE definition (`FLEET_SEGMENTS`) and TWO projections that are both CONSUMED and TIED. 10 cells — equality (a refactor cannot silently move a directory we WRITE into) · `vendor-deadline.js` really consumes the accessor, proven by a REAL SPAWN against an override rather than a grep · the CLASS, not the case: nobody re-assembles the root anywhere in `src/` + `tools/`, perimeter DERIVED from `git ls-files`, segments taken from `fleetHooksSegments()`, detection by AST, and a FABRICATED offender smuggled into the very scan that delivers the verdict (proof of life must ride WITH the judgement — `ast-grep` answers `[]` with exit 0 on an unresolved path, so a broken scan and a clean repo look identical) · perimeter floors plus an identity check · a witness per rule atom, derived from the `.yml` · the MENTION-vs-CODE counter-proof (four files document the forbidden form in comments) · no exemption inside the rule · the PUBLISHED contract `.claude/hooks` asserted LITERALLY, the one place it is written as a literal · the NO-LEAK cell (the label is relative, home-free and immune to the test override — emitting the absolute accessor would put a real user path into every injected document of a public repo) · the two projections tied together, because separate is not the same as unrelated. 🛑 No pure module on purpose, and the reason is written in the file: there is no policy to mutate here, only one `every` and one `!==`.
- `rules/fleet-root.yml` — ast-grep rule DETECTING a path CONSTRUCTION (`join`/`resolve` bare AND behind any receiver, plus binary concatenation). DETECTION ONLY, zero exemption; it deliberately matches innocent joins because the DECISION (does this carry every segment of the fleet root?) belongs to the gate, and those segments are DERIVED from `paths.fleetHooksDir()` so no copy of them lives in the detector. Keyed on the METHOD, never the receiver — the fail-OPEN lesson already paid on `rules/fs-call.yml`. Declared blind cells: a root built inside a template literal, through a third-party helper, or from segments held in far-apart variables.
- `test/shadow-relic-extinct.test.js` — the shadow relic is gone and NOTHING designates it any more (files absent + zero surviving citation, with an anti-vacuity floor).
- `test/scale-bench.test.js` — SCALE BENCH: how per-request cost and retained memory GROW from 128 to 1024 agent scopes. Ratios only, never a millisecond; margin 1.67 DERIVED from the measured pair (healthy 0.89, sabotaged 3.13) and printed on every run so it can be re-derived. SEEN RED against a store that walks the whole state per write.
- `docs/framework/corpus-cache.md` — injectable doc of the resident corpus (the 41 ms are the re-read, not the pipe).
- `docs/framework/scale-bench.md` — injectable doc of the bench (what it proves, and what it does NOT).
- `docs/framework/state-eviction.md` — injectable doc of the eviction.
- `src/lifecycle-log-pure.js` — decides WHAT a daemon lifecycle record looks like and WHEN the journal turns over. Closed event vocabulary (a name outside it renders `null`, which is what keeps the writer from ever growing with traffic), one-line-per-record enforced on values this process does not author, and a fail-open rotation at 256 KB. Pure, mutated. Shape copied from the operator's fleet reaper — one convention, never two — but RE-IMPLEMENTED, because a public repository must not depend on a personal fleet tool.
- `src/lifecycle-log.js` — the I/O shell of that journal: `stat`, one `rename` onto `.1` that OVERWRITES (that single rename IS the two-file ceiling) and an append, all under one `catch`. 512 KB for life, at any traffic and any uptime, declared in `disk-writers.json`. Address through `paths.stateDir()`, never a literal. It exists because the daemon restarted NINE TIMES IN ONE HOUR on 22/08/2026 with exit 90 and left no trace at all.
- `test/lifecycle-log-pure.test.js` — the deterministic cells of the rule, inside the mutation runner: the ceiling asserted literally, the vocabulary asserted literally AND frozen, every declared event proven to render, a heartbeat/per-request event proven to render NOTHING, and the newline forgery closed on both the instant and the values.
- `test/lifecycle-log.test.js` — the ceiling PROVEN BY OVERFLOWING IT at the real 256 KB (file count and total bytes asserted, plus an anti-vacuity floor on the rotated file, because a rotation that never fires passes every bound), the predecessor proven DESTROYED rather than archived, the fail-open paths, and the derived check that the daemon shell still emits every declared event.
- `docs/framework/lifecycle-log.md` — injectable doc of the daemon journal.
- `docs/framework/store-ownership.md` — injectable doc of the single resolution point.
- `src/store-resolve.js` — the SINGLE point that says which memory a shell decides on, and it hands out `{ store, withLock }` TOGETHER so the incoherent pair cannot be built (memory + file lock protects nothing; disk + empty lock IS the production bug of 07/08/2026). Born of a defect MEASURED on 21/08/2026: with the gate on the daemon and the three other consumers on disk, a real PreCompact left the daemon answering 2 bytes — skills and `once` documents gone after every compaction, silently. An unknown backend is a NAMED refusal, never a quiet fallback to the disk.
- `src/turn-core.js` — the turn counter's read-modify-write, in ONE place, shared by the spawned shell and the daemon: a rule about the shape of a store, read in two places, diverges (paid twice — ㊱, ㊳).
- `test/store-resolve.test.js` — the pair travels together, the default is unchanged to the byte, an unknown backend is refused: the three ways a split brain could be rebuilt.
- `test/daemon-purge.test.js` — after a REAL PreCompact the daemon must forget: the real reset shell spawned against a live daemon, the `once` must come back. The silence in the middle is the anti-vacuity — without it the cell passes on a daemon holding no state at all.
- `test/client-core.test.js` — the daemon's answer is emitted VERBATIM and nothing is re-decided behind it · with no daemon the store can neither know nor write and the lock never grants · the inert store leaves nothing on disk, PROVEN against a control where the real store does write · a real address nobody listens on yields an immediate verdict.
- `docs/framework/client-lane.md` — injectable doc of the client lane: switching over is ONE LINE in each EXISTING shell (`run(...)` → `client.run(...)`), never a new file — a second shell per harness was written and deleted on 21/08/2026 because it multiplies lanes x harnesses. Also carries the DATED Windows measurement that refuted the obvious release plan: `MoveFileExW` reports an error on an existing directory, [MS-FSA] refuses a directory rename normatively, and `atomic` appears on none of the pages. Keep the citations — without them the next session redoes the research.
- `test/memory-store.test.js` — the same contract as the disk store, prefix isolation, a hostile key (`__proto__`) as DATA, the real ceiling and its LRU (reading protects), purge by prefix, snapshot/restore across two daemons, and fail-open on a corrupt or MALFORMED snapshot.
- `test/kernel-endpoint.test.js` — the address, per kernel, by ARGUMENT (a pure function): the namespace Windows imposes, the leading NUL that makes Linux abstract, the macOS file, two clones separated, one directory = one address whatever the spelling, and no path fragment leaking into the address.
- `test/dual-transport.test.js` — the REAL server answers on the port and on the kernel rendezvous, BYTE FOR BYTE the same, with an anti-vacuity floor (two empty answers are also equal); and an address nobody owns fails IMMEDIATELY with a kernel code, never a timeout. Seen red by opening only one listener. ⚠️ Its second half FORKS THE PRODUCTION SHELL (`require.main` block, reachable by no require — a copy would be one more twin): a rendezvous the kernel refuses degrades ONE lane and says so on stderr while the port keeps ANSWERING · `EADDRINUSE` on that same rendezvous KILLS · a tree with no `state/` gets it created BEFORE the bind (checked at the instant the lane starts listening — later the snapshot would have created it). Readiness comes from the kernel's `listening` event, forwarded by a `--require` preload; zero polling, zero timer.
- `test/state-daemon.test.js` — THE proof: **16 real processes, one `once` document, delivered EXACTLY once, with no lock and ZERO file written**. Plus the anti-vacuity control (the daemon is not mute), the memory really consulted (a second action is silent), and "no daemon" settling AT ONCE on a kernel fact.
- `docs/framework/kernel-state.md` — the injectable doc of those four modules: the kernel for what is alive, the disk only to survive the death of the process.
- `src/session-store.js` — shared per-session state I/O (distinct prefixes: `ctxroute-seen-` servers / `doc-seen-` docs). Extracted via the jscpd gate. ATOMIC write (tmp + rename, bounded retry on Windows EPERM).
- `test/session-store.test.js` — state-write atomicity: 2 real processes (writer/reader), zero hollow reads, zero lost writes, zero abandoned `.tmp`.
- `src/hooks/session-inject.js` — SESSION GATE (SessionStart, wired 17/07/2026): injects ALL `docs/session/*.md` at every session start AND after every compaction (the framework-managed "CLAUDE.md"). Zero state, zero dedup, fail-open.
- `src/sources/session.js` — "session" SOURCE: docs/session corpus → ordered docs (alpha, frontmatter stripped). PURE, mutated 13/13.
- `src/docfacts.js` — GENERIC CORE "a doc can no longer state a fact the code contradicts": `<!-- AUTO:name -->` blocks extracted/compared/regenerated. PURE, zero dependencies, knows NEITHER ctxroute NOR a path — reusable as-is in another repository. Built-in anti-dormancy (zero facts = FAILURE).
- `test/docfacts.test.js` — the core's contract on SYNTHETIC data (18 cases): absent block ≠ empty block, unclosed marker, homonym, idempotence, check⟷write round-trip, anti-dormancy in the 3 forms of emptiness.
- `test/docfacts.property.test.js` — PROPERTIES (fast-check) of the encode↔decode pair, REQUIRED by the doctrine (parser/serializer). Round-trip, idempotence, repair (--write always satisfies the check), TOTALITY. ⚠️ FOUND a real defect on 14/08/2026: a `nom` with a broken `toString` made the core THROW — hence the injection gate die. Excluded from the Stryker runner ⇒ every guard has its deterministic case in `docfacts.test.js`. Also pins the KNOWN LIMIT (content carrying the end marker is truncated on re-read).
- `tools/language-doc.js` — SPECIFIC SHELL: derives the facts of `docs/session/language.md` + `LANGUAGE.md` from `frontmatter.js`/`sources/file.js` (constants IMPORTED, never copied). CLI `node tools/language-doc.js [--write]`. `faits()` core PURE; I/O and exit code live under `require.main`.
- `test/language-doc-gate.test.js` — THE GATE on the REAL corpus (6 parts): non-empty facts · no stale block · vocabulary COMPLETENESS readable in the doc · in-memory negative-check · `--write` repairs what the check refuses · anti-copy (a generated fact is not restated in prose).
- `docs/session/language.md` — THE LANGUAGE, derived from the code (closed vocabulary, triggers, `scope`/`exclude` and their asymmetry, cadence, MEASURED expressiveness). The ONLY VERSIONED `docs/session/*.md` (named exception in `.gitignore`: zero personal data, generic) — the fleet only keeps a pointer, so this is the single source. ⚠️ Delivered at every session AND after compaction; update it when the engine changes, NEVER copy its rules elsewhere.
- `src/sources/skill.js` — "skill" SOURCE (18/07/2026): `config.skills` registry → skills triggered by PERIMETER. Dimensions REUSED (file via matchingDocs, MCP via lib.serverName), deduped union. The adapter injects the skill's BODY read live (paths.skillsDir(), harness frontmatter stripped), pointer fallback if the file is unreadable. PURE, 100% mutated.
- `src/collisions.js` — PURE CORE of the rule-crossing analysis (3 sort levels, verdict = an AGENT, never a gate). 100% mutated.
- `tools/check-collisions.js` — on-demand shell (`node tools/check-collisions.js [--json]`), source = frontmatters via loader.
- `src/hooks/doc-write-guard.js` — WRITE GUARD (PostToolUse Write|Edit, wired 17/07/2026): Claude Code shell (direct file_path); common body in guard-core.js. Validation delegated to frontmatter.js.
- `src/guard-core.js` — PostToolUse GUARD CORE (19/07/2026, multi-harness single source): docKind + multi-file validation, run(filePaths) → block/silence.
- `src/hooks/codex-doc-write-guard.js` — CODEX PostToolUse shell (19/07/2026): paths extracted from the apply_patch patch (tool_input.command) via sources/file.js#extractFilePaths (the SAME parser as the input match).
- `test/codex-doc-write-guard.test.js` — spawn suite of the Codex guard (block on invalid patch, multi-file, out-of-fleet silent).
- `docs/framework/` — EVERYTHING FOR A FORK (19/07/2026): versioned mirror of the skill (`SKILL.md`) + of the framework's injectable docs. ⚠️ Edit the FLEET (wired) then copy HERE in the SAME move — drift = RED (mirror-sync-gate).
- `test/mirror-sync-gate.test.js` — fleet↔repo GATE: skill + every fleet injectable doc == repo mirror byte-for-byte, zero omission, zero orphan; clean skip when the fleet is absent (CI/fork).
- `src/vendor-deadline.js` — vendors `deadline.js` into the hook fleet + arms the fleet's hooks. Dry-run by default, idempotent, insertion BEFORE the 1st executable line, reports (never guesses) unpatched files.
- `test/git-env-door-gate.test.js` — GATE (21/08/2026): every spawn of the `git` binary in a tracked JavaScript file passes an env that strips the WHOLE `GIT_*` family. Born from a MEASURED defect — git exports `GIT_DIR`/`GIT_INDEX_FILE` to its hooks, a child INHERITS them and they BEAT `cwd`, so a `git add` aimed at a tmpdir sandbox staged its trap file into the REAL index and reached a commit immune to three `--amend`. Spawn primitives DERIVED from the `node:child_process` imports (a list is born stale), call sites detected by `ast-grep` (a spawn quoted in a comment is a MENTION), scrub PROVEN from the binding's own declaration and FAIL-CLOSED otherwise. Four anti-vacuity floors, one witness per derived primitive, in-memory sabotages naming file and line — and it judges its own `git` calls.
- `test/ps1-bom-gate.test.js` — GATE (21/08/2026): every tracked `.ps1` carrying a byte ≥ 0x80 starts with the UTF-8 BOM `EF BB BF`. Born from a real CI run — PowerShell 5.1 reads a `.ps1` as ANSI without the BOM, and 2,045 non-ASCII bytes killed `install-windows.ps1` with a parser error before its first line. Files read as BUFFERS (the decoder turns the BOM into `U+FEFF`, so a text comparison cannot tell the fatal file from the fixed one — proven here on real files). A pure-ASCII `.ps1` is EXEMPT and the exemption is stated in the failure message, so nobody widens the rule by accident.
- `docs/framework/env-and-encoding-gates.md` — injectable doc of those two gates (fleet mirror): the inherited-`GIT_*` law, the one-door rule, the fail-closed scrub proof, the PowerShell 5.1 ANSI fact and the deliberate pure-ASCII exemption.

**Config & state**:
- `deps-criticality.json` — dependency criticality MANIFEST: every dependency of every `package.json` is classified `moteur` (engine: determines the shipped output ⇒ EXACT pinning mandatory) or `ordinaire` (ordinary). Unclassified = RED — deciding IS the point.
- `src/deps-criticality-pure.js` — PURE CORE of the criticality gate (Stryker-mutated). The rule lives HERE, not in the test: Stryker does not mutate test code, a rule living there would be UNVERIFIABLE.
- `ctxroute-config.json.example` — generic shipped config (the real `ctxroute-config.json` is gitignored: it carries skill/project names = personal data).
- `package-lock.json` — dependency lock (`npm ci`). Never edit by hand.
- `ctxroute-config.json` — config (mode + thresholds + filters). Applied immediately, no restart. `$schema` → IDE validation.
- `ctxroute-config.schema.json` — the config's JSON Schema (closed enums, strict keys). Drift-test in config-gate.test.js: a config key outside the schema = RED (the testserver999 bug class).
- `docs/mcp/{server}.md` / `{server}/{tool}.md` / `{server}/{subTool}.md` — per-server/tool/sub-tool docs. ⚠️ Gitignored (real personal invariants). Generic versions pushed to GitHub = `{server}.md.example`.
- `state/ctxroute-seen-<session_id>.json` — runtime state (auto-generated, purged after 30 days).
- `state/.lock-<session_id>/` — temporary lock folder (exists only during the critical section, never committed).

**Tests & quality** (all MANDATORY, never temporary):
- `test/lib-pure.test.js` — pure unit tests (zero spawn), Stryker target. ⚠️ NEVER write a test count here: two skills already went out of sync on it — the runner is the only source.
- `test/explain.test.js` — `explain` suite by REAL SPAWN on a throwaway fleet. ⚠️ Contains the 2 FOUNDING CASES replaying the false greens of 31/07: NEVER delete them — if the behavior changes, the verdict is INVERTED (done for the wildcard), the case stays.
- `test/deps-criticality-pure.test.js` — DETERMINISTIC tests of the criticality core (Stryker target), adversarial edge cases included (`/regex/.test(['1.2.3'])` is TRUE by JS coercion — the `typeof` guard is NOT decorative).
- `test/deps-criticality-gate.test.js` — GATE: every real dependency is classified, an engine dep is pinned EXACT, a GHOST entry is RED. ⚠️ Empty perimeter here (zero runtime dependencies) ⇒ mandatory ANTI-DORMANCY test, otherwise the gate would sleep green.
- `test/coverage-gate.test.js` — COVERAGE GATE (DERIVED checks, never a list): ① every `.js` receives an injectable doc · ② every tracked file is in THIS map · ③ every module is in the dependency-cruiser `includeOnly` (outside the list = coupling gate GREEN while analyzing nothing) · ④ no doc GROWS (ratchet + debt that can only shrink). Born from an audit that found 5 omissions, 3 of them pre-existing.
- `test/triggers-gate.test.js` — GATE: every trigger of `TRIGGERS` MUST be proven consumed by a REAL source (a call, never a copied list); no `validate` message may advise a rejected key. Adding a trigger without its proof case = RED.
- `test/operator-consumption-gate.test.js` — GATE: every NARROWING operator (list DERIVED from `RULE_KEYS`) must CHANGE the engine's decision on EVERY dimension carrying matching operators (file, tool, skill×4). Probed by behaviour, anti-vacuity on each base case; part ② requires the MCP corpus to REFUSE those operators rather than accept-and-ignore them. Born because `scope`/`exclude` (12/08) then `keys` (19/08) each shipped INERT on one dimension, under green tests and 100 % mutation.
- `test/config-gate.test.js` — GATE: the COMMITTED config must cover every documented server. Dead-man switch born from the "framework silently disabled" bug (15/07/2026).
- `test/collisions.test.js` — DETERMINISTIC tests of collisions.js (Stryker target, internal bricks tested directly).
- `test/doc-write-guard.test.js` — guard integration (spawn, tmpdir fleet): block on typo/forbidden key, silence on healthy/session/out-of-fleet, fail-open.
- `test/sources-session.test.js` — DETERMINISTIC tests of `sources/session.js` (Stryker target).
- `test/sources-skill.test.js` — DETERMINISTIC tests of `sources/skill.js` (Stryker target): skillRules, matchingSkills (file+server union, dedup), serverMatches, declFor, MODES contract hardcoded.
- `test/skill-registry-gate.test.js` — GATE: every skill in `config.skills` EXISTS in the harness. Rename/deletion = ghost pointer = RED. REVERSE DIRECTION (19/07), EXPLICIT OPT-IN: only active when the `skillsWithoutPerimeter` key is present (even []) — the adoption switch of the zero-silence discipline (a language never imposes a policy). Active → every harness skill MUST be registered (`skills`) OR declared perimeter-less, else RED. `findMissing`/`findUndeclared` negative-checked. Skipped on a pristine clone (env CTXROUTE_SKILLS_DIR for tests).
- `test/session-inject.test.js` — session gate integration (spawn, tmpdir): order, SessionStart contract, fail-open, enabled:false.
- `test/sources-file.test.js` — DETERMINISTIC tests of `sources/file.js` (Stryker target). ⚠️ Created after an audit: the refactor's core had NO unit test, its only coverage was the 75-min differential (unlaunchable by Stryker).
test/keys-operator.test.js - contract of the `keys` operator (WHERE to look): both forms (flat = the three axes, object = one universe per axis), whitelist REPLACES / blacklist REMOVES, per-axis independence, and the two halves that matter together — citing a project in a command no longer injects it, working in it still does. Also the runtime robustness a hand-edited config needs (mixed list read the most restrictive way, non-list `keys` inert and never a throw). Launched by Stryker (vitest.stryker.config.mjs) and wired in mutation.yml `paths:` — a suite Stryker runs must trigger the workflow.
- `test/sources-mcp.test.js` — DETERMINISTIC tests of `sources/mcp.js` (Stryker target): corpus ids, global→specific order, filters, declFor.
- `test/sources-tool.test.js` — DETERMINISTIC tests of `sources/tool.js` (Stryker target): exact/case match, scope/exclude, totality, order.
- `test/mcp-differential.test.js` — MCP DIFFERENTIAL: spawns old (legacy-mcp-inject) vs unified gate over 9 sequences (modes, overrides, granularity, filters) — parity gate of the legacy removal. 60 s timeout per test (spawns under load).
- `test/gate.test.js` — DETERMINISTIC tests of gate.js (Stryker target).
- `test/gate.property.test.js` — property-based tests of gate.js (fast-check, generated inputs). NEVER run by Stryker (non-deterministic): every invariant ALSO has its deterministic case in gate.test.js.
- `test/doc-inject.test.js` — gate integration (spawn, tmpdir corpus): allow/dedup smart/fail-open, zero state writes on a dumb corpus.
- `src/oracle.js` — the ONLY reader of `protect-files.js` output (spawns it, parses the JSON, then splits the blocks and keeps the LAST `[source:]` marker of each — 61 docs carry a hardcoded one in their body). ONE consumer left (`test/file-differential.test.js`): NOT a reason to inline it back, the rule guarded here is "one parser", violated the moment a SECOND caller improvises one (experienced 3× on 15/07/2026).
- `src/differential-normalize.js` — SHARED NORMALIZATION for the 2 differentials: strips the `[DOC i/T]` ordinal before comparison (the oracle is FROZEN, it ignores whatever was born after it) AND `reassemble(frames)` glues a CHUNKED multi-frame delivery back into the whole document, by the protocol's own fields (`FRAME k/N`, `CHUNK j/m`, shared end marker) — so parity no longer stops at docs that FIT one frame. Anchored on the source tag, never a blind erasure; fail-closed, zero fallback (`{ok:false, reason}` ⇒ the caller stays RED). SINGLE SOURCE — never a copy inside a suite.
- `test/differential-normalize.test.js` — NEGATIVE-CHECK of the module above: an untested comparison filter can swallow a REAL regression. Key parts = a `[DOC x/y]` in a doc's BODY SURVIVES, and a divergence in the LAST chunk turns RED (the blind spot of the prefix comparison it replaced). Fixtures composed by the ENGINE (`budget.js::planFrames`), never hand-written.
- `test/pretool-differential.test.js` — GATE DIFFERENTIAL: old vs new engine on the REAL fleet, injected content BYTE-FOR-BYTE + mirrored decision. It DRIVES the frames of one action (`--frame k --frames N`, one `tool_use_id` shared by them) and compares the REASSEMBLED document for EQUALITY. Parity gate of the switchover. Skipped on a pristine clone.
- `test/gitignore-gate.test.js` — STATIC GATE: no `state/` file (whatever its format) is tracked by git. Born from a real incident (16/07/2026: shadow `.jsonl` journal not covered by the per-extension pattern → real payloads pushed to GitHub). `state/` = PRIVATE runtime, never committable.
- `test/frontmatter.test.js` — DETERMINISTIC tests of the parser (Stryker target). ⚠️ It used to have ONLY properties → 100% of its mutants would have survived.
- `test/loader.test.js` — DETERMINISTIC tests of the loader (Stryker target), including the interleaving trap and the 25-element cases (real TimSort — V8's insertion sort <23 elements hides the comparator's mutants).
- `test/lib-pure.property.test.js` — property-based (fast-check): security/totality invariants on generated inputs. Found the `serverName` hole that hand-written cases had missed.
- `test/doctor.test.js` — the doctor's NEGATIVE-CHECK: sabotages a COPY of the framework (tmpdir) and requires it to scream. Without that, the dead-man switch might never fire.
- `test/turn-count.test.js` — TURN gate integration (real spawn): per-turn increment, stdout ALWAYS empty (UserPromptSubmit contract), isolated sessions, enabled:false, fail-open, PreCompact reset.
- `test/deadline.test.js` — PROOF by real spawn that the deadline kills a zombie, including the negative-check that REPRODUCES the bug (without a deadline → the process does not die) and the anti-latency test (unref).
- `test/deadline-load.test.js` — the deadline must NEVER kill legitimate work. Gate on the VALUE (≥ 15 s) + proof under 24 simultaneous spawns. ⚠️ Born from a REAL production regression (2 s threshold → 19/24 hooks exited without injecting). The test SELF-VALIDATES at rest (its 1st version targeted a rule-less path → false red).
- `test/deadline-gate.test.js` — STATIC gate: every stdin-reading file (direct OR via `stdin-json.js`) MUST arm `deadline.arm()`. ⚠️ Its 1st version only looked for `process.stdin` → GREEN while analyzing NO real hook. A blind gate CERTIFIES instead of protecting.
- `test/frontmatter.property.test.js` — property-based tests of the parser (totality/round-trip/idempotence + "no match = always rejected").
- `test/hooks-fleet-gate.test.js` — FLEET GATE: no hook of the user's hook fleet reads stdin without a deadline. Covers the ones `deadline-gate.test.js` cannot see (it only reads this repo). Skipped on a pristine clone.
- `test/deadline-vendor.test.js` — DRIFT-TEST: the vendored copy ≠ the original = RED. That is the ONLY thing that makes the copy acceptable. Skipped on a pristine clone.
- `test/vendor-deadline.test.js` — PROOF on a tmpdir COPY before touching production: harmless dry-run, idempotence, 0 "manual", `node --check` (syntax), the fleet suites IDENTICAL before/after (anti-regression), and every patched hook really dies. ⚠️ All 3 angles are necessary: "the process dies" was GREEN on a broken file (a crash dies too).
- `test/file-differential.test.js` — DIFFERENTIAL: replays `src/sources/file.js` AND the historical engine over 2,021 cases derived from the real rules, requires identical AND ORDERED docs. ⚠️ ~75 min (spawns) → `npm run test:differential`, outside `npm test`. Skipped when the oracle is absent (pristine clone).
- `test/lock.test.js` — 9 tests dedicated to the cross-process lock, including the "fresh checkout" bug regression (15/07/2026: lock.js assumed `state/` already existed, broke in CI, invisible locally).
- `test/legacy-mcp-inject.test.js` — integration tests (process spawn, including 1 real concurrency test). ⚠️ Test config = tmpdir via `CTXROUTE_CONFIG_PATH`, NEVER the repo's real file.
- `stryker.conf.json` — mutation config (mutates ALL the pure modules; break 99, vitest perTest runner — node:test/commandRunner BANNED 16/07/2026).
- `vitest.config.mjs` — default vitest config (`npm test`): all suites EXCEPT the heavy ones.
- `vitest.stryker.config.mjs` — vitest config DEDICATED to Stryker: ONLY the deterministic suites of the mutated modules (never property/spawn).
- `vitest.heavy.config.mjs` — vitest config of the heavy suites (load, fleet, vendor, differential) — the only way to run them (the default config's exclude beats CLI args).
- `test/mutation-workflow-gate.test.js` — GATE: mutate ⟷ CI paths ⟷ Stryker suites mirror + commandRunner/node:test anti-return.
- `src/lint.js` — PURE CORE of the fleet audit (dead/ghost docs, Stryker-mutated). Shell = `tools/lint-corpus.js`.
- `tools/lint-corpus.js` — I/O shell of the fleet lint (`npm run lint`), liveness probe exit 2.
- `test/lint.test.js` / `test/lint-corpus.test.js` — the lint's suites (deterministic core · negative-check on a fake tmpdir fleet).
- `.github/workflows/mutation.yml` — separate mutation workflow with strict paths (gated mirror).
- `.github/workflows/spec-tlc.yml` — the TLA+ gate in CI, strict paths (`specs/tla/**` + the 4 modelled modules): the spec must be replayed when the transport moves, and only then — a TLC job under a wide path would burn the Actions quota. Java is installed there, so the local skip-by-name never hides a real red.
- `docs/framework/transport-spec.md` — published mirror of the transport spec doc (what a fork receives). Mirrors are copies: never edit here, edit the fleet doc and re-copy, `mirror-sync-gate` compares them byte for byte.
- `.dependency-cruiser.json` — anti-coupling rules (lib-pure must stay pure, no cycles, stdin-json standalone).
- `.jscpd.json` — duplication detection (1% threshold).
- `jsconfig.json` — `tsc --checkJs` config (㉑, 16/08/2026): JSDoc + noEmit, node types, noImplicitAny off — the checker, never the syntax; the executed file stays the written file.

**Doc & meta**:
- `HOOK-INTERNALS.md` — detailed internal doc of the frozen legacy oracle (mechanism, invariants).
- `README.md` — public install/usage doc.
- `CHANGELOG.md` — versioned history.
- `LICENSE` — MIT.
- `CONTRIBUTING.md` — contributor ground rules (English only, mechanical proofs, layer discipline).
- `SECURITY.md` — security policy (guardrail ≠ security boundary, private advisory channel).
- `.gitattributes` — LF normalization.
- `.gitignore` — excludes state/, personal docs, node_modules, Stryker/jscpd artifacts.
- `docs/framework/substrate.md` — injectable doc of the SUBSTRATE gate (fleet mirror): the 2nd symmetry, what each source SEES.
- `docs/framework/harness-conformance.md` — injectable doc of the conformance module (fleet mirror).
- `docs/framework/language-atoms.md` — injectable doc of the atom table (fleet mirror).
- `docs/framework/language-completeness.md` — injectable doc of the language's EXPRESSIVENESS (fleet mirror): computed, not proof-read.
- `docs/framework/live-production.md` — enforce GUARD against editing the engine in production (fleet mirror, 15/08/2026): deny ONCE per session on any write action targeting a live core file (tool Write/Edit/NotebookEdit/apply_patch + grouped scope). It bit its own author the day it was laid.
- `docs/framework/pre-commit.md` — injectable doc of the pre-commit hook (fleet mirror). Why it is allowed to BLOCK while the doctrine forbids it elsewhere, why never a pipe around vitest, why fail-open without node_modules.
- `.githooks/pre-commit` — the repo's ONLY BLOCKING gate (10/08/2026): runs the anti-leak gate BEFORE the commit enters history (~0.9 s). ⚠️ Born from the finding that the gate was RIGHT but had never RUN — nothing triggered it between writing and pushing. The only possible place for the "client names" half (CI necessarily runs in generic mode). Fail-open when `node_modules` is absent (never break a clone's 1st commit). Installed by the `prepare` script (`core.hooksPath`).
- `package.json` — scripts (`test`, `test:all`, `t`, `test:mutation`, `check:coupling`, `check:types`, `check:all`).
- `src/leak-list.js` — SINGLE SOURCE of the anti-leak list (declared + derived − exceptions), I/O shell never mutated; consumed by leak-gate AND the pre-commit launchers of the fleet's public repositories.
- `test/leak-list.test.js` — the anti-leak list's suite (spawn, fake file via CTXROUTE_FUITE_LISTE): the case that matters = an exception removes ONLY what is listed (the too-wide direction would silently remove protections WITHOUT A RED).
- `.github/workflows/test.yml` — CI (3 jobs: ubuntu/windows/macos matrix test, coupling, mutation).
- `specs/tla/Transport.tla` — **TLA+ MODEL OF THE TRANSPORT FRONTIERS (20/08/2026, work item 58 ②)**: leader election among N PARALLEL frame processes, the `remainder-` queue producer/consumer, the cross-process lock, the atomic state write. The decision core is DETERMINISTIC hence deliberately OUT of scope. The order of the three writes (queue → state → plan) is what carries the proof: publishing the plan LAST is what makes a dead leader harmless to the survivors. Carries its own declared abstraction boundary (`lostToCrash`: a process dying between its writes and the harness — no injection is ever acknowledged, so no ordering of ours can recover it; the seal and the canary answer that, not this spec).
- `specs/tla/runs.json` — **SINGLE SOURCE of the TLC run matrix**: 11 runs, each with its constants, the properties it checks and the verdict the gate REQUIRES. The `.cfg` files are GENERATED from it (gitignored) — two truths for one model would drift. Carries 4 sabotages, 3 anti-vacuity runs, the ROTATION run (a `dumb` corpus above capacity rotates for ever, and that RED is the proof the behaviour is real), and the DECLARED DEBT `TransportKnownDefect` with its candidate fix.
- `specs/tla/run-tlc.mjs` — **THE SPEC GATE** (`npm run spec:tlc`): generates the cfgs, runs TLC, demands the EXACT declared verdict. TWO-WAY — an expected RED that turns green fails as loudly as a green that turns red (hollow spec, or a defect that was fixed and whose check must move into the baseline). `tla2tools.jar` v1.7.4 SHA-256 pinned, downloaded on demand, NEVER committed.
- `specs/tla/README.md` — what is modelled, what is NOT and why, the run matrix, and the DEFECT the spec found on 20/08/2026 (the lock-less fallback delivers a `once` document without recording it ⇒ the next leader delivers it again).
- `test/transport-spec-gate.test.js` — SHIELD of the spec (fast lane, no Java): ① DRIFT SHIELD on the order of the three writes in `pretool-core.js` and on the lock-less branch (reads, writes nothing), with an IN-MEMORY negative-check; ② coherence of the run matrix DERIVED from the module (every checked name is defined, every CONSTANT has a default, every sabotage knob is really flipped, the anti-vacuity and rotation runs are still there). A spec drifting away from its code certifies a system that no longer exists — in a language nobody re-reads.
- `test/transport-conformance.test.js` — **THE ANCHOR TO THE REAL CODE**: the model's predictions replayed on the real engine (positive control, `NoWriteWithoutLock`, the found duplicate window, rotation-without-starvation). ⚠️ It ASSERTS a defect on purpose — witness of the debt of `TransportKnownDefect`, solidary with it: the day the engine stops duplicating, BOTH flip and BOTH are updated in the same move. 🛑 A state-by-state replay is IMPOSSIBLE here (frame processes are short-lived, only the three writes survive them) — fabricating the missing states would prove `x === x`, and that is said rather than dressed up.
- `test/transport-spec-tlc.test.js` — EXECUTES the real gate (`node specs/tla/run-tlc.mjs`) and demands the LINE of every declared verdict (anti-mute probe: exit 0 alone is also what a gate that checked nothing returns). Heavy lane (~11 JVM starts). Java absent ⇒ skip BY NAME, and a second cell that NEVER skips proves the artifacts exist regardless.
