---
rules: [{"pattern":"temporal-budget.json","scope":["ctxroute"]},{"pattern":"temporal-call.yml","scope":["ctxroute"]},{"pattern":"temporal-budget-gate.test.js","scope":["ctxroute"]},{"pattern":"temporal-budget-pure.js","scope":["ctxroute"]},{"pattern":"temporal-budget-pure.test.js","scope":["ctxroute"]}]
mode: dumb
---
# temporal-budget.json — TIME DECLARES ITSELF (2026-08-20)
🛑 **BEFORE ANY DELAY, ASK WHO KNOWS.** LOCAL, the kernel KNOWS — process exit, closed socket, `EADDRINUSE` are FACTS the OS delivers. Waiting a fixed number of milliseconds instead of asking the authority that knows is not caution: it is a BUG that fires on a loaded machine. Only two motives are admissible, and the gate refuses every other word: **`distant`** (no authority is reachable at all) and **`undecidable`** (alive-versus-frozen, i.e. the halting problem).
⚠️ **SEPARATION OF POWERS, DO NOT BLUR IT**: `rules/temporal-call.yml` DETECTS with **no exemption whatsoever**; `temporal-budget.json` + `test/temporal-budget-gate.test.js` carry the POLICY and the RATCHET. An `ignores:`/`files:` added to the rule would hide occurrences from the budget in silence — sealed by a dedicated check.
⚠️ **AST, NEVER REGEX**, and it is load-bearing here: a `setTimeout` quoted in a comment or a string is a false positive. 📐 **MEASURED 2026-08-20: 9 temporal calls over 5 files, out of 151 tracked JavaScript files** — 1 `src/deadline.js`, 1 `src/lock.js`, 1 `test/deadline.test.js`, 2 `test/vendor-deadline.test.js`, 4 `test/http-daemon-lifecycle.test.js`, all `undecidable`, each with its written justification.
⚠️ **THE RATCHET IS AN EQUALITY, NOT A CEILING.** Measured above the declaration = a new wait, prove its motive or remove it; measured below = a STALE declaration, lower it — a declaration nobody uses widens the budget for free, exactly like a stale layer justification.
⚠️ **THE PERIMETER IS DERIVED FROM `git ls-files`, AND THE FILES ARE NAMED TO `ast-grep` EXPLICITLY.** That is not a style choice: `ast-grep` HONOURS `.gitignore`, so a folder gitignored tomorrow would make a tree walk go blind **without saying a word**. Naming the tracked files removes that failure mode instead of documenting it.
⚠️ **ANTI-INERT, THE GUARANTEE THAT HOLDS THE REST**: every `pattern:` atom of the rule is DERIVED from the `.yml` and must be paired with a witness line whose detection is REQUIRED (written to the OS tmpdir, never to gitignored `state/`). Atom without a witness = RED, so an atom added tomorrow is covered the day it is written. 5 of the 7 atoms have zero occurrence here and are declared PREVENTIVE rather than passed off as encountered forms.
⚠️ **OUT OF SCOPE, WITH ITS MEASUREMENT (40 occurrences)**: an option key named `timeout`/`testTimeout`/`hookTimeout` is a **BOUND, not a WAIT** — it makes nothing slower, it only refuses to wait for ever. Enrolling 40 legitimate bounds would make the gate noisy, and a noisy gate gets disarmed.
⚠️ **OUT OF SCOPE TOO, DECIDED 2026-08-21 (1 occurrence, `test/client-core.test.js`)**: `setImmediate` is a **TICK, not a WAIT** — it yields to the next iteration of the event loop, makes nothing slower and guesses NO number of milliseconds. 🛑 It is NOT enrolled as an atom, and that is the point: the budget admits exactly `distant` and `undecidable`, a tick is NEITHER, so declaring it would force an INVENTED justification — and an invented justification makes a case look settled. The clause lives in the gate with an EQUALITY on its count: a new `setImmediate` reddens and must be RE-DECIDED (still a tick ⇒ raise the number with its reason · a disguised wait ⇒ delete the call), never widened in silence.
⚠️ **DECLARED BLIND CELL**: a temporal call inside a template literal (the source of a child script — real case, `test/session-store.test.js` line 95) is invisible to the parser, which is the correct verdict for that file (the call belongs to another program) and also a way out nobody may take deliberately. Parsing every string as a program would turn every code sample in every comment into a defect.
⚠️ **THE VERDICT LIVES IN `src/temporal-budget-pure.js`, PURE AND MUTATED — never in the test file.** Stryker does not mutate test code, so a rule written in a suite is UNVERIFIABLE: an inverted comparison stays green for ever, and a false gate REASSURES. Precedent and remedy: `deps-criticality-pure.js`. 🛑 Zero I/O here, and the suite imports the module DIRECTLY (a re-export makes `perTest` coverage lose the mapping = phantom survivors), with THUNK fixtures (a `const` at module load belongs to no test = static mutant). 📐 **MEASURED 2026-08-20: 100.00 % on the file — 57 mutants, 57 killed, 0 survivors, 0 timeouts, 0 without coverage, 3.48 tests per mutant.** ⚠️ The two count comparisons are written as TWO independent `if`s on purpose: nested under an `if (count !== n)`, `>` → `>=` becomes an EQUIVALENT mutant, hence an eternal survivor.
⚠️ **SEEN RED, by IN-MEMORY sabotage only** (undeclared file · ratchet crossed · stale declaration · motive `local` · missing justification · atom without a witness). Never sabotage a real file here: a first version of another gate did and brought down 38 tests of parallel suites.
