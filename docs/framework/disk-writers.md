---
rules: [{"pattern":"disk-writers.json","scope":["ctxroute"]},{"pattern":"fs-call.yml","scope":["ctxroute"]},{"pattern":"disk-writers-pure.js","scope":["ctxroute"]},{"pattern":"disk-writers-gate.test.js","scope":["ctxroute"]},{"pattern":"disk-writers-pure.test.js","scope":["ctxroute"]}]
mode: smart
threshold: 30
---
# disk-writers.json — SPACE DECLARES ITSELF (2026-08-21)
🛑 **ANYTHING THAT WRITES DECLARES ITS CEILING AND ITS EVICTION IN THE SAME GESTURE AS ITS CREATION.** Disk is FINITE and this machine runs 24/7 for YEARS with no operator: without a ceiling the component DOES NOT EXIST — an ARCHITECTURE bug, never an operations one. The question is never "is it big?" but "at 10 years, how much is this worth?"; monotonic growth is a DATED outage and "we will purge later" puts a human back inside the loop.
⚠️ **TRAP ①: A THRESHOLD PROBE IS NOT AN EVICTION** — it CONSTATES, and by then it is too late. Alert on the SLOPE, evict by age or by tier. **TRAP ②: AN EVICTION IS PROVEN BY WHAT IT DELETES** — a real fleet script targeted `*.tar.gz` while the producer wrote `*.sql.gz`: 0 bytes removed since forever, disk at 87 %. A cleaner that matches nothing is indistinguishable from a cleaner that works.
🔴 **A DETECTOR KEYED ON A RECEIVER NAME IS BLIND TO ALIASES, AND IT GOES BLIND IN SILENCE — MEASURED 2026-08-21**: `require("fs").writeFileSync(…)` and `const fsSab = require('fs'); fsSab.writeFileSync(…)` both left this gate GREEN. An unmatched form is NOT "an undeclared writer that goes red": it is a writer NOBODY SEES, i.e. fail-OPEN. ⇒ **Key on the METHOD, never on the receiver** (`writeFileSync` is distinctive on its own), and GENERATE those atoms from the manifest's `write` entries so the detector cannot drift from the policy. Never write a blind cell as if it failed closed — that tells the next reader the case is handled.
⚠️ **TWO FAMILIES, NEITHER REPLACES THE OTHER**: `rules/fs-call.yml` is RECEIVER-anchored and is the only half that matches an UNKNOWN method name, which is what forces every `fs` method met to be CLASSIFIED (the write set stays DERIVED, never a hand list that only knows the past); the METHOD-anchored half is generated at gate time and kills the alias, double-quote and destructured-import classes. `openSync` is FAIL-CLOSED as `write` (its flags are a runtime value the AST cannot decide). **AST, never regex** — these files carry "NEVER a direct `writeFileSync`" in comments, and a regex would accuse the documentation that protects the invariant.
⚠️ **SEPARATION OF POWERS**: the rules DETECT with **no exemption whatsoever**; `disk-writers.json` + `test/disk-writers-gate.test.js` carry the POLICY and the RATCHET, in BOTH directions — an undeclared writer is held at ZERO, a declaration whose writes vanished is a DORMANT PERMIT that gets re-inherited, budget included.
📐 **MEASURED 2026-08-21: 11 writers over 57 runtime files, 18 write primitives derived, 15 distinct `fs` methods met.** Widening the net to any receiver found NO new writer — a result, not a formality. Perimeter = `src/` only; `test/` writes into the OS tmpdir and removes it, `tools/` is operator-invoked (`doctor.js` writes only inside `mkdtempSync` sandboxes it deletes). Paths come from `src/paths.js` or `src/kernel-endpoint.js`, never a literal.
✅ **WI-STATE-EVICTION CLOSED 2026-08-21 — and `policy: "none"` stays ADMISSIBLE, silence is not.** `src/state-eviction.js` bounds `state/`: AGE from `deadline.DEFAULT_MS` × 10 for `plan-` and `.tmp`, COUNT 4096 durable + 2048 ephemeral taken from `memory-store-pure.js`, fail-closed on an unknown name, swept per turn from `turn-count.js`.
✅ **WI-VENDOR-PATH and WI-SHADOW-LOG CLOSED 2026-08-21, and by OPPOSITE moves — which is the lesson.** The first got the missing accessor (`paths.fleetHooksDir()`, consumed by the vendoring script, the fleet lint and the reach tool: one directory, ONE definition). The second got NO retention at all: **the first question a writer with no ceiling raises is "should this exist"**, and that one was a switch-over rehearsal, unwired since 17/07/2026, whose only reader answered a question settled the same day. A retention there would have been a cleaner with nothing to clean — indistinguishable from one that works.
⚠️ **WHAT REMAINS BLIND, AND ON EVERY ONE THE GATE GOES SILENT (fail-OPEN)**: a method RENAMED at the destructure (`{ writeFileSync: put }`), a method name COMPUTED at runtime (`fs[verb]`), a write through a module OTHER than `fs`, and a file still UNTRACKED by git. A child process writing is a boundary, not a hole.
⚠️ **THE RAM SIDE OF THIS PROBLEM IS ALREADY CLOSED** in `src/memory-store-pure.js` — one LRU and one ceiling PER KEY CLASS, because a shared ceiling let the ephemeral flood evict the durable. Read it before proposing a disk policy: the same two lifetimes are what `state/` holds.
⚠️ **THE VERDICT LIVES IN `src/disk-writers-pure.js`, PURE AND MUTATED — never in the test file.** Stryker does not mutate test code, so a rule written in a suite is UNVERIFIABLE: an inverted comparison stays green for ever and a false gate REASSURES. Zero I/O, imported DIRECTLY by its suite (a re-export loses the `perTest` mapping = phantom survivors), THUNK fixtures only.
⚠️ **SEEN RED by sabotage in memory or in the OS TMPDIR only** (undeclared writer · stale declaration · declaration missing its policy · unclassified primitive · empty `pathSources` · the three alias forms that once passed), plus four anti-vacuity floors, a witness per rule atom and a witness FABRICATED per derived write primitive in both call forms. Never sabotage a real file: a first version of another gate here brought down 38 tests of parallel suites.
