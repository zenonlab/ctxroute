---
rules: [{"pattern":"FILE-MAP.md","scope":["ctxroute"]},{"pattern":"REFACTOR-PLAN.md","scope":["ctxroute"]},{"pattern":"REFACTOR-ARCHIVE.md","scope":["ctxroute"]},{"pattern":"steering-single-copy.test.js","scope":["ctxroute"]}]
mode: smart
threshold: 30
---
# FILE-MAP.md / REFACTOR-PLAN.md — the repo's 2 STEERING documents
⚠️ **`FILE-MAP.md` = EXHAUSTIVENESS net** : 1 line per file, NEVER a judgment of importance — a file outside the list is a hole BY DEFINITION. Add/delete/rename ⇒ UPDATE **in the same action**. Sealed by part ② of `coverage-gate.test.js` (it reads this file AND the skill).
⚠️ **NEVER merge it back into the skill** : pulled out on 2026-07-31 because it weighed 48 % of it and pushed the whole skill past the harness frame — it was therefore EVICTED, i.e. ABSENT from the context.
🛑 **ONE PHYSICAL COPY FOR EVERY WORKTREE, RESOLVED BY `paths.js` — NEVER BY A TREE'S OWN `__dirname` (2026-08-22).** Both journals are GITIGNORED (personal values, public repo), so they follow NO branch, NO merge and NO checkout: each worktree held its OWN copy and they DIVERGED — two trees on the same commit, **75 lines apart on the plan, 17 on the archive**, and **TWO of that morning's three reds came from that single cause** (judges accusing rows already rewritten in the other tree, i.e. a repair about to be done TWICE). They now live in the repository's COMMON directory (`git rev-parse --git-common-dir`), which git shares between every worktree and which is intrinsically outside the tracked tree — **one state, one owner, everyone else a client**, the injection-store doctrine applied to the steering documents. 🛑 Making them tracked is REFUSED (personal data, public repo), and the resolution is **FAIL-CLOSED**: no common directory ⇒ a NAMED REFUSAL, never a fallback to a per-tree path — that fallback IS the divergence, re-created with nothing going red. Sealed by `steering-single-copy.test.js` (a REAL repo + a REAL linked worktree, seen red on the exact defect).
⚠️ **`REFACTOR-PLAN.md` = backlog + decisions.** Write the TARGET there, never the progress status alone. A "closed" work item that fills only half of its target MUST say so — otherwise the missing half becomes invisible (happened 07-31: the 07-20 section declared itself closed while full injection, its real target, was still open).
⚠️ **Also record DISCARDED leads with their MEASUREMENT** (not just the verdict): multi-hooks was rejected, then reopened, then re-rejected on figures in a single day. Without the written measurement, the loop starts over.
⚠️ **A REVERSED judgment gets rewritten, it does not stack** : the backlog states what is TRUE today, with the date and the proof. A reader must never have to guess which line is stale.
⚠️ **A LYING HEADER IS NOW RED (gate, 2026-08-06)** : `backlog-coherence-gate.test.js` refuses a section announced OPEN whose sub-sections are ALL closed. Born of **3 occurrences in 2 days** ("3 OPEN out of 4" while ① was closed · "TRUNCATED INJECTION" red 2 weeks after its resolution · "TWO DEFECTS" of which ① was ✅). The prose instruction above failed three times ⇒ it becomes mechanical. 🛑 It proves INTERNAL COHERENCE, NEVER that a work item still exists in the code — undecidable.
⚠️ **PART ④ HAS A SYMMETRIC BLIND SPOT, MEASURED ON 2026-08-14** : it requires every OPEN work item to be IN the table, **never that everything in the table be OPEN**. Observed result: **25 lines of which 14 CLOSED** — a table that says the opposite of its title for more than half of it. The cost is the SAME as the one that motivated part ④ (an agent resuming reads the head and misses the real work; already paid one session with ㊴/㊵), simply by drowning instead of absence. ⇒ **closed items MOVE to the archive, they are never deleted — and since 2026-08-15 the archive is a FILE: `REFACTOR-ARCHIVE.md`** (maintainer decision "light backlog": the active plan reads without grep) (measurements and discarded leads are the value of the backlog: a rejected lead without its measurement gets redone). ✅ **CLOSED BY PART ⑤ ON 2026-08-21, AND THE 08-14 VERDICT ABOVE IS REVERSED BY MEASUREMENT.** That verdict said no gate could cover this direction because deciding "closed" would mean reading an emoji. The measurement says otherwise: the active table carries a STATUS COLUMN with a closed author-written vocabulary, so a stale `OUVERT` row is decidable without guessing. Part ⑥ is DERIVED from the file (no hand list of rows), anchored on the FIRST open table — the historical tables below legitimately hold closed rows, and a gate that accuses the archive gets unplugged — and it reddens on a closed row left in place, on an UNCLASSIFIABLE status (renaming the states must not silence it) and on an ambiguous one. Three anti-vacuity floors: table unreachable, separator not found, fewer than 3 rows parsed — all RED, because a mis-parsed table looks EXACTLY like a coherent one. 🛑 It still does not prove a work item lives in the CODE: that stays undecidable, same honesty as parts ①-④.
⚠️ **PART ④ (2026-08-12): every numbered OPEN work item MUST appear in the "WHAT REMAINS OPEN" table.** 🔴 Born of an omission that cost a WHOLE SESSION: ㊴ and ㊵ (the 2 priorities, written the day before) were not there ⇒ an agent read the head, concluded "only comfort work remains", and worked on something else. 🛑 **The table declares itself a "COMPLETE list": it must therefore be one, mechanically.** Scope = NUMBERED sections (measured: 2 numbered / 3 narrative ⇒ 0 false positives); it does not prove that a work item still lives in the code (undecidable), only that it is REACHABLE from the head.
⚠️ **PART ③ (㉜, 2026-08-08): if you WRITE the head, it must cite the commits it does not yet name.** ② compared two figures both coming from the head itself ⇒ GREEN on a head that counts right and forgets 9 commits (measured). 🛑 The "commits OF THE DAY" rule is DISCARDED on measurement: a NIGHT session crosses midnight, "the day" is not decidable. Silent if the head is untouched (otherwise it would require rewriting it at every commit) and on shallow clones (CI has no history).
🔴 **AND IT WAS A GREEN JUDGE THAT SAW NOTHING FOR SIX DAYS — this repository's worst defect class, inside the file that exists to forbid it (2026-08-22).** It armed on `git status --porcelain -- REFACTOR-PLAN.md`, and the journal has been GITIGNORED since 2026-08-16: **git answers ZERO lines for an ignored path, always**, so the part returned `null` — *out of scope* — on EVERY run, through complete green suites. MEASURED: `status --porcelain` 0 lines · `check-ignore -v` naming `.gitignore:36` · `ls-files` 0 · and `git log -1 -- REFACTOR-PLAN.md` **EMPTY**, so the ANCHOR was dead too. Two dead signals, one silence — and since the journal left the work tree, no command on a tracked path can ever see it again.
✅ **RE-ARMED ON FACTS OF THE FILE AND OF THE HISTORY, WITH NO STORED STATE AT ALL.** ① THE GESTURE = the journal is **FRESHER THAN HEAD** (`mtime` > HEAD's commit date) — the very predicate `git status` used to express, *written since the last commit*, read off the FILE instead of off an index that refuses to look at it. 🛑 Nothing memorises a previous digest, and that absence is the design: a file made to carry a conversation between runs is a class this project has already paid for. ② THE ANCHOR = the **NEWEST commit the head CITES** — *the previous edition of the head* died with the file's tracking, and what the head names is the only fact left that dates it. ⇒ everything newer than that anchor must be cited, and the part re-arms and disarms **on its own**: commit, and HEAD's date passes the journal's mtime, so it goes quiet until the journal is written again.
🛑 **HONEST SCOPE, NARROWER THAN THE 08-08 VERSION — do not sell it wider.** It proves the head is STALE (it names nothing newer than commit X while N commits exist after X); it NO LONGER catches an omission in the MIDDLE of a cited range, which the tracked-file anchor could. That capability died with the tracking, and claiming it would be the lying doc this repo refuses.
⚠️ **ANTI-VACUITY IS THE WHOLE REPAIR**: every `null` is now spent on a question that genuinely does not arise, never on *I could not measure*. A head citing **NONE** of the reachable commits is **RED**, never silent — a dead anchor looked EXACTLY like that case, and answering `null` there is how the part slept. ✅ Seen red on both guards sabotaged and restored, and **RED on its very first real run: 13 commits newer than everything the journal named** (`e448421` back to `d82c4a4`), cited nowhere — the omission it had been unable to see.
⚠️ **AND THE JOURNAL IS RESOLVED IN THE COMMON DIRECTORY, never at the tree's root**: since the move of 2026-08-22 a linked worktree finds nothing at `<tree>/REFACTOR-PLAN.md`, so parts ①-⑤ ALL skipped there in silence. A root fallback would re-create the per-tree divergence the move removed; no git ⇒ clean skip.
⚠️ **PART ② (㉚, 2026-08-07): "The N commits of the day" must cite N DISTINCT hashes.** The head lied 2× in 2 h ("The 10" over a list of 9 · branch announced alive while deleted). 🛑 **Checking that the hashes EXIST is discarded on MEASUREMENT**: `actions/checkout@v5` clones with `fetch-depth: 1` ⇒ no old commit in CI. Every hash cited in this section must therefore be one of the listed commits (a proof "CI green on X" reuses X).
⚠️ **A FILE IS INVISIBLE TO THE NET UNTIL IT IS TRACKED — measured 2026-08-20, and it is a PROPERTY, not a hole.** Part ② scans TRACKED files, so a brand-new file passes the suite GREEN and only reddens on the run AFTER its commit; `english-only-gate.test.js` shipped through that gap on the very day the net was being praised. 🛑 Do NOT "fix" it by scanning the disk: that would drag in `node_modules` and every scratch file, and a noisy net gets disarmed. The net DOES catch it, one run late — and the pre-commit is deliberately not the place for a full suite.
