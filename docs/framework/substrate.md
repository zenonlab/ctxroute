---
match: substrate-gate.test.js
scope: ["ctxroute"]
mode: dumb
rank: 568
---
# substrate-gate.test.js — the 2nd symmetry: what each source SEES

🔴 **THERE ARE TWO SYMMETRIES, AND ONLY ONE WAS GUARDED.** ① the VOCABULARY (which operators a corpus ACCEPTS — gates ①/② of `frontmatter.test.js`) · ② the SUBSTRATE (which PROJECTIONS of the event a source LOOKS AT). **2 of the 3 defects of 2026-08-11/12 were SUBSTRATE ones**: ㊵ (`scope` read only the 1st level ⇒ blind to the 16 MCPs) and ㊴ part ② (`serverMatches` ignored `scope`). Two corpora can accept the SAME words and behave differently — a gate that compares KEYS cannot see it.
🛑 **A SUBSTRATE HOLE BREAKS NOTHING: IT MAKES THINGS MUTE.** A silence is indistinguishable from normal operation ⇒ neither tests, nor mutation, nor the doctor see it (they prove what is DELIVERED). This gate exists to make ABSENCE observable.
⚠️ **PROBED BY BEHAVIOR, NEVER BY TEXT**: a unique witness placed in each projection, then we ask the REAL source whether it reacts. A grep would say "file.js mentions cwd" — yet it READS `toolInput.cwd` while nothing puts anything there: capability present and INERT. Only behavior decides.
🛑 **AN INCOMPLETE PROBE MANUFACTURES A FALSE BLINDNESS** — happened on the 1st run (08-12): without the `tool: [<exact name>]` form, the gate declared `skill` blind to the tool name and was about to ENGRAVE that with its justification. Any source that owns an axis must be probed ON THAT AXIS.
⚠️ **4 parts, all necessary**: ① unjustified blindness = RED · ② STALE justification = RED (it bit the same day on `tool`/tool name) · ③ every source of `ADAPTERS` is probed (`session` excluded: it LOOKS AT nothing) · ④ the witness discriminates — a probe that ALWAYS matches would certify instead of protect.
⚠️ **`file/cwd` and `tool/cwd` = asymmetries BY INHERITED CONSTRAINT, not by principle** (parity with the FROZEN oracle): a skill reacts to the current folder, a doc does not. Declared here — that is the whole point of the gate. What would lift them: the end of the `protect-files` parity.
