---
match: declfor-gate.test.js
mode: smart
threshold: 30
---
# declfor-gate.test.js — a FILTERED decision key is a DEAD key (2026-08-06)
🔴 **REAL DEFECT, THE COSTLIEST POSSIBLE**: `enforce` (the word that REFUSES an action, shipped 08-05) was not copied over by `sources/mcp.js#declFor` ⇒ accepted by `validateMcp`, documented everywhere, **INERT on the MCP channel** — that is, precisely where the FOUNDING incident lives (the Stripe payment click). Found 24 h later by arming it FOR REAL: `create_refund` returned `allow`. **A safety catch that does not catch is WORSE than nothing: we trust it.**
🛑 **THE CAUSE WAS A COMMENT**: "a decl carries ONLY cadence" — right in substance (a source arbitrates nothing), read as "so don't copy `enforce`". **Transporting ≠ deciding**: a decl TRANSPORTS what the author declared, `gate.js` alone decides.
⚠️ **WHY THE VOCABULARY SYMMETRY GATE DID NOT SEE IT**: it checks that a key is ALLOWED in the 4 corpora (validation), not that it is TRANSPORTED to `gate.decide` (propagation). Two distinct invariants — admitting and honoring are not the same thing.
⚠️ **KEYS DERIVED FROM `gate.js`** (the `xForDoc` resolvers), never copied: a list here would diverge, which is the very bug we hunt. `bloque` is excluded — it is the VERDICT, not a frontmatter key.
⚠️ **PART ③ = ANTI-BLIND-SPOT, do not remove it**: without it, a key added without a sample would give `undefined === undefined` ⇒ GREEN while it is filtered. The gate would certify itself.
⚠️ EXPLICIT `false` must survive (part ④): it is the only way to UNSUBSCRIBE from a `defaults.{source}.enforce`. An "empty value" filter would make unsubscription impossible.
🛑 **A SOURCE POSES, IT NEVER RESOLVES (㊳, 2026-08-09)** — 2nd defect in the SAME file. `sources/mcp.js#declFor` called `lib.modeFor`/`lib.thresholdFor`, so it ALWAYS posed a value: the cascade stopping at the first found, tier `defaults.mcp` was **INERT**. ⚠️ The trap is not FILTERING a key (the 08-06 defect) but FILLING one: both kill a tier, in opposite directions. **Absent or invalid key ⇒ OMITTED**, that is what lets the next tier exist. 🛑 `declFor(fm)` takes ONLY the frontmatter — neither `config` nor `server`: it is the SIGNATURE that forbids going back, not a comment. ⚠️ The resolution was LEGITIMATE originally (it read `servers.{name}.mode`), but the schema has REJECTED that key since: a vestige that only did harm. When the frozen oracle and the schema contradict each other, **the schema states the contract**.
🛑 **PART ⑤ — THE OTHER DIRECTION, ADDED 2026-08-09: a FILLED key kills the next tier.** Parts ①-④ check that a DECLARED key reaches the end; they were BLIND to the mirror — a source that INVENTS a value absent from the frontmatter short-circuits `defaults.{source}` (the cascade stops at the 1st found). **Invariant: empty input ⇒ EMPTY decl.** A source has no right to know a default; defaults live in `gate.js`, single point. ⚠️ **DERIVED from the `sources/` folder** (every future source exposing a `declFor` is covered the day it is written) — never a list, which would depend on vigilance. Its negative-check requires inspecting ≥ 2 real sources AND that the criterion detects a source that fills: without it, a folder rename would make the part GREEN by inspecting nothing.
🛑 **THE FILE SOURCE DOES NOT HAVE THIS RISK** (it passes the WHOLE frontmatter); `mcp` and `skill` copy key by key. As long as this asymmetry exists, this gate is the ONLY net. Reddening proven by real sabotage on 2026-08-06.
