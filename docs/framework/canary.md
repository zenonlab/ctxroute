---
rules: [{"pattern":"canary.js","scope":["ctxroute"]},{"pattern":"canary-check.js","scope":["ctxroute"]},{"pattern":"canary.test.js","scope":["ctxroute"]},{"pattern":"canary-check.test.js","scope":["ctxroute"]}]
mode: smart
---
# canary — the ONLY witness that watches the OTHER END of the pipe
⚠️ **EVERYTHING ELSE TESTS ITSELF.** The doctor spawns OUR hook with OUR payload and checks OUR output — blind to the only remaining risk: that the HARNESS changes its mind (renamed fields, `additionalContext` no longer consumed). Then the hooks fail open silently, the doctor stays GREEN, and nothing reaches the agent any more.
⚠️ **THE VERDICT = "we EMITTED N times, did it ARRIVE?"** — numerator `[source: …]` in the transcript (OUR mark), denominator = `emission-core.emissionCount` (OUR counter). ONE single trace is enough to prove the channel is alive; we NEVER compare received vs expected.
🛑 **THE NUMERATOR NO LONGER COUNTS BARE `[source:` — DEFECT ㉘ FIXED (2026-08-07).** That literal ALSO lives in the text that TALKS about it: the comments of `canary.js` and **64 fleet docs out of 386** that CITE a source file that way ⇒ an agent READING one of those docs (the exact action of whoever INVESTIGATES a dead injection) turned the canary GREEN. ✅ Only a label of EMITTED shape counts (`.md` suffix or `skill/` prefix); a mark without `]` is NEVER counted (the window cut would FABRICATE a label). ✅ **㉘ bis CLOSED 2026-08-08, in DATA and not in the engine.** The "4 docs" CITED nothing: they carried the `[source: …]` tag **hard-pasted**, a copy-paste of an injection (the engine already adds it ⇒ they arrived with their tag TWICE). Removed from all 4 ⇒ no source of false green left. 🛑 **The fix "accept only labels actually emitted via `emission-core`" is ABANDONED, not deferred**: it imposed a state write on the HOT path (12 processes × every tool call × every agent) for a defect that 4 text corrections remove. Extension contract: **a hole is fixed in DATA first, the engine as a LAST resort.** Anti-return = the lint rule `tag-source-en-dur` (ERROR, zero exemptions over 393 docs).
🛑 **NEVER COUNT ANYTHING IN THE TRANSCRIPT SCHEMA** (2026-08-07). Official Codex doc: *"the transcript format isn't a stable interface for hooks and may change over time"*. The denominator came from `"type":"tool_use"` (Claude dialect) and the port planned to guess the Codex equivalent — abandoned. Searching for `[source:` stays LEGITIMATE: it is a substring we write ourselves, not a schema field.
⚠️ **ONE SINGLE SHELL FOR BOTH HARNESSES** (no more `MARQUE_APPEL_*`). ✅ **VERIFIED IN BOTH OFFICIAL DOCS on 2026-08-07, not deduced**: `transcript_path` ("Path to conversation JSON") and `session_id` are COMMON input fields at Claude (`code.claude.com/docs/en/hooks`) as at Codex, and Claude explicitly allows silence ("Exit 0 means success […] stdout is written to the debug log but not shown"). A harness differing on one of the 3 requires a shell — never an `if`.
🛑 **THE TRANSCRIPT IS ASYNCHRONOUS — official fact, found by checking (2026-08-07).** Claude doc, verbatim: *"The transcript file is written asynchronously and may lag the in-memory conversation, so it may not yet include the current turn's most recent messages when a hook fires"*. ⇒ an injection that just landed may NOT be there yet. **Harmless here** (the lag covers a turn's last messages; we require 25 emissions and read 2 MB). 🛑 **THIS FACT FORBIDS lowering the threshold to 1-2 or looking only at the current turn**: we would read a lagging file and cry the death of a living channel. The threshold is not caution, it is what makes reading an asynchronous file decidable.
⚠️ **THE TRANSCRIPT FORMAT IS SPECIFIED NEITHER AT CLAUDE NOR AT CODEX** (checked on both sides 2026-08-07). The old `"type":"tool_use"` counting was therefore a bet on BOTH sides, not only on Codex — the removal was even more justified than announced.
⚠️ **NO EMISSION, NO ACCUSATION**: a very active transcript but zero emissions ⇒ `indecidable`, silence. Before 2026-08-07 the HARNESS's activity was enough to accuse — a false positive on a user who touches no documented file.
⚠️ **SCOPE = `lib.scopeId(session_id, agent_id)`, identical to the gate**: composing the key otherwise would read a non-existent counter ⇒ eternal `indecidable`, mute and green. Sealed by the "BOUNDARY CONTRACT" test.
⚠️ **THE ALARM NEVER GOES THROUGH THE PIPE UNDER TEST**: output = `state/canary.json`, read by a display OUTSIDE the framework. 🔴 That display pointed at `Desktop/mcp-doc-hooks` for 3 days after the rename to `ctxroute` (08-04→08-07): a verdict written for NOBODY. ✅ **NOW MECHANICAL (④, 2026-08-07)**: part ④ of `parc-sync-gate` — when a display exists, its **absolute paths**, the **export** it destructures and the **`verdict` key** are checked. 🛑 The pattern does NOT filter on `ctxroute`: the 1st version did and would have missed its founding case (the stale path cited the OLD name). Measured: 5 paths, 0 dead ⇒ 0 exemptions. Skip if no fleet OR no display — the framework still DEMANDS none.
⚠️ **MUTE when all is well** (`sourceTag()` returns `''`) — NEVER add an "✅ ok". **On error: write NOTHING** (previous verdict preserved); writing "alive" without a measurement would fabricate green.
⚠️ **READ BOUNDED TO 2 MB from the END** (fleet transcript measured at 104 MB, 524 ms for the whole vs 5 ms for the tail). **THRESHOLD = 25 emissions** = a SAMPLE size, not a delay.
🔴 **BLIND WINDOW AFTER COMPACTION — REGRESSION ACCEPTED, DECLARED (2026-08-07).** `PreCompact` purges `remainder-`, so the counter restarts from ZERO: the canary is `indecidable` until the threshold is reached again. **Before, the denominator came from the transcript, which SURVIVES compaction** — so we lost on that axis what we gained in independence from third-party formats. ⚠️ **No test had seen it** (1081 green, 100 % mutation, doctor 74 ok): found by investigating the question "is it really solid?". **Measurement**: 13 real compactions, 94→335 injections between two ⇒ the threshold is crossed early, the trade holds ON THIS FLEET. 🛑 The sealed invariant is NOT "it sees everything" but **"it never lies"**: after compaction ⇒ SILENCE, never an accusation (test "AFTER COMPACTION", negative-check proven by in-memory sabotage).
⚠️ **THE VERDICT CARRIES ITS OBSERVATION LOG (2026-08-09)** — `transcript` (the file EXACTLY read), `tailleTranscript`, `octetsLus`, `scope`. 🔴 Born of a REAL incident: verdict "dead" while EVERYTHING was injecting (240 labels and 95 injection events in its own window); replayed afterwards: "alive". **NOT REPRODUCED, cause NOT ESTABLISHED — and impossible to investigate**, because the verdict did not say what it had observed. **A dead-man switch that accuses without leaving a trace is UNAUDITABLE.** ⚠️ These fields are a LOG, never an INPUT: the engine does NOT re-read them (feeding them into a decision would create a 2nd truth). Free: they travel in the write that already existed. 🛑 `transcript` carries a user path ⇒ this file lives under `state/`, **GITIGNORED**: NEVER move this log to a git-tracked path (public repo). 🛑 Sealed by 2 cases, one ANTI-INERT (2 files of different sizes: the log must FOLLOW) — without it, hard-coded fields would pass the test.
⚠️ **UserPromptSubmit, never PreToolUse** (~330 ms per spawn); **EMPTY stdout and exit 0 ALWAYS** — an `exit≠0` there would block the prompt.
⚠️ **WIRED ON BOTH HARNESSES and watched by the doctor**: probe 9 (BOTH verdicts required) + Claude wiring + Codex wiring, negative-checks 3i/3j/5f/7f. A dead-man switch nobody watches is WORSE than nothing: it manufactures trust.
