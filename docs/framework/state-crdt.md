---
rules: [{"pattern":"state-crdt-pure.js","scope":["ctxroute"]},{"pattern":"state-crdt-pure.test.js","scope":["ctxroute"]},{"pattern":"state-crdt-pure.property.test.js","scope":["ctxroute"]}]
mode: dumb
---
# state-crdt-pure — WRITES THAT COMMUTE, so no lock has to be right (2026-08-23)

🔑 **TWO ACTORS DOING A READ-MODIFY-WRITE ON ONE STATE HAVE THREE OUTCOMES, NOT FOUR**: (a) a lock — correct while everyone agrees on its ADDRESS and every exemption stays true · (b) operations that COMMUTE — correct unconditionally · (c) one writer — correct while it lives. This repo is in (a) and paid both conditions on 2026-08-23 (**209 lost read-modify-writes out of 800** on a `remainder-` key; control, both locked: 0-1 of 800). (c) fails too: the daemon exits BY DESIGN at every edit. This module is (b).
🛑 **NOTHING USES IT, ON PURPOSE, AND THE LOCK STAYS.** Expand/contract: proven before wiring, switch-over is a separate gesture with its own GO. It is a BELT removal for the lock's two CONDITIONS, never for the lock.
🛑 **THREE LATTICE PIECES, NO FOURTH**: G-SET (union) · G-COUNTER (one slot per WRITER, merge = per-slot MAX, value = SUM) · MAX-REGISTER (per-key max). `merge` is commutative, associative, idempotent and every write is monotone — those four facts, together, are what make an interleaving converge with no lock, no order, no clock.
🛑 **EVERY RESET OR REMOVAL BECOMES AN ADDITION TO A SECOND GROWING SET, READ AS A DIFFERENCE** — that is the whole trick, used three times: the queue's consumption → `emitted`, remaining = `decided − emitted` · the drift counter's reset → `recalled`, drift = `actions − recalled` · the refusal toggle → `denied`, refused ⇔ `denied[d] === recalled[d]`. **Removal never commutes; addition always does.** The `driftUnit: "turn"` path of `gate.js` ALREADY had this shape — the `tool` path is the same quantity written the non-commutative way.
🛑 **THE WRITER IDENTITY IS MEASURED, NEVER INVENTED, AND IT IS THE LANE** (`daemon` | `spawn`, a CLOSED set, an argument that already exists). `process.pid` was REFUSED: pids are recycled, and a recycled G-Counter slot loses the earlier process's count in silence. **The closed set is also what BOUNDS THE SPACE**: a G-Counter's slots grow with the number of distinct writers and NO per-slot compaction preserves commutativity, so a per-process id would be a monotone growth with no eviction. ⚠️ RESIDUAL, STATED: two concurrent SPAWNED peers share a slot — that pair is what the lock still serialises.
🛑 **CEILINGS, DECLARED WITH THE STRUCTURES**: the counters are bounded by `WRITERS.length` = 2 BY CONSTRUCTION · `seen`/`recalled`/`denied` by the CORPUS, not by time · only `decided`/`emitted` grow with traffic · the outer bound stays `MAX_SCOPES`/`MAX_EPHEMERAL` and the LRU **BY SCOPE** of `memory-store-pure.js`. **Eviction by SCOPE preserves commutativity, eviction by ELEMENT does not** — dropping an `emitted` entry whose `decided` twin survives re-delivers the document.
🔴 **COMPACTION IS SAFE ONLY UNDER CAUSAL STABILITY, and the counter-example is a CELL (⑥c), not a hope.** Removing an id present in BOTH sets leaves the difference unchanged LOCALLY — but a peer holding only the `decided` half re-owes it at the next merge. ⇒ **the MERGE is lock-free, the COLLECTION is not**: compact at a point of quiescence (under the belt lock, or at PreCompact where the record dies anyway), never on the hot path. ⚠️ Compaction returns the NAMES it removed and its cells assert them (47 of 50 measured): a cleaner that matches nothing is indistinguishable from one that works.
✅ **THE DIFFERENTIAL FOUND A DEFECT IN `gate.js`, AND THE ENGINE WAS REPAIRED THE SAME DAY (2026-08-23) — the cell that pinned it WENT RED, exactly as designed.** The engine states in bold that *a block is NEVER followed by a block* and anchors it in the `denied` flag; the FOREIGN-ACTION branch rebuilt a smart doc's entry as `{ seen, sinceLastCall: n + 1 }` and **dropped `denied`**, so a refused gesture was refused a SECOND time. **28 of 1,024 decisions diverged (delivery identical on all 1,024); after the fix, ZERO** — and the tolerance clause was DELETED rather than softened, because a differential that tolerates a difference has stopped being one. Cell ①ter is now a REGRESSION GUARD: engine and model both answer `deny → allow → allow`. 🔑 **THE LESSON IS BIGGER THAN THE BUG, AND IT IS ABOUT MODELS**: `cadence-spec.js` — the independent model whose 11,346 exhaustive cases exist to CONTRADICT the engine — carried the SAME rebuilt literal, so it was GREEN on this defect for its whole life. **A model that copies the engine's SHAPE cannot contradict it.** The symptom was already written: `jscpd` had flagged those two loops as an 11-line clone, and it was read as duplication noise. A model is written from the INTENTION, never from the code it judges. ⚠️ `turn` was dropped by the same rebuild: harmless on today's config, but a mid-session `driftUnit` flip would have made the drift `NaN` and silenced the doc — closed for free by the spread.
⚠️ **THE DIFFERENTIAL COMPARES THE DECISION, NEVER THE STORED BYTES** — the two representations are deliberately different, so comparing bytes would compare the thing being changed. Oracle = the REAL engine (`gate.decide`, `turn-core.bump`, `emission-core.emit`), never a re-description of it.
⚠️ **THE LAWS ARE GENERATED (`*.property.test.js`) AND TWINNED DETERMINISTICALLY** — fast-check never enters the mutation runner, so a law proven only there leaves its mutant alive.
