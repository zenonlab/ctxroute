---
match: [derived-observables.js, derived-observables.test.js]
mode: dumb
---

# derived-observables.js — the facts we DERIVE, as a REGISTRY

🔑 **The language decides on FACTS.** Most are handed over by the harness (`harness-profile.js` = its dialect). A few are OURS: no harness sends "the directory this command designates" — we read it out of the command. **Universe = the harness's facts ∪ our derivations**, hence strictly larger than what any harness delivers, hence DECLARED and never discovered.
🛑 **ONE FIELD PER FACT IS A TOOL — the registry is what makes it a language.** Born 2026-08-20 evening: two hard-coded profile fields produced TWO near-identical engine branches in one evening, and a third fact would have added a third, in the engine AND the independent model AND every judge. ⇒ **adding a derived fact = ONE entry here, zero engine line, zero judge edited.**
⚠️ **`from` names the key family a derivation reads, and the trigger reads it WHOLE** — never narrowed by `keys`. An entry dropping the raw `command` must KEEP seeing where the command works: measured, the other reading costs **47.7 % of real work** over 28,703 actions.
⚠️ **`temoin` is MANDATORY and is a THUNK, not an object.** The reach table BUILDS its cells from what it returns, so a witness producing nothing would make those cells green while measuring nothing. A literal at module level is a STATIC mutant (workers keep modules cached) — measured here: 2 false survivors, 92.86 % on fully covered logic.
🔴 **THE 10th DEFECT OF THE SAME FAMILY LIVES HERE.** `commandPaths` glues the directory to EVERY following word, so a project name merely QUOTED after a `cd` becomes a plausible path OF that project and triggers its skill. Measured: **13,910 shell actions, 6,336 with a `cd` (46 %), 402,734 fabricated paths, up to 1,740 for one command** — one of which delivered a FOREIGN project's 90 KB skill into an unrelated session. `keys: {match:["-command"]}` shuts the raw text; the reconstruction reopens the window.
🛑 **REMOVING THE RECONSTRUCTION WAS PROPOSED AND REFUSED** (maintainer, 2026-08-20): it serves the awkward cases, and dropping a capability to close a case is a REGRESSION. We CUT IN TWO, never REMOVE. Price of dropping it, measured over 8,000 real actions: **20 injections out of 5,978 (0.33 %)**.
⚠️ **The directory is a PREFIX of every reconstructed path** ⇒ declaring it is decision-neutral by default, and dropping ONLY `commandCwd` changes nothing while the reconstruction lives. The pair an entry actually writes is `["-command", "-commandPaths"]`.
⚠️ **Known limit, MEASURED not assumed**: a directory containing a SPACE derives nothing (the capture stops at the first blank). Inherited from `protect-files` — widening it is a DIFFERENTIAL question, never a "fix".
⚠️ **Registry ORDER is part of the contract**: it fixes the candidate order, hence the `mordants` an `exclude` sees. Reordering is a behaviour change — replay the exhaustive differential.
