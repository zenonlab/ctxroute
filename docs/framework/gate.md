---
rules: [{"pattern":"gate.js","scope":["ctxroute"]},{"pattern":"gate.test.js","scope":["ctxroute"]},{"pattern":"gate.property.test.js","scope":["ctxroute"]},{"pattern":"doc-inject.js","scope":["ctxroute"]},{"pattern":"pretool-core.js","scope":["ctxroute"]},{"pattern":"codex-doc-inject.js","scope":["ctxroute"]},{"pattern":"codex-doc-inject.test.js","scope":["ctxroute"]},{"pattern":"source-adapters.js","scope":["ctxroute"]},{"pattern":"doc-inject.test.js","scope":["ctxroute"]},{"pattern":"corpus.js","scope":["ctxroute"]},{"pattern":"session-store.js","scope":["ctxroute"]},{"pattern":"session-store.test.js","scope":["ctxroute"]},{"pattern":"pretool-differential.test.js","scope":["ctxroute"]},{"pattern":"turn-count.js","scope":["ctxroute"]},{"pattern":"turn-count.test.js","scope":["ctxroute"]}]
mode: smart
threshold: 28
---
# gate.js / doc-inject.js / corpus.js / session-store.js — SINGLE GATE (LIVE 2026-07-17)

⚠️ `doc-inject.js` = SINGLE PreToolUse hook (matcher `*`): injects ALL docs — file (frontmatters, sources/file.js) AND MCP (docs/mcp/, sources/mcp.js). `legacy-mcp-inject.js` REMOVED from the wiring — rewiring it = double injection, the doctor screams.
⚠️ **COLLECTION = `collect-core.js` (SINGLE SOURCE, 2026-07-31)**: the gate AND `explain.js` collect through the SAME code. Rebuilding an accumulator elsewhere = the introspection tool would describe an engine that does not exist — the very bug it prevents.
⚠️ EXTENSION = `source-adapters.js` (registry, contract documented at the top): new source = 1 PURE module in sources/ + 1 adapter in the registry — NEVER edit doc-inject.js/gate.js for that. docId unique across sources (dedicated prefix).
⚠️ `gate.js` = PURE (Stryker-mutated 100%): decision PER DOC (dumb/once/smart, foreign counters, threshold PER DOC via decl). File corpus 100% dumb ⇒ NO state write at all.
⚠️ PARITY sealed by `pretool-differential.test.js` (file, byte) AND `mcp-differential.test.js` (MCP, old vs new). Any output-format change = rerun BOTH.
⚠️ **4-STAGE CASCADE, SINGLE POINT = gate.js** (2026-08-04): entry > `defaults.{source}` > global (mode/defaultThreshold/defaultDriftUnit) > FRAMEWORK default. TOTAL fallback at every stage. The `defaults.{source}` stage exists ONLY because `decide()` receives `owners` (= `acc.owner`, set by the adapter): removing it would make the stage MUTE while still leaving it schema-valid. Accepted keys = the `id`s of ADAPTERS, DERIVED (config-gate) — `session` is not one (no cadence, cf. skill §2bis).
⚠️ `driftUnit` (2026-07-18): unit of the smart counter, `tool` (framework default) | `turn` — same cascade, never copied elsewhere. `turn` = sensor `turn-count.js` (UserPromptSubmit, store 'turn-count-', MUTE by contract: stdout = injected context). Reset of the 3 stores by ctxroute-reset.js. decide() requires an INTEGER turnCount (0 if unknown) — the gate reads it lazily (only if a matched doc is in 'turn').
⚠️ **PER-AGENT SCOPE DOCTRINE (2026-07-19)**: master and EACH sub-agent = DISTINCT contexts ⇒ once/smart/turn state keyed by `lib.scopeId(session_id, agent_id)` — NEVER session_id alone (shared master/sub-agents, harness contract: proven hole, sub-agents deprived of skills). Without agent_id = historical key (Codex backward compat). Compose the key ONLY via scopeId. Master reset = purge by session PREFIX (sub-agents included); reset with agent_id = targeted. Sealed by the "SUB-AGENT" tests of doc-inject.test.js.
⚠️ **PORTING (2026-07-19)**: common body = `pretool-core.js` (SINGLE SOURCE, run(data, emit)) — the shells keep ONLY their output dialect (~15 lines). Claude = doc-inject.js (allow/deny) · Codex = codex-doc-inject.js (WITHOUT permissionDecision outside deny). Any orchestration change → pretool-core.js, NEVER a shell. reset/turn-count/session-inject wire AS IS on the Codex side (identical dialect).
⚠️ **`confirm`/`ask` REMOVED on 2026-08-05** — 3 decisions and not 4: `none` · `allow` · `deny`. `ask` escalated to a HUMAN (anti 0-human), did not exist on Codex, and `enforce` covers the need better. `WRITE_TOOLS` goes with it (no decision depends on the tool NAME anymore). Anti-return sealed by a REAL spawn on both shells. The gate NEVER reads `.rush` either. `corpus.js`/`session-store.js` = I/O SHARED by the live consumers only (the shadow relic that used to be listed here was DELETED on 2026-08-21): any change = re-prove via their suites + doctor.
⚠️ **`enforce` (2026-08-05) — the gate can STOP the action.** Reason: official doc, a PreToolUse `additionalContext` arrives "next to the tool result" ⇒ an injection CANNOT prevent the action it targets, only the next one. `enforce: true` ⇒ decision `deny`, the doc goes into `permissionDecisionReason` (NEVER into additionalContext: it would arrive after). SAME JSON on both harnesses (verified in the Codex 0.144.6 binary). It stays the EXCEPTION: absent = previous behavior to the byte. ⚠️ This is NOT security but a GUARDRAIL: the gate is fail-open, a dead hook lets everything through.
⚠️ **ANTI-LOOP = ALTERNATION, never a mode ban**: a block is NEVER followed by a block (`denied` flag in the state) ⇒ the retried action ALWAYS passes, then the cadence resumes. The 3 modes are therefore legitimate (`dumb` = block/pass/block). ⚠️ An `enforce` doc writes its state EVEN in `dumb` (without which nothing remembers the refusal); without `enforce`, state shape UNCHANGED — parity intact.
⚠️ **CODEX TOOL COVERAGE — official doc re-read 2026-08-05 (`learn.chatgpt.com/codex/hooks`)**: "code mode" (JS calling `tools.shell_command`) does NOT bypass hooks — *"When a model uses code mode to call a tool from JavaScript, hook decisions apply to that nested call"* ⇒ the hook receives the NESTED call with its canonical `tool_name`, NEVER the JavaScript. Shell and unified exec (`exec_command`) match **like `Bash`** (`tool_input.command`) — `sources/file.js` is compliant WITHOUT change. Do NOT "fix" the engine for code mode.
⚠️ **TWO COVERAGE HOLES ON THE CODEX SIDE, by harness contract (2026-08-05)**: ① HOSTED tools (`WebSearch`…) *"don't use the local function-tool hook path"* ⇒ a `tool: ["WebSearch"]` trigger is MUTE on Codex (HARNESS asymmetry, not a bug of ours — do not hunt it in the engine); ② *"Some specialized tool paths can opt out of the default hook path. Treat tool hooks as a useful guardrail, not a complete enforcement boundary"* ⇒ `enforce` stays a GUARDRAIL, never an enforcement boundary. `write_stdin` does NOT replay `PreToolUse`.
⚠️ **THE DIFFERENTIAL ORACLE IS DATED — compare the CONTENT, not the envelope (2026-08-05)**: `protect-files.js` is FROZEN (its doc forbids it any evolution) and ignores the SEAL born on 08-03. As soon as an injection exceeds 50 % of the budget (4,000 c) the gate seals, the oracle does not ⇒ the RAW byte always diverges, without any engine having changed. `pretool-differential.test.js` therefore unseals before comparing (back-reference on the marker + dedicated negative-check). 🛑 NEVER turn that red green by SHORTENING a doc: that would be degrading a deliverable to fit OUR plumbing.
⚠️ **THE CORE NO LONGER KILLS THE PROCESS (2026-08-06)**: `run()` RETURNS when there is nothing to emit, the SHELL exits. Same layer leak as the transport, another axis. Sealed by `emission-core-gate.test.js` (derived from `*-core.js`).
⚠️ **THE QUEUE HAS LEFT THIS FILE (2026-08-05, ⑯)**: queue + chunking live in `emission-core.js`, the layer that EVERY emitter crosses (derived gate `emission-core-gate.test.js`). Recalling them here would make them optional elsewhere — the exact defect that left the SESSION gate without transport. NEVER redeclare a queue prefix in an orchestration.
⚠️ **EMISSION QUEUE `remainder-` — it is what makes "everything arrives" TRUE.** What does not fit in the N frames of an action is PERSISTED and re-emitted on the next action, **at the HEAD** (RFC 6455: a fragmented document is NEVER interleaved — the queue goes before fresh content, otherwise reassembly becomes impossible). **DEDUP mandatory by base doc** (`doc#3` → `doc`): a `dumb` doc is re-decided at every action and would be re-stacked behind its own chunks. **We store the TEXT, not ids**: the N processes must see the same entry (so the plan memoizes the SEGMENTS) and a doc edited mid-flight would otherwise glue two versions together. **Loop exit**: test `segments.length`, NEVER `r.inject.length` — the queue can carry content while `gate.decide` decided nothing new. Without a lock: neither read nor write of the queue (we degrade to fresh content, never to corruption).
⚠️ **THE QUEUE ONLY DRAINS FOR A CORPUS THAT STOPS BEING RE-DECIDED** (`once`/`smart`). A `dumb` corpus durably above capacity delivers **in ROTATION, indefinitely** — this is CORRECT (`dumb` = "reinject at every action"), nothing is lost, but do not expect an empty queue and do not "fix" this case. Measured 2026-08-05.
⚠️ **THE DEFERRED-STATE RESTORATION LOOP WAS DELETED** along with the queue — and MUST NOT come back. It "unmarked" a deferred doc so it would be re-decided, because a deferred doc was then LOST. Now it is IN FLIGHT: keeping it would deliver the doc twice (queue + re-decision). The "never consumed without being delivered" guarantee has not disappeared, it changed keeper (property ⑧ of `budget.property.test.js`).
🛑 **N GATE DECLARATIONS = THE BANDWIDTH OF ONE ACTION (12 today).** 🔴 This line advocated a SINGLE declaration for 24 h — **FALSE, cancelled on 2026-08-07**: at 1 frame capacity drops to ~7,661 c, so a 53,830 c skill spreads over 8 actions and **the agent acts 7 times with partial knowledge**. The requirement is not display order, it is **the COMPLETE context before the next tool call**. Disorder is glued back with `k/N`; knowledge absent at the moment of acting is not. Setting = `frames` in `ctxroute-config.json`, CONFRONTED with the wiring by `doctor.js --settings` (same number everywhere · indices 1..N with no gap or duplicate) — because the wiring lives outside the repo and two places for one figure diverge in silence. Detail and measurements: `single-frame.md`.
🛑 **THE LOCKLESS FALLBACK READS THE STATE — NEVER put `{}` back there (2026-08-07, prod bug).** It decided with an EMPTY state ⇒ an already-delivered `once` was re-emitted, alone, on its frame (ORPHAN chunk after complete delivery, no compaction, empty queue). **The lock serializes WRITES; the READ never needed it.** We read, we decide, we write NOTHING. ⚠️ No suite made the lock FAIL ⇒ 1096 blind tests: **a degradation path tested only with an empty state is not tested** (sealed "LOCKLESS FALLBACK", 2 parts). 🛑 The previous line said "duplicate REFUTED": a FAILED reproduction is NOT a refutation — honest status = "not reproduced to date". Story: `REFACTOR-PLAN.md`.
✅ **THE SAME DUPLICATE BY THE OPPOSITE DOOR — FOUND BY TLC, CLOSED 2026-08-20.** The fallback DELIVERED a fresh `once` and RECORDED NOTHING (it may not write), so the next leader re-decided it as fresh and delivered it TWICE. Fix proved by `TransportCandidateFix.cfg` BEFORE being written: the fallback delivers `gate.injectLockless` = everything EXCEPT `once`. A `once` is DELAYED by one action, never duplicated. `TransportKnownDefect.cfg` is now a LEGACY negative-check, required RED for ever — green there = `AtMostOnceDelivery` has stopped discriminating.
🔴 **AND A THIRD DEFECT OF THAT FAMILY, SAME DAY: A REFUSAL THAT CANNOT REMEMBER ITSELF.** The anti-loop ("a block is never followed by a block") is anchored in the `denied` flag IN THE STATE; this path may not write ⇒ a `deny` it emitted left no trace, so the redone action was refused AGAIN, for as long as the contention lasted — the infinite block the alternation exists to forbid. ⇒ **THE LOCK-LESS PATH NEVER REFUSES**: `decisionLockless ∈ {none, allow}`. The doc is still DELIVERED; the refusal lands next action, under the lock, WITH its memory. 🛑 A rule whose termination depends on being recorded may not be applied by whoever cannot record. Seen RED first (cell ⑤ of `transport-conformance.test.js`), and it eliminated BY CONSTRUCTION the `every`→`some` mutant that no test could tell apart.
✅ **AND THE READER NOW CROSSES THE WINDOW — CAUSE ESTABLISHED BY TWO CONCORDANT CI MEASUREMENTS (2026-08-20).** Replacing a file on a Windows runner leaves a window in which the NAME does not exist; a reader landing in it answers `{}`, which ASSERTS "nothing has ever been injected" and the document goes out a SECOND time. Measured twice on the same suite: `{"ENOENT":512}`, then `{"ENOENT":593,"transient":1}` — **100 % absence**, zero `EPERM`, zero partial read, and the `transient` proves the file WAS there right after. **The atomic write was never in question.** ⇒ `readThrough` retries **ONLY on `ENOENT`**, immediately, with the same bound as the write side: absence is the one error a concurrent rename can fabricate, while `EPERM` or a truncated JSON are REAL problems that retrying would HIDE. 🛑 **UNREPRODUCIBLE LOCALLY** (0 out of 7,164, reader and writer pinned to a single core) ⇒ the DECISION is isolated from the I/O and driven with an INJECTED reader — racing to prove a race is how a suite becomes flaky in turn, and it is why a first attempt at this fix was rightly rejected the same day. Seen RED. ⚠️ The retry may cross an absence, it may never INVENT a presence: an absence that lasts still answers `{}`.
🛑 **`session-store` WRITES ATOMICALLY (tmp + rename, bounded retry) — NEVER go back to a direct `writeFileSync`.** That one TRUNCATES before filling: a concurrent reader reads cut JSON, `loadState` returns `{}`, and that `{}` ASSERTS "nothing was ever injected". MEASURED at the real fleet size: 4,779 hollow reads out of 16,656. ⚠️ On Windows, replacing a file BEING READ fails with `EPERM` (1,045 measured) ⇒ immediate retry is MANDATORY, otherwise the write is silently lost. Solidary with the lockless fallback above: one without the other does not hold.
⚠️ **ORDER, HOWEVER, REMAINS UNGUARANTEED** (official doc: hooks in parallel, aggregation unspecified): 🛑 NEVER code a precedence chain to force it — that would be betting on undocumented behavior, forbidden by `budget.md`.
⚠️ **WHAT REMAINS TRUE OF THE MECHANISM**: the SHELL passes the index and `invocationId` (`tool_use_id`) — the core NEVER reads a harness field. **Plan MEMOIZED per invocation** (store `plan-`, purged by ctxroute-reset): it remains the guardrail that prevents a second call to `gate.decide` from consuming the `once` docs. Incomplete declaration ⇒ single COMPLETE frame, never a broken split.
⚠️ **BADGE = NAME + POSITION, and the SUFFIX is composed ONLY ONCE in `pretool-core.js` (2026-08-06)** — never in the adapters' `message()` (4 copies that would diverge at the first added source). Empty badge ⇒ suffix removed: an orphan "(chunk 3/7)", without saying OF WHAT, worries more than silence. ⚠️ **REAL BUG fixed the same day**: the `[source:]` tag lives at the END of a document, so NO chunk except the last carries it ⇒ `docLabel` fell back to its "markdown title" fallback and caught the SEAL FOOTER ("📄 doc: ##END:7426e64b###"). Two SOLIDARY fixes: CommonMark-compliant ATX regex (space mandatory after the `#`) + fallback on `acc.labels` — one without the other leaves either a false name or NO name. 🛑 The FILE badge IGNORES `showNotification` (protect-files parity), MCP and `tool` respect it: DELIBERATE asymmetry, do not "harmonize".
⚠️ **54 (2026-08-15): the shell's dialect is an exported PURE function** — `doc-inject.js::output(decision, fullDoc, systemMessage)` RETURNS the JSON, `emit` (I/O) prints it and exits. In-process tests consume `sortie` + `pretool-core.run`: NEVER a twin of the format. 🛑 **The deadline (`deadline.arm`) is armed INSIDE the `require.main` block** — hoisting it to the top of the file would make a plain test `require` arm the fail-open timer and KILL the runner in the middle of a suite. In hook execution nothing changes: the block is the first I/O. 🛑 The spawns marked `spawn: true` in `doc-inject.test.js` are the process CONTRACT (stdin/exit/cross-process lock) — never convert them "because it's covered in memory".
⚠️ **GLOBAL FILTER (52, 2026-08-15)**: `decide()` receives the TARGET (7th argument, `toolName`) and RETURNS `filteredOut`; the gate announces `🚫 N doc(s) excluded by filterMode/filterList` on the 1st frame ONLY (lesson of the alarm: N identical shouts = unreadable), `filteredOut` is MEMOIZED in the plan (frames 2..N see the same finding). A filter that cuts in silence = a mute hole — sealed by spawn (`doc-inject.test.js`, FILTER case 52).
⚠️ **THE BADGE NAMES ALL DELIVERED DOCS, NOT JUST ONE (2026-08-07)** — `labelDoc` read `injected[0]`: four docs delivered, ONE named. **REAL consequence, not cosmetic**: the maintainer saw "chunk 1/8", "2/8", then another name, and concluded that delivery was STOPPING — it was complete. A morning lost on a nonexistent failure. Dedup by `budget.baseId` (otherwise `doc#2` and `doc#3` would cite the same name twice), **3 names max then `+N`** (a badge is a status line, not a table of contents). 🛑 **SINGLE-DOC CASE UNTOUCHED, to the byte** (`[source:]` tag then `acc.labels` fallback): that is the protect-files parity, and `pretool-differential` seals it. ⚠️ This differential now DECLARES the multi-doc gap (exact prefix + suffix limited to doc names) — the oracle has been frozen since July, requiring strict badge equality would forbid any display improvement.
🛑 **`[source: <path>]` IS A CONTRACT, NOT AN ORNAMENT (2026-08-08)** — the agent reads the EXACT path there to go UPDATE THE DOC when it finds it wrong: that is the loop that makes the corpus self-repairing. The ordinal `[DOC i/T]` is placed **AFTER the closing bracket, NEVER inside** — inside, `gate.js`'s capture produced "<path> — DOC 2/5", an INVALID path. 🔴 **538 tests were GREEN on it**; the maintainer is the one who saw it. Sealed since by a real spawn (`doc-inject.test.js`, anti-mute-probe witness). Silent at T=1 ⇒ protect-files parity intact.
⚠️ **DIFFERENTIAL PARITY**: the ordinal is stripped BEFORE comparison by `differential-normalize.js` (SINGLE SOURCE of both nets), **anchored on the source tag** — never a blind erasure, otherwise a `[DOC x/y]` in the BODY of a doc would be swallowed and the differentials would go one-eyed. Its negative-check is MANDATORY: an untested comparison filter can mask a real regression.
⚠️ **`systemMessage` IS DISPLAYED WITH NO CONFIG AT ALL — official doc `code.claude.com/docs/en/hooks`, re-read 2026-08-06**: *"Warning message shown to the user"*, zero flag, zero verbose mode, zero `Ctrl-R`. **Contrast worth knowing**: a hook's RAW `stdout` goes to the debug log and is NOT shown (except `UserPromptSubmit`/`UserPromptExpansion`/`SessionStart`, where it becomes CONTEXT) — `systemMessage` is therefore the ONLY channel to speak to the HUMAN, `additionalContext` the only one to speak to the AGENT. Never confuse them nor try to display via stdout. ⇒ **whoever clones the repo sees the badges as soon as the hooks are wired**: nothing more to install, the framework provides NO display component (same principle as the canary).
✅ **THE FRAMEWORK NOW SAYS WHEN IT DELIVERS LESS THAN IT DECIDED (2026-08-21).** The lock-less path delivers `injectLockless` — everything EXCEPT `once` — and it did so in TOTAL SILENCE: the agent acted once without knowledge it was owed, no error, no red, no badge, **on every lock contention in production**. `pretool-core` counts `inject − injectLockless` and announces `N doc(s) WITHHELD`, ONCE per action (`indice === 1` — sixteen identical shouts are an alarm nobody reads).
🛑 **IT ANNOUNCES A COUNT, NEVER A CAUSE.** "The lock was busy", "the daemon is down" are guesses about WHY made by a layer that only observes WHAT — and the count is what makes the notice UNIVERSAL: the same true sentence on every harness, on the spawn lane and the daemon lane alike, **with no liveness probe of any kind**. That is also why "no daemon = total silence" is not a daemon problem: it is one case of this rule.
🛑 **WHEN EVERYTHING IS WITHHELD, THE FRAME SPEAKS WITHOUT DECIDING** — `pretool-core.noticeOutput` returns `{systemMessage}` ALONE, shared by both shells like `denyOutput`. Official doc read 2026-08-21: `permissionDecision` is OPTIONAL on PreToolUse and its ABSENCE leaves the normal permission flow untouched. The usual envelope would carry `permissionDecision: "allow"` with an EMPTY `additionalContext` — a warning that AUTHORISES the tool call as a side effect. **A notice must never change a decision.** ⚠️ It follows `showNotification` like every other badge (a total silence stays total), and it is NOT a fourth decision: the three remain `none`/`allow`/`deny`. Seen RED on both positive cells with the control cells still green (`transport-conformance.test.js`, cells ⑥).
