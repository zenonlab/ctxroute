---
match: [language-spec.js, spec-differential.test.js]
scope: [ctxroute]
mode: smart
rank: 512
---
# language-spec.js — THE INDEPENDENT MODEL (the only judge of SEMANTICS)

🛑 **THIS IS NOT THE ENGINE, AND IT MUST NEVER BECOME IT.** It is the definition of what the language MUST do, in quantifiers: `trigger` = **∃** over the candidates · `scope` = **∃** over the params (AND between groups) · `exclude` = **∀¬** over params ∪ context. `spec-differential.test.js` confronts this model with the REAL engine, **exhaustively**.
🔴 **REASON FOR EXISTING: every other test calls the engine, so they prove what it DOES, never what it SHOULD DO.** When the semantics were wrong, they were wrong on both sides and everything stayed green — ㊵ (scope blind to the 16 MCPs), ㊴ (operators ignored on one dimension), ㊼ (`exclude` existential therefore bypassable). Three sessions, each saved by a human who insisted. **That is what we replace with a machine.**
⚠️ **WRITTEN FROM THE INTENT, NEVER COPIED FROM THE ENGINE** — a twin only proves that a copy agrees with itself (session lost on 2026-07-31: 3 homemade probes, 3 false verdicts). Modifying the engine **never authorizes** "adjusting" the spec to silence a red: a divergence says the two disagree, you must **DECIDE which one is right and write it down**.
📐 **EXHAUSTIVE = A PROOF, NOT A SAMPLE**: finite and small domain (3 atoms) ⇒ **408,996 cases** (3,246 rules × 126 action forms — BOUNDARY forms between two values included, 53bis) checked in ~1 s, zero dependency. AWS (Cedar/Zelkova) goes through Lean and SMT solvers because their domain is INFINITE; on a domain you can exhaust, the `for` loop is strictly stronger. Assumption accepted: *small scope* (a semantic defect shows up on small cases).
⚠️ **THE DOMAIN MUST OFFER EVERY FORM OF THE LANGUAGE** — grouped forms of `scope`, `cd X && …`, nested params, `apply_patch`, MCP tool names. Removing one makes the differential blind to a whole class: without `cd X && …`, ㊼ was **unreachable** and the laws passed BY VACUITY (measured the same day).
✅ **FIRST HOLE FOUND BY THE MACHINE BEFORE THE HUMAN (53bis, 2026-08-15)**: the spec (per-value since birth) contradicted the engine's `join(' ')` concatenation — a pattern with a space matched astride two params. The differential turned RED as soon as the domain carried the boundary form, the engine was aligned on the spec, 0 change over 7,355 real actions. That is exactly the role this file exists for.
⚠️ **NEGATIVE-CHECK PROVEN ON 3 DISTINCT SABOTAGES** (2026-08-14): `exclude` put back to existential · the flat form of `scope` turned into an AND · trigger widened to all params ⇒ **red every time**. A differential you have never seen turn red proves nothing.
✅ **ALL 4 SOURCES ARE COVERED since 2026-08-15 (㊻③)** — the judge only covered `file` (1 of 4; ㊴ lived in an uncovered source). `toolInjects` (∃ EXACT name + wildcard, never a substring) · `mcpCandidates` (the PATH is the trigger: global→specific, SAFE segments — `../evil` = zero candidate; NO filter: since 52 the global filter lives in gate.js, outside the sources' perimeter) · `skillInjects` (UNION of the 3 dimensions, `cwd` in the params of the file dimension only, filters applied EVERYWHERE — context = tool name). **+148,320** exhaustive cases, negative-checks: wildcard on an empty name · unsafe sub-tool · `servers` in all-or-nothing (= ㊴). ⚠️ The FILTERS (`filters()`) are ONE shared function: a source that "forgot" them diverges, that is the point.
⚠️ **WHAT IT WILL NEVER PROVE**: that the SPEC is what we want. That remains a human judgment — but it bears on ~40 lines of quantifiers instead of 200 lines of engine. Never sell it as a proof of absolute correctness.
⚠️ **PURE and PUBLISHABLE**: zero I/O, zero dependency, zero harness dialect, zero personal data. `npm run test:spec`.
