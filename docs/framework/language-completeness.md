---
match: language-completeness.test.js
scope: ["ctxroute"]
mode: smart
rank: 569
---
# language-completeness.test.js — the language's SCOPE, computed and not re-read

🔴 **BORN OF A FALSE PROMISE THAT LIVED 3 WEEKS**: the skill announced "OR+AND+NOT = completeness ⇒ ANY condition is expressible". Plausible, well written, and **nothing confronted it with the engine**. A language's scope is not re-read — it is **COMPUTED**.
📐 **MEASURED FACT, RE-EVALUATED 2026-08-14 AFTER ㊺①**: **128 out of 256**, and above all **CHARACTERIZED** (part ⑤) — the sayable is EXACTLY `{f | f(empty action) = false}`, **0 missing, 0 extra**. ⇒ **the language is COMPLETE**, up to ONE structural constraint: nothing injects on an action that contains nothing (the load-bearing wall "we only inject on FACTS"). `A ∧ B` ✅ · `A ∧ ¬C` ✅ · **`A ∧ B ∧ C` ✅ since ㊺①** (`scope: [["a"],["b"]]`) — part ② was therefore **INVERTED, never deleted**, as this file had required since 08-12. ⚠️ A COUNT does not say WHAT is missing: the characterization is what answers ㊻, not the number.
⚠️ **THE GENERATOR MUST OFFER ALL FORMS OF THE LANGUAGE**: without the GROUPED form of `scope`, the enumeration CANNOT produce `A ∧ B ∧ C` and part ② would turn green while proving only that the GENERATOR is incomplete. **An amputated enumeration measures a scope that does not exist.**
⚠️ **METHOD**: all possible rules (1 pattern × every FLAT AND GROUPED `scope` × every `exclude`) → truth table of each **by calling `sources/file.js::matchingDocs`** → closure by UNION (several `rules` = OR). The result is the EXACT set of the sayable.
🛑 **NEVER reimplement the matcher here to "predict" the tables**: we would prove our copy agrees with itself. Mistake paid with a session on 2026-07-31 (3 homemade probes, 3 false verdicts). We call the source, period.
⚠️ **3 atoms = a REASONED choice, not a convenience**: the thesis to refute is "at most TWO positive conjunctions" ⇒ the minimal counter-example is at 3. Widening costs 2^(2^n) without proving anything more.
🛑 **RULE ACTUALLY APPLIED ON 2026-08-14: the scope changed (㊺①), part ② was INVERTED, NEVER deleted.** It is the only thing preventing "we can do anything" from coming back without proof.
⚠️ **Part ④ = anti-dormancy**: an error making the set EMPTY would turn ② green while proving NOTHING. It therefore requires the enumeration to really observe, and that NOT everything be reachable.
⚠️ **㊼ DID NOT MOVE THIS NUMBER (120/256 before AND after that particular fix; it is ㊺①, the same day, that brought it to 128), and that is NORMAL.** This harness places all atoms IN THE SAME place so that the 3 operators see the same universe ⇒ it measures BOOLEAN EXPRESSIVENESS, never an operator's UNIVERSE. ㊼'s contribution (`exclude` = ∀¬ over all params) is a universe widening: it is proven in `sources-file.property.test.js` (monotonicity and write-independence laws). 🛑 Do not conclude "nothing changed" from a count that is insensitive by construction.
📐 **RE-MEASURED WITH `keys` IN THE GENERATOR (19/08/2026): 128/256, UNCHANGED.** `keys` was offered to the enumeration for ONE reason — this file's own rule is that the generator must carry EVERY FORM of the language, and it was the amputation of ㊺① that once produced a false green. **The result is now COMPUTED, no longer assumed**: this harness puts every atom in the SAME key, so narrowing the key universe can only blind a rule, never express a NEW truth table. `keys` widens the UNIVERSE, this file measures BOOLEAN EXPRESSIVENESS — two orthogonal axes, exactly like ㊼ before it. 🛑 The value lies entirely in having measured it: "an operator on the universe cannot change the boolean scope" is plausible, and this repo has already shipped a plausible sentence that stayed false for three weeks.
