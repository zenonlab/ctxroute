---
rules: [{"pattern":"legacy-mcp-inject.js","rank":350},{"pattern":"ctxroute-reset.js","rank":351},{"pattern":"ctxroute-config.json","rank":352},{"pattern":"legacy-mcp-inject.test.js","rank":353},{"pattern":"lib-pure.js","scope":["ctxroute"],"rank":362},{"pattern":"lock.js","scope":["ctxroute"],"exclude":["package-lock.json"],"rank":365},{"pattern":"stdin-json.js","rank":368}]
mode: smart
threshold: 20
rank: 350
---
# ctxroute — moved to a standalone folder (separate git repo)

The framework (code + tests) now lives in `~/Desktop/ctxroute/` (clean git repo, pushable to GitHub without mixing with the rest of the home directory).
`settings.json` wiring → absolute paths to that folder: `doc-inject.js` (the GATE — **32 declarations of type `http` since 2026-08-22**, pointing at the daemon on `127.0.0.1:8787/pretool?frame=k&frames=32`; setting `frames`. 🔴 **100 WAS TRIED THE SAME DAY AND ROLLED BACK IN MINUTES**: the transport made a frame nearly free (5 300 ms → 182 ms per action at 16, 1 040 ms at 100), but the CLI renders ONE hook entry PER DECLARATION per tool call, and 100 of them made the display unusable. **The win came from the TRANSPORT, never from the count**: more frames buy CAPACITY, never speed. 🔴 **AND « capacity nobody needs » WAS TRUE OF 100 AND FALSE GENERALISED — REFUTED BY MEASUREMENT 2026-08-22**: at 16 frames the skill ALONE took 121,809 c of a 122,944 c action, leaving **1,135 c (0.9 %) for every other doc**, so `capacity-alarm` was RED on the real fleet corpus and at least one doc was DEFERRED on EVERY action. Raised to **32** the same evening — 245,888 c, half the action free, alarm green. 🛑 The ceiling is the DISPLAY, and its knee is NOT measured: the 100 rollback changed the count AND `statusMessage` in one go, so nothing attributes it. Known: 32 reads fine, 100 does not), `ctxroute-reset.js`, `turn-count.js`, `session-inject.js`, `canary-check.js`. ⚠️ `legacy-mcp-inject.js` is NOT wired (relic = differential oracle).
Full internal doc: `Desktop/ctxroute/HOOK-INTERNALS.md`. Usage/config: `Desktop/ctxroute/README.md` (and skill `.claude/commands/ctxroute.md`).
⚠️ The maintainer's personalized MCP docs (`docs/mcp/stripe.md`, `odoo.md` — with real emails/clients) live in `Desktop/ctxroute/docs/mcp/*.md`, gitignored. Only the generic `.md.example` files are pushed to GitHub.
Framework change → edit in `Desktop/ctxroute/`, NOT here (this folder no longer contains the code).
⚠️ **PRODUCTION RUNS A FROZEN COPY SINCE 2026-08-24 — `Desktop/ctxroute-release`, NEVER the working repo.** The daemon exits on its own source change (`watchOwnCode`, correct by design), and production ran from the directory agents edit, so every delivery cost the whole fleet its injection window — 32 red lines per tool call while it came back. 🔴 **AND « THE FROZEN COPY MAKES THE CAUSE DISAPPEAR » WAS FALSE — MEASURED THE SAME DAY, 2026-08-24.** Nothing had written to that copy since it was built and the daemon killed itself **258 times** anyway: an independent witness caught the event at the millisecond of a death (18:47:19.717Z) with `mtime` 13:46:20 and `ctime` 13:53:22 both UNCHANGED and **only `atime` moving**. libuv subscribes ReadDirectoryChangesW to `FILE_NOTIFY_CHANGE_LAST_ACCESS` among eight filters and delivers every one as a bare `'change'`, and NTFS may defer that access-time write by up to an hour (`fsutil behavior`) — so **merely READING a file killed the service**, up to an hour later. Freezing the copy removed the WRITES, never the notifications. ✅ The real cure shipped the same day: freshness is an OBSERVATION at the point of use (the bytes this process compiled vs the bytes on disk), so a spurious notification kills nothing and a LOST one can no longer let stale code be served — `stale-code.md`. The frozen copy stays, for its own reason: a delivery is then a gesture the operator chooses, never a surprise mid-session. 🛑 Never repoint the launcher (the OS launcher that starts the daemon) at the working repo. **Delivering = rebuild the copy (`git archive HEAD` + `npm ci`) then restart the scheduled task**, at a moment you choose. 🛑 **EXTRACT INTO THE EXISTING DIRECTORY — NEVER build beside it and RENAME (measured 2026-08-30).** On Windows the running daemon HOLDS that directory: a rename fails with `Permission denied`, and killing the daemon to free it makes the exit-90 trigger restart it instantly, which re-locks the directory — you end up fighting the guard that is doing its job. 🔑 **AND IT IS ONE GESTURE, NOT TWO**: once the bytes on disk differ, the daemon observes it at its next request, exits 90 on its own and the trigger relaunches it on the new code. **Never kill the daemon, never disable the task, never rename anything.** Disabling the task is how the whole fleet silently loses its injection until someone notices. ⚠️ The DATA stays live: the configuration lives at the OS conventional address (`%APPDATA%\ctxroute\ctxroute-config.json` on Windows) and DECLARES where the state and the docs are, so editing a doc still reaches production instantly. The repo-local config was REMOVED — two configs would be two truths. ⚠️ The `command` hooks of `settings.json` still point at the working repo: they are spawned fresh at every event, so stale code cannot happen to them; only the long-lived daemon needed the frozen copy.
