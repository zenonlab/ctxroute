---
rules: [{"pattern":"skill.js","scope":["ctxroute"]},{"pattern":"sources-skill.test.js","scope":["ctxroute"]},{"pattern":"skill-registry-gate","scope":["ctxroute"]}]
mode: smart
threshold: 20
---
# sources/skill.js — the "skill" SOURCE: triggers a skill by PERIMETER (0 duplication)
⚠️ PURE, mutated 100%. REUSES `matchingDocs` (FILE dimension: `match`/`scope`/`exclude` OR per-entry `rules` since 07-19 — SAME vocabulary as the docs, `perimeter` removed 07-18, a synonym = duplication; `rules` exclusive with match/scope/exclude, `not` schema + runtime precedence, validation delegated to matchingDocs — zero duplicated guard) + `lib.serverName`/`toolSuffix`/`getByPath` (MCP dimension, 3 GRAINS: `"srv"` · `"srv/tool"` · `"srv/sub-tool"` via subToolParam) — NEVER a 2nd matcher. Both dimensions in a UNION deduped per doc.
⚠️ Injects the SKILL BODY read LIVE (`paths.skillsDir()`, harness frontmatter stripped) — maintainer decision 2026-07-18: MECHANICAL injection guaranteed, never "hoping the agent obeys". Zero duplication: the skill file = the only truth, read at every injection. Pointer fallback ONLY if the file is unreadable. The read lives in the ADAPTER (source-adapters), the source stays PURE.
⚠️ CADENCE (2026-08-04): `declFor(entry)` SETS the entry and resolves NOTHING — invalid value = key OMITTED. The whole cascade (`defaults.skill` > global > framework `once`) lives in gate.js, SINGLE POINT. ⚠️ `skillDefaults` REMOVED (generalized into `defaults.skill`), and `declFor` no longer takes a 2nd argument: giving it back would resurrect the DOUBLE cascade (two resolutions that diverge in silence) — a case in sources-skill.test.js turns red if it comes back. ⚠️ DELIBERATE ASYMMETRY: a skill SKIPS the global stage (default `once`, docs `smart`) — unifying would flip ALL skills at the first global config set.
⚠️ **`enforce` (2026-08-05)**: `declFor` SETS it as is, **`false` INCLUDED** — that is what lets a skill opt out of a `defaults.skill.enforce: true`. Filtering it as an "empty" value would make opting out IMPOSSIBLE. Non-boolean value = key OMITTED (never a guessed block).
⚠️ Registry = the `skills` section of the global JSON (config we OWN), NEVER the skill's frontmatter (a HARNESS file, format not under our control, fragile to updates). Sealed by `skill-registry-gate` THREE WAYS: named skill = existing file · every harness skill = registered OR listed in `skillsWithoutPerimeter` (07-19, silence is not an option — mirror of `inject: never`) · **and a name answers ONCE (08-21)**. 🔴 The first two are both satisfied by a CONTRADICTION — a name in BOTH keys is neither missing nor undeclared — so `zenon-dispatcher` sat in both for a day with nothing red. The keys are mutually exclusive BY DEFINITION (auto-inject on a perimeter vs deliberately on-demand): a name in both declares an intention and its negation, and whichever the engine honours, the other half is a lie the next reader trusts. Fail-closed: refuse, never pick.
⚠️ **3rd DIMENSION `tool` + `scope`/`exclude` ON `servers` (㊴, 2026-08-12) — delivered TOGETHER.** A skill could only react to a PLACE, never to the CONTENT of a parameter: `servers` was ALL OR NOTHING (a client's folder got injected while writing to ANY other one) and `tool` was REFUSED by the schema. REAL cost: a client email written without its folder, ~10 versions. 🛑 **NO word created** — `tool`/`scope`/`exclude` existed for the docs; we extend their USE, the OR/AND/NOT base stays CLOSED. `vise` (wildcard `*`) and `shouldSkip` come from the `tool`/`file` sources: NEVER a 2nd copy. BARE wildcard (without `scope` or `exclude`) = REFUSED by the schema, EXACT parity with `frontmatter.validate`.
🔴 **`skillRules` REBUILDS ITS RULE FIELD BY FIELD — ANY OPERATOR ABSENT FROM THAT LIST IS BORN INERT (19/08/2026).** The other three dimensions (`rules`, `servers`, `tool`) hand the WHOLE entry to `file.shouldSkip` and therefore honour everything; this one does not. Measured result: `keys` — shipped with its schema, its validator, 959 tests and 100 % mutation — was **inert on 8 fleet entries out of 8**, `match` being the only form in use. That is class ㊴, one week later, in the same place. ⇒ sealed by `operator-consumption-gate.test.js` (behavioural probe PER DIMENSION, operator list DERIVED from `RULE_KEYS`): adding an operator without propagating it turns red, by name.
⚠️ New field/dimension → ctxroute-config.schema.json FIRST (config-gate) + mirror mutate/include/mutation.yml/dep-cruiser (mutation-workflow-gate) + re-mutation 100%.
⚠️ The `if (server == null)` guard is NECESSARY since the tool grain (2026-07-18): the concatenation `server+'/'+suffix` would be 'null/null' → a pathological entry would match any non-MCP tool. Tested (no longer an equivalent mutant). `includes(null)` remains the rejection of a missing subCand.
