---
rules: [{"pattern":"quadratic-budget.json","scope":["ctxroute"]},{"pattern":"no-undeclared-quadratic.yml","scope":["ctxroute"]},{"pattern":"quadratic-gate.test.js","scope":["ctxroute"]},{"pattern":"quadratic-budget-pure.js","scope":["ctxroute"]},{"pattern":"quadratic-budget-pure.test.js","scope":["ctxroute"]}]
mode: smart
threshold: 30
---
# quadratic-budget.json — COMPLEXITY DECLARES ITSELF (third twin of TIME and SPACE)
🛑 **ASK "AND AT 10,000?" BEFORE EVERY LOOP.** A scale defect does not slow anything down, it CLOSES A CONTRACT (target: hundreds of sites, thousands of pages) and its retrofit is prohibitive, so it belongs to the SPEC. This gate exists because the rule lived in PROSE and prose refuses nothing: a quadratic write path reached `src/memory-store-pure.js` and was caught by LUCK (two cells timing out), not by a judge.
⚠️ **SEPARATION OF POWERS, DO NOT BLUR IT**: `rules/no-undeclared-quadratic.yml` DETECTS with **zero exemption**; `quadratic-budget.json` + `src/quadratic-budget-pure.js` + `test/quadratic-gate.test.js` carry the POLICY and the RATCHET. 🛑 No `ignores:`/`files:` and no glob exclusion (`!**/*.test.*`) in the rule — that would be a PERMANENT hole, sealed by a dedicated check.
⚠️⚠️ **A TRAVERSAL IS NOT ONLY A `for` LOOP**: the only O(N²) ever measured on this doctrine was a `.find()` INSIDE a loop, syntactically NOT a nested loop. The rule targets the 4 loop NODES **and** the array methods — shrinking that list makes the gate hollow AND bypassable by rewriting in `.map()`. ⚠️ A chained `a.map(f).filter(g)` matches too (the receiver is a descendant): write one traversal per statement.
🛑 **NON-CAPTURING METAVARIABLES (`$_IT`, `$$$_A`) — NEVER remove the `_`.** Capturing, they UNIFY across the two levels: the first version required the SAME array inside and outside, so `xs.map(x => ys.find(...))` went GREEN. It saw 4 shapes out of 5 and looked correct — found by the WITNESSES before service, never by reading.
⚠️ **BUDGET KEY = RELATIVE PATH, never a basename**: this repo holds homonyms across `src/`, `src/sources/`, `src/hooks/`, `tools/`, `test/` — two melted into one key make the red NON-ACTIONABLE.
⚠️ **THE BASELINE IS GENERATED BY MEASUREMENT OF THIS REPOSITORY**, never copied from another: the recipe is shared across the fleet, the NUMBERS are not, and an imported figure is an INVENTED ratchet that protects nothing.
⚠️ **THE RATCHET IS AN EQUALITY**: measured above = a new nesting, prove the class or remove it (🛑 NEVER raise a `max` to make a push go through); measured below = a STALE declaration, LOWER IT, or the ground gained is silently taken back. **A file ABSENT from `files` is held at ZERO** — 100 % of the rule on new code without first repairing the old. Zero phantom entry: a declaration without occurrences is a DORMANT PERMIT.
⚠️ **`INHERITED_DEBT` IS THE MEASUREMENT OF THE DAY, NOT A JUSTIFICATION** — exempt from `why` ON PURPOSE: demanding one would produce INVENTED sentences, and a false justification makes a case look settled. Every other class is an act of instruction: `why` over 60 characters, plus a NUMERIC `impact` of at least 40 on a `DEBT` (a budget measures QUANTITY, never GRAVITY).
🔴 **THE MUTE SCAN IS NOT THEORETICAL**: `ast-grep` returns an EMPTY JSON with EXIT 0 on a path outside the project, so a misresolved perimeter is INDISTINGUISHABLE from a clean repo and the gate would be green FOR EVER. Hence the perimeter floor + a CONTROL smuggled into a real scan + a WITNESS per atom DERIVED from the `.yml`. Perimeter from `git ls-files`, files named to `ast-grep` EXPLICITLY (a tree walk honours `.gitignore` and goes blind without a word).
⚠️ **FAIL-CLOSED ON THE BINARY**: absent ⇒ THROW. 🛑 Never a silent fallback on `npx`, which fetches a stranger package from the network — a gate that finds its tool "some other way" can also fail to find it and stay quiet. ⚠️ Witnesses and decoys live in the OS tmpdir: a decoy in `src/`/`test/` would make PARALLEL gates go red at random.
⚠️ **THE VERDICT LIVES IN `src/quadratic-budget-pure.js`, PURE AND MUTATED — never in the test file** (Stryker does not mutate test code; a rule written in a suite stays green on an inverted comparison for ever). Zero I/O, imported DIRECTLY, THUNK fixtures. The two count comparisons are TWO independent `if`s on purpose: nested, `>` → `>=` would be an EQUIVALENT mutant, hence an eternal survivor. Sabotage IN MEMORY only.
⚠️ **LEAVING A DEBT = inverted index / Map / Set, at PROVEN equivalence** (property-based, oracle = the old code). 🛑 Never a probabilistic algorithm (LSH, sampling): here a false negative has a client cost. EXIT CONDITION of the file: zero `INHERITED_DEBT`.

🔴 **THE GATE WAS RIGHT, IT WAS SIMPLY NEVER RUN LOCALLY BEFORE THE PUSH THAT PUBLISHED IT (2026-08-30).** Two test files shipped in the SAME commit as their own nested traversals (`test/daemon-dialect-gate.test.js`, `test/delivery-notice-integration.test.js`, 3 occurrences each) with no budget entry — held at ZERO by design, hence RED. 🛑 **NOT an OS-count divergence**: reproduced identically on Windows with `npx vitest run test/quadratic-gate.test.js`, same two files, same counts. The `58`/`259` figures seen elsewhere in the same CI run are UNRELATED `ast-grep` stderr noise from OTHER gates' own scans in the same job, not this gate's verdict — a re-measured premise, not a confirmed one. ✅ **FIX = DECLARE, not widen**: both classified `O(N)` with a written `why` — every occurrence walks a collection bounded by THIS repository or by a fixed test constant (tracked files, a local `require()` graph, ast-grep matches on one decoy, a fixed 5-frame loop), never by a fleet's sites or pages.
