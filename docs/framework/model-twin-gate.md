---
rules: [{"pattern":"model-twin-budget.json","scope":["ctxroute"]},{"pattern":"model-twin-gate.test.js","scope":["ctxroute"]},{"pattern":"model-twin-pure.js","scope":["ctxroute"]},{"pattern":"model-twin-pure.test.js","scope":["ctxroute"]}]
mode: smart
threshold: 30
---
# model-twin — A MODEL MAY NOT SHARE CODE WITH WHAT IT JUDGES
🔴 **THE CLASS: an independent model exists ONLY to CONTRADICT the engine, and a model that copies the engine's SHAPE cannot.** On 2026-08-23 `cadence-spec.js` and `gate.js` both wrote `{ seen: true, sinceLastCall: entry.sinceLastCall + 1 }`: the engine dropped `denied`, the model dropped the same flag, and **the 11,346 exhaustive cases were GREEN on that production bug from the day they were written.** The heaviest judge in the repository certified a defect, in silence, for weeks. 🔴 **AND THE OBVIOUS « MISSED SIGNAL » IS A FALSE MEMORY — REFUTED BY MEASUREMENT THE SAME DAY.** It is tempting to say the trace existed: `jscpd` HAD reported those two loops as an 11-line clone, and the model's own comment said so. **But that clone was UNCORRELATED with the defect** — it is reported identically on the REPAIRED repository, because it comes from the surrounding shape, not from the faulty statement. Nobody missed a warning; **no warning existed.** ⚠️ Say it this way and never the other: « we had the clue and ignored it » comforts, and it would send the next reader to tune a tool that CANNOT see this class.
⚠️ **THE RULE THIS MAKES TRUE IS THE ONE ALREADY WRITTEN IN `cadence-spec.md` AND `language-spec.md`** — *written from the intention, NEVER copied from the engine; a twin only proves a copy agrees with itself.* That prose existed on 2026-08-23 and stopped nothing. This gate is what turns it into a refusal.
⚠️ **SEPARATION OF POWERS, DO NOT BLUR IT**: `src/model-twin-pure.js` → `tokenize` + `sharedRuns` DETECT, with **zero exemption and no way to grant one**; `model-twin-budget.json` carries the POLICY and the RATCHET; `verdict` (same module, PURE hence MUTATED) confronts them. A verdict in the suite would be unverifiable — Stryker does not mutate test code.
🛑 **THE PAIRS ARE DERIVED, NEVER LISTED.** A MODEL is a src module that is ① PURE ② deliberately OUTSIDE Stryker's `mutate` ③ imported by **NO** production module — it exists only to be confronted ④ confronted by a suite that also imports mutated modules, which are exactly its JUDGED set. Condition ③ is what separates a model from a shared core or a data table: **without it the derivation returned 19 pairs, 12 meaningless.** Measured today: 2 models, 7 pairs — and those floors live in the pure module, because a derivation that finds nothing looks exactly like a repository with no models.
📐 **`jscpd` WAS THE OBVIOUS TOOL AND THE MEASUREMENT REFUSED IT.** With the 2026-08-23 defect restored in BOTH files, jscpd returns **byte-identical clone counts** to the healthy repository at every setting from 5 lines/50 tokens down to 1 line/10 tokens — **zero sensitivity**, because its window is a LINE window and the defect is a 13-token EXPRESSION on one line. It also emits **no file name** in any reporter (`name: ""` in the JSON), so a per-pair verdict could not even name the pair. The signal is the shared TOKEN RUN, and nothing in the toolbox measured it.
📐 **THRESHOLD = 12 TOKENS, CHOSEN BY MEASUREMENT, AND THE WIDER READINGS ARE REFUSED WITH THEIR NUMBERS.** The defect run is 13 tokens, so any window above 13 is blind to it by construction. NOISE FLOOR on 10 CONTROL pairs of engine files that share a subject but judge nothing: 10 tokens → 4 runs · **12 tokens → 3 runs** (harness boilerplate, longest 16) · 14 → 3 · 16 → 2 · 18 → 0. 18 is silent on the control but loses the defect run **and 15 of the 34 twins measured today**; 24 keeps only 4 of them. 🛑 Raising it is how this gate gets disarmed without a single red — hence the number is asserted HARDCODED in the suite, never read back from the file it guards.
⚠️ **THE RATCHET IS AN EQUALITY ON THE RUNS THEMSELVES, NEVER ON A COUNT** — a count is blind to the 2026-08-23 defect, which REPLACED one shared statement by another without changing how many there were. Undeclared run = a NEW twin. **Declared run no longer observed = a DORMANT PERMIT and it REDDENS**, so an exemption that stops being necessary cannot rot. A pair absent from `pairs` is held at ZERO.
🔴 **THE BASELINE IS THE HONEST STATE, AND IT IS NOT SMALL: 34 shared runs over 3 of the 7 pairs, up to 78 tokens.** The 2026-08-23 repair fixed the SEMANTICS on both sides and left the TWINNING fully intact. Named latent twins, each of the same class: `language-spec.js` ↔ `sources/file.js` copies the patch-tool extraction **verbatim, 78 tokens**, and the `keyValues` recursion **42 tokens** — the model cannot contradict the engine on either. `cadence-spec.js` ↔ `gate.js` still shares `next[doc] = { seen: true, sinceLastCall: 0, turn: turnCount }` (23 tokens): **the delivered-branch sibling of the defect, word for word.** `cadence-spec.js` ↔ `frontmatter.js` re-states the vocabulary constants (21 tokens) instead of deriving them.
⚠️ **`INHERITED_TWIN` IS THE MEASUREMENT OF THE DAY, NOT A JUSTIFICATION** — exempt from `why` ON PURPOSE: demanding one per run would produce INVENTED sentences, and a false justification makes a case look settled. `CONTRACT` (a signature or vocabulary the differential REQUIRES) owes a `why` over 60 characters. EXIT CONDITION of the file: zero `INHERITED_TWIN`, reached by REWRITING the model's statement in its own words — quantifiers, a partition, a table — never by deleting a model and never by relaxing the threshold.
🔴 **ANTI-VACUITY, four layers**: the derivation FLOOR (in the pure module, hence mutated) · a WITNESS pair fabricated in the OS tmpdir carrying the real 2026-08-23 statement, whose detection is REQUIRED · a DECOY pair sharing the SUBJECT and not the code, which must yield NOTHING (a detector that returned everything would pass the witness) · a PROSE pair carrying the same long comment, which must yield nothing too, because this repository is mostly prose. Witnesses live in the tmpdir: a fabricated file in `src/`/`test/` makes PARALLEL gates red at random.
⚠️ **SEEN RED, on the real defect restored in BOTH files**: 3 faults — the new 13-token twin at `cadence-spec.js:225` ↔ `gate.js:297`, a 14-token neighbour, and the DORMANT PERMIT left by the run it replaced. Restored afterwards and verified by `git hash-object`. Sabotage IN MEMORY for every other cell.
