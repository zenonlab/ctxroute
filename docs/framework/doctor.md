---
match: [doctor.js, doctor.test.js]
scope: [ctxroute]
mode: smart
rank: 356
---
# doctor.js / doctor.test.js — dead-man switch

⚠️ REASON TO EXIST: a dead PreToolUse hook is INDISTINGUISHABLE from an absent hook (no error, no log — just no more injected docs). Already experienced 2× on 2026-07-15. Wired on SessionStart (`--quiet --settings ...`): silent if alive, screams if dead.
NEVER make it fail-open like the hooks: a hook MUST be silent/non-blocking, a DIAGNOSTIC MUST scream (exit≠0 + stderr). Opposite roles, never merged.
⚠️ Every probe proves a REAL EFFECT (context injected, store incremented, stores CLEARED), NEVER a plain exit 0 — hole closed 2026-07-19: the reset was checked as "exits without crashing"; a mute exit-0 stub was indistinguishable from a live reset (negative-check 3f proves it). New probe = set the 3 stores → reset → require their ABSENCE.
NEVER delete the NEGATIVE cases of `doctor.test.js` (real sabotage on a tmpdir COPY): a dead-man switch never triggered = false confidence. Green on a healthy repo proves NOTHING.
`--quiet` reduces ONLY the success output — a failure always screams, quiet or not.
The probe isolates itself via `CTXROUTE_CONFIG_PATH`/`DOCS_DIR`/`STATE_DIR` (tmpdir): it must NEVER touch the shipped files (case 6 checks this).
The `--settings` check is the ONLY coverage of the wiring: it lives outside the repo, no repo test can see it.
⚠️ **BANDWIDTH — 4 CHECKS IN `--settings` (2026-08-07)**: ① all declarations announce the SAME `--frames` · ② as many declarations as announced frames · ③ `--frame` indices covering **1..N with no gap and no duplicate** (a missing index = a frame never emitted; a duplicate = content delivered twice — **both SILENT**) · ④ **equality with `frames` from `ctxroute-config.json`**. 🔴 ④ exists because the same number lives in TWO places (config = intent, settings = what the harness executes) and the engine cannot impose the second: that is the silent divergence paid on 08-05 (skill delivered in 11 actions instead of 1, 995 green tests). Key absent from the config ⇒ no reproach (we do not force anyone to declare their bandwidth). 🛑 These checks REPLACED two checks from 08-06 that required a SINGLE declaration and refused `--frames N>1` — **false premise, cancelled** (cf. `single-frame.md`): do not resurrect them.
⚠️ CODEX (2026-07-19): probes 7-8 = shells codex-doc-inject/codex-doc-write-guard (same real-effect proofs); `--codex-hooks <path>` = Codex wiring (**6 channels** — this line said 5, fixed 2026-08-09, the canary is the 6th since 08-07 — + files = THIS repo + anti-DOUBLE injection: protect-files.js must NO LONGER be wired there). Negative-checks 3g/3h/7 in doctor.test.js — never deleted.
⚠️ **CODEX CONTEXT CEILING (2026-08-04)**: both EMITTERS (`codex-doc-inject`, `session-inject`) MUST declare `additionalContextLimit = 0` in the wiring — omitted = default 2500 TOKENS, disk spill + preview SILENTLY (large skills never arrived whole). Checked PER BLOCK (a global match would leave the other emitter mute) and with OPTIONAL-quote patterns (TOML `=` AND JSON `":`) — without that the check is inert in JSON. Require it ONLY of the emitters: imposing it on the reset/guard/counter would be an inert declaration. Negative-check 7d (4 parts, including "only one emitter configured").
⚠️ **PROBE 9 = THE CANARY (2026-08-05)**: real spawn on a FABRICATED transcript, verdict read in state/canary.json. **BOTH verdicts are required** (dead without injection, alive as soon as there is ONE injection) — a single case would let through a canary frozen on a constant, exactly the trap of inert purity gates. Negative-checks 3i (mute) / 3j (FROZEN on alive) / 5f (not wired) — never deleted.
⚠️ **THE PROBE MUST SET THE DENOMINATOR (2026-08-07)**: it writes `remainder-doctor-probe.json` with `emissions: 30` BEFORE launching the canary. The `"type":"tool_use"` lines of the fabricated transcript are now only NOISE — nothing is counted in the transcript schema any more (Codex doc: *"isn't a stable interface"*). Without that file, the canary answers `indecidable` and the probe proves NOTHING.
⚠️ **6th CODEX CHANNEL = THE CANARY (2026-08-07, project ② closed)**: `canary-check.js` is required in `--codex-hooks` — the SAME file as on the Claude side, with no shell (`transcript_path`/`session_id` documented on both sides). 🛑 Without it, Codex is blind to its own death, and the framework loses its ONLY practicable anti-deprecation gate (the 3 other leads measured and closed on 08-05). 🛑 Do NOT demand `additionalContextLimit` of it: it emits no context, the declaration would be inert. Negative-check **7f** (exit≠0 + the canary NAMED) — never deleted.
⚠️ **CODEX 0.146.0 — OFFICIAL DOC RE-READ 2026-08-05** (learn.chatgpt.com/docs/hooks + /config-file/config-basic). ① The flag is **`[features].hooks`** (Stable, "Enable lifecycle hooks from hooks.json or inline [hooks]") — **`codex_hooks` NO LONGER EXISTS** in the official list: renamed in config.toml on 08-05. ② **CONTRACT UNCHANGED**: `hookSpecificOutput` + `additionalContext` + `permissionDecision: deny` work as before — our `enforce`/deny stays valid. **`ask` IS NOT in the doc** (only `deny` and `allow`, the latter with `updatedInput` which REWRITES the call): removing `ask` from the framework is therefore aligned with Codex, not only with 0-human. ③ `additionalContextLimit`: **tokens, default 2500, `0` = full** — UNCHANGED, and finally ACTIVE (0 → 18 occurrences in the binary between 0.144.6 and 0.146.0). ④ **11 events** including **PostCompact · SubagentStart/Stop · PermissionRequest · SessionEnd · Stop** = channels NOT used by the framework (PostCompact notably: on the Claude side we only have PreCompact). ⑤ ⚠️ **GAP NOT TO BE BLURRED**: the doc cites `hooks.json` as a valid source, whereas our 0.144 field measurement found it IGNORED — re-measure before relying on it. ⑥ 🛑 **The "code mode" IS NOT DOCUMENTED** (an `exec` tool receiving JavaScript `tools.shell_command({...})`, observed in a real 0.146 run): no documented flag describes it (`unified_exec` = "unified PTY-backed exec tool", default true EXCEPT Windows; `shell_tool` = "default shell tool", true). Undocumented ⇒ **MEASURE** (capture the real payload), never reverse-engineer blindly. Cf. REFACTOR-PLAN ⑨.
⚠️ **CODEX FEATURE FLAG — `--codex-config <config.toml>` (2026-08-05)**: opt-in SEPARATE from `--codex-hooks`, because the flag lives in a DIFFERENT file from the managed wiring. Requires `hooks = true` PRESENT **AND** `codex_hooks` (deprecated in 0.146) ABSENT — both, never just one. Anchored at line start: a COMMENT is allowed to name `codex_hooks` (same lesson as the protect-files false positive). Without this check, the best wiring in the world is dead silently. Negative-check 7e (5 parts).
⚠️ Codex 0.144 FIELD DATA (measured 2026-07-19): `~/.codex/hooks.json` IGNORED. Framework wiring = `C:\ProgramData\OpenAI\Codex\requirements.toml` (MANAGED hooks = trusted by policy, ZERO /hooks — proven without bypass); point --codex-hooks at it. Personal hooks (rush-banner/sync-mcps) stay in config.toml — NEVER re-wire the framework there (double injection). Real payload: tool_name="Bash" even for shell_command.
🔴 **THE WIRING CHECK READS BOTH TRANSPORTS — AND READING ONLY ONE MADE IT JUDGE NOTHING (2026-08-21).** Frame coordinates live in `--frame k --frames N` on the `command` lane and in `?frame=k&frames=N` on the `http` one; the collector matched `"command"` ALONE, so the day the http lane was wired the doctor counted **ZERO frames** and accused `ctxroute-config.json` of a divergence that did not exist. It failed LOUDLY, the acceptable direction — but a judge that knows one transport stops judging the day the other is used, and this is the ONLY coverage the wiring has (it lives outside the repo). ⇒ ONE reader for both dialects (`coord`), a gate declaration recognised by the COORDINATES it carries and never by a file name (an http declaration has none), anchored on `frames=` and never on a host or a port, which belong to the operator. Sealed by cases 7/7b, seen RED by restoring the blindness.
