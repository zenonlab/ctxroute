---
rules: [{"pattern":"scale-bench.test.js","scope":["ctxroute"]}]
mode: smart
threshold: 30
---
# scale-bench.test.js: the SLOPE at the real sizing, never a millisecond
🛑 **ASSERT ON RATIOS ONLY.** A duration belongs to this machine; how the cost GROWS when the load multiplies belongs to the algorithm. Never add a threshold in ms, in bytes or in requests per second.
🔴 **THREE AXES, AND THEY ANSWER DIFFERENT QUESTIONS — never quote one for another.** **A = HELD STATE** (cells ①②, scopes 384→3072): does holding more agents' memory cost more per request? **B = CONNECTED AT ONCE** (cells ③④, 64→1024 established clients, held state FROZEN at 128 scopes): does serving ONE agent cost more because N others exist? **D = CONTENTION** (cells ⑤⑥, 4→32 writers on ONE lock, 2026-08-23): what does one critical section cost when the 32 frames of a single action dispute the same lock? **A map with 1024 entries is neither 1024 concurrent clients nor 32 writers inside one critical section.** ⚠️ `EVICT_LEVELS`/`BAR_EVICT` (axis C, eviction) are DECLARED in the file with NO cells behind them: constants, never a measurement — do not quote them.
🔴 **MARGIN A = 1.67, DERIVED FROM THE MEASURED PAIR** HEALTHY 0.89 · SABOTAGED 3.13 ⇒ √(0.89×3.13). It was 3.0 and the sabotaged store cleared it by 4 % — the gate CERTIFIED a defect. **RE-DERIVED 2026-08-25 on the redefined quantity and it came back to the SAME number**: healthy 0.83/0.91 · sabotaged 3.10/3.30 ⇒ √(0.91×3.10) = 1.68. Nothing was tuned to fit.
🔴 **A'S RANGE WAS WIDENED TO 384…3072 ON 2026-08-23 BECAUSE THE LOCK BURIED THE SIGNAL, and that is "shrink the constant", never "move the bar".** `/turn` and `/emit` now take the real cross-process lock: **421 µs per request MEASURED** (mkdir+rmdir 326 µs, parent `mkdir -p` 123 µs) where the work used to be ~3 µs, so at 128…1024 the sabotage read **1.18x against 1.67** and cell ② reddened *"certifying, not measuring"* — correctly. Fit `C + a·N`: a ≈ 165 ns/scope, i.e. the constant is worth ~2,550 scopes. Widened: **1.87 / 1.99 measured, healthy 1.06 / 1.13.** 🛑 The top is DERIVED as ¾ of `MAX_DURABLE`; the last quarter is for the `doc-seen-` scopes the `/pretool` track creates, and a cell REFUSES a run where the LRU ran (that would be eviction, not held state). ⚠️ `SOUS_LOTS` 5→9 in the same gesture: at five the same sabotage read 1.97/1.87/**1.71** — a verdict landing within 2 % of the line is a cell that will flip, then be disarmed.
🔴 **AND WIDENING WAS NOT ENOUGH EITHER — THE LOCK IS 99.9 % OF A REQUEST, MEASURED 2026-08-25.** On a quiet machine cell ① was GREEN and cell ② FAILED at **1.15x**: "certifying, not measuring". Three quantities isolated on the same store and the same address settle it: one whole `handle('/turn')` **1,267,266 ns** · `withLock` on an EMPTY critical section **1,413,648 ns** · `turnCore.bump` alone **1,254 ns**. The work whose cost CAN depend on the held state is **0.1 %** of what was timed, so a defect had to multiply it by ~1,000 before the ratio moved. Widening again is arithmetically closed: C ≈ 1.35 ms against a ≈ 156 ns/scope ⇒ the constant is worth **~8,600 scopes**, and clearing 1.67 would need a top level of ~5,500 — above the 4096 ceiling, where the LRU runs and the axis stops measuring held state.
✅ **SO THE LOCK IS PRICED OUTSIDE THE TIMED REGION, EXACTLY AS THE SOCKET ALREADY IS (2026-08-25), ON BOTH A AND B.** The verdict is the nanoseconds spent INSIDE the store; the clock starts inside the critical section. 🛑 **The real path is unchanged** — every timed call still goes through `hs.handle`, still takes the REAL lock at the REAL address, still writes through the real store. The lock is not removed, stubbed or shortened, and it is measured SEPARATELY by axis D, which exists for nothing else. ⚠️ **The wrapper is OUTERMOST** (the sabotage's work is INSIDE the timed region — the other way round would time the healthy store and let the defect run for free) and installed on **BOTH modes** (an instrument on one side only compares two different measurements). 🛑 **DECLARED BLIND SPOT**: a cost growing with the held state WITHOUT touching the store is invisible — bounded by `only-store-resolve-opens-a-store`, and `scopes()`/`size()` are wrapped too, so a handler walking the state through them IS seen. 📐 **RESULT, same run**: axis A healthy **0.83** vs sabotaged **3.30** (bar 1.67) · axis B healthy **0.70** vs sabotaged **4.11** (bar 2.83). 🛑 The bars were NOT moved and neither sabotage was touched.
🔴 **AXIS B'S OLD ARITHMETIC, KEPT BECAUSE IT IS WHY THE FIX HAD TO BE THE CONSTANT.** Its sabotage walks the connection set for **~68 ns per client**; against the lock constant cell ④ read **1.24-1.28 against its 2.83 bar** and stayed RED. Solving `(C+12aL)/(C+1.5aL) ≥ 2.83` gives a top level of **≈ 23,000 established sockets** — past the ephemeral port range, and far past this bench's declared load bound (~2048 fds). ⇒ **no legitimate range restores it**. 🛑 Do NOT lower the bar, and never read such a red as noise: it is the true statement that the criterion has stopped discriminating.
📐 **BAR B = √(load factor) = √8 = 2.83, `Math.sqrt` OF THE LEVELS, never a literal** — geometric mean of model O(1) (ratio 1) and model linear-in-fleet (ratio 8 = 768/96). Move a level, the bar moves. **~1.0 = healthy · ≥ 8 = linear in the fleet · ≥ 2.83 reddens.**
📐 **MEASURED 2026-08-21, busy workstation, at the FIRST range 64…512 (K=4, bar 2.00): healthy 0.81 / 1.29 · sabotaged 2.95 / 2.55 · jitter 1.71 / 3.35.** Cell ④ reddened as it must, but 2.55 vs 2.00 is only 27 % of clearance. Fitting `C + a·N` on those two runs gives a fixed cost worth **52–90 connections' worth of walking** — and a fixed cost is diluted by a LARGER N, never by a different threshold. So the RANGE was widened (predicted 4.6–5.6 vs 2.83 — a prediction, not a reading). 🛑 The bar was NOT lowered and the sabotage was NOT made louder: tuning a strawman until it clears gives a gate that only catches the defect it was shaped for.
📐 **BAR D = √(K_D) = √4 = 2.00, `Math.sqrt` OF THE LEVELS** — geometric mean of model ① (a mutual exclusion whose acquisition work ignores how many others want it ⇒ ratio 1) and model ② (a section proportional to the disputants — every home-made FIFO lock ⇒ ratio K_D). MEASURED 2026-08-23: **healthy 1.44-1.66 · sabotaged 3.06-3.19.** Top level = 32 because 32 is the live wiring's frame count, never a round number.
🛑 **AXIS D GATES ON WALL TIME PER COMPLETED SECTION, AND CPU IS PRINTED BESIDE IT, ASSERTED ON BY NOTHING.** CPU looks like the purer quantity and is a TRAP: `lock.js` waits by POLLING, so the machine does ~W wake-ups per handover BY CONSTRUCTION and a perfectly correct lock is already linear in W in CPU — the healthy side read 1.28 then 2.05 against the same bar depending only on how long the phase ran. **A metric a healthy implementation cannot pass is a coin toss, not a criterion.** The CPU column PRICES that polling (~3 wasted syscalls per section at 32 disputants); it cannot judge it.
🛑 **AXIS D'S DISPUTANTS ARE REAL OS THREADS, and the substitution is axis B's, one floor down**: `mkdirSync` is atomic for the KERNEL and a synchronous fs call runs on its calling thread, so the lock cannot tell a thread from a process; 32 node processes would measure the SCHEDULER. NOT exercised by it: distinct address spaces, and a holder that DIES mid-section (`STALE_MS`, measured nowhere). Sections are shared out so every level runs the SAME count (1024), the barrier is a worker MESSAGE, and every disputant calibrates its own uncontended cost on its OWN private address.
🔴 **AND AXIS D FOUND A REAL DEFECT ON ITS FIRST RUN — `withLock` FAILS OPEN WITHOUT EVER TIMING OUT.** 32 threads on one address: **12 EPERM out of 768 acquisitions (1.6 %)** with `fs.mkdirSync` patched to record the kernel's own codes; on Windows a directory whose deletion is still pending answers EPERM, and `lock.js` treats every code but `EEXIST` as unrecoverable — it `break`s, the section never runs, the caller gets the fallback. **In production each one is a state update silently not made**, at the exact fan-out of one action. Cell ⑤ is RED on it, by name. 🛑 The fix belongs to `lock.js` (retry the transient codes); never raise the timeout, never relax the cell.
🛑 **THE NOISE FIGURE THAT GATES IS THE INTER-QUARTILE RATIO, NEVER max/min.** `nu` = the spread of the CENTRAL half (the noise of the median, the statistic actually used) and it gates; `spread` = max/min, reported and gating NOTHING — it measures the outliers the median exists to discard.
🔴 **AXIS B'S SUB-BATCH IS A FIXED 128 CALLS AGAIN SINCE 2026-08-25, AND THE PREMISE IS WHAT CHANGED.** Sizing in TIME (≥ 20 ms, calibrated by a 3-pass pilot) was right while the reading was the WALL time of a batch: at 160 calls a batch lasted under 1 ms against a ~15.6 ms quantum, so one preemption WAS the reading and the refusal flipped 1.71 → 3.35 between identical runs. With the lock priced out, the timed window is **~4 µs inside a ~1.3 ms call (0.3 %)**, and the untimed 99.7 % is where the `mkdir`/`rmdir` syscalls yield — a scheduler event now lands OUTSIDE it almost always. A longer batch buys no protection and only wall cost. 📐 **AND IT HAD STOPPED ANSWERING: `LOT_MIN = 200` forced 270 ms per sub-batch (13× its own floor) plus 1,500 pilot calls per level ⇒ ~26 s before a socket was opened, and BOTH axis B cells hit the 30 s lane timeout.** Now ~10.5 s per cell. What protects the reading: the tiny timed fraction · the MEDIAN of nine · `nu` refusing by name. **Warm-up 2,000 → 512** for the same reason and no other (four passes over the frozen pool; a runtime is hot after a few hundred calls).
🛑 **THE ANTI-VACUITY MOVED WITH THE QUANTITY: "longer than a scheduler quantum" was right for a WALL reading and is meaningless for this one.** The new way to measure nothing is a reading of the same order as `hrtime.bigint` itself. So each driver MEASURES its own instrument (`clockNs`, two brackets) and every cell requires the smallest reading to stand `PLANCHER_RESOLUTION` (10) times above it — a MULTIPLE of a measured quantity, never a typed nanosecond figure. Observed: readings ~3,200-95,000 ns against a bracket of ~130-195 ns.
🛑 **THE REFUSAL NAMES A SATURATED MACHINE, not background activity.** `nu ≥ BAR` ⇒ loud "THIS RUN CANNOT DECIDE", never a quiet green, and never a widened bar. MEASURED 2026-08-25: `nu` = 1.09 and 1.35 against a 2.83 bar.
🔴 **CONNECTIONS OPEN IN WAVES OF 64 — a KERNEL fact, measured:** one burst overruns the LISTEN BACKLOG and Windows REFUSES what it cannot queue (64 → 0 refused · 128 → 0 · 256 → 152 · 512 → all 512). Not a bigger backlog (one kernel's number, we ship on three) and not a timer: each wave is drained by the server's own `connection` event. A bench that dies there measures the accept queue and gets read as if it had measured our cost.
🛑 **THE REAL PATH IS EXERCISED ON EVERY TIMED CALL — `handle()`, the real lock, the real store — AND THE CONSTANTS ARE PRICED OUTSIDE THE CLOCK**: the socket (0.17 ms, measured on the HTTP track) and, since 2026-08-25, the cross-process lock (~1.3 ms, measured by axis D). Both are flat in the quantity each axis moves, and a constant cannot change a slope — it can only bury one. The WALL figure per request is PRINTED beside every reading, asserted on by NOTHING, so the price of the lock stays visible on a GREEN run. Axis B's in-flight round trips are the REALITY anchor: PRINTED too, and judged by nothing — one point has nothing to compare with.
🛑 **ANTI-VACUITY IS LOAD-BEARING, and axis B has two ways to measure nothing:** never open the sockets, or open them client-side while the server never accepts. Hence the SERVER's own `connection` count per level, plus `tenues` and `portees === 128` both read BEFORE the round trips (the anchor opens its own sockets and creates a `doc-seen-` scope: reading after reported 544 vs 512 and accused sockets of dying when none had).
🛑 **EVERY CELL PRINTS ITS READINGS, RATIOS, JITTER AND BATCH SIZES BEFORE ASSERTING**, success included. A gate that only speaks when it fails turns its own threshold into folklore.
⚠️ **`test.sequential` ON ALL SIX — LOAD-BEARING.** The `integration` lane is `sequence.concurrent`, so by default the four drivers would time themselves while competing: that corrupts the RATIO, not just the noise. **Load imposed: ONE node process at a time, ~2048 loopback fds, tens of MB** — axis B's clients are SOCKETS, never processes.
⚠️ **NO TIMER, EVER**: readiness = the socket's own `connect`/`listening` event and the server's `connection` handler resolving a promise; axis D's barrier = a worker MESSAGE (every thread says it is ready, blocks on `once('message')`, the main thread — the only party that KNOWS — says go); completion = the child's EXIT; the driver is killed in a `finally` on every path and every worker is `terminate()`d at every level. **HEAVY LANE by content**: no per-test timeout, the global 30 s applies, and it runs on every push.

🔴 **CELLS E/F/G ADDED 2026-08-30 — AXES ①-⑥ TIME `handle()` IN MEMORY AND COULD NOT SEE THE 2026-08-28 CONNECTION-LOSS DEFECT** (`frame-sequencer-pure.js`/`frame-sequencer.md`: ~6% of one action's frame connections lost in silence on Windows loopback, `SIO_TCP_INITIAL_RTO`). A lost CONNECTION is an ABSENT delivery, never a slow one, and a bench that only measures TIME cannot render an absent value. **E** drives the REAL `http` daemon over REAL TCP with `K` of 32 declared connections actually opened (K decreasing 32→6, the harshest legal shape: the K survivors are the HIGHEST-numbered `?frame=`, i.e. exactly the ones the OLD per-URL attribution would have served NOTHING) and asserts every chunk 1..m arrives exactly once as long as `K >= m` — negative-check reverts `frame-sequencer-pure.nextIndex` to `requestedFrame` IN MEMORY and the SAME pattern must name missing chunks. **F** is E/G's anti-vacuity: counts REAL kernel `connection` events and requires `accepted === requests emitted`, never zero. **G** reproduces the PRODUCTION SHAPE: 20 bursts of 32 BRAND-NEW concurrent connections (never sockets held open across gestures, unlike axis B), asserting a ZERO delivery-loss rate — this is a LIVE regression guard, not a hypothetical one.
🛑 **RUN IN-PROCESS, NOT VIA A SPAWNED DRIVER** (unlike ①-⑥): nothing here is timed, only DELIVERED, so a separate process buys no isolation benefit. Uses `hs.createServer({})` directly and `createRequire` to bridge CJS `src/` modules into this ESM test file — same pattern as `test/http-frame-resequencing.test.js`, whose OWN header states it drives `handle()` directly and explicitly leaves the REAL-SOCKET proof to this file.
🔴 **`agent: false` IS MANDATORY ON EVERY REQUEST, MEASURED**: Node's default global agent pooled ONE socket across 24 sequential calls to the same host:port even at its own `keepAlive: false` default — without it, F silently measured an in-memory-shaped call it believed was 24 real connections.
🔴 **A BOUNDED, IMMEDIATE RETRY ON THE CLIENT IS REQUIRED, MEASURED THE SAME DAY**: with `agent: false` this bench hit REAL `ETIMEDOUT` on `connect()` — the very Windows-loopback pathology under test — and an unhandled one CRASHED THE WHOLE WORKER. `PRETOOL_REQUEST_RETRIES` (25, measured on a host already running ~62 `node.exe` processes; 5 was not enough for G's 32-wide burst) retries ONLY `ETIMEDOUT`/`ECONNREFUSED`/`ECONNRESET`, immediately, no sleep — a KERNEL FACT ("this attempt did not reach the daemon"), never a guessed delay. 🛑 This retries the HARNESS's own OS-level connection noise, never anything the daemon does — the (32-K) connections a cell deliberately never opens remain the ONLY simulated loss the assertions are about.
⚠️ **CELL H (SAME PUSH) — A WITNESS BEFORE SCALE-A/B/D, NOT INSIDE E/F/G**: three full local runs of this suite produced DIFFERENT red sub-sets each time, some crossing a bar purely from machine load. `witness()` runs `WITNESS_SAMPLES` (9) pure-CPU samples (`witnessSample`, no I/O, no store, no lock) and computes their inter-quartile ratio; `iqr >= WITNESS_NOISE_BAR` (1.5, a NAMED constant independent of MARGE/BAR_CONC/BAR_CONT on purpose) ⇒ `ctx.skip('UNMEASURED: …')`, never a red, never a silent green. The seam for its own SEEN RED (`witnessInternals.sample`, sabotaged in memory) proves BOTH directions: a stable witness lets a real defect redden, an engineered-unstable one forces `loaded === true` regardless of the real machine.
🔴 **THE GUARD WAS HALF-WIRED FOR FOUR DAYS, MEASURED 2026-08-30: `witness()` GATED THE HEALTHY CELLS ONLY, NOT THEIR OWN SEEN RED SABOTAGE.** `SEEN RED (axis A/B/D)` ran unconditionally — no witness call at all — so a negative-check's OWN assertion (`rTurn`/`rMur` against its bar) could flip on machine noise alone, with nothing rendering `UNMEASURED`. LIVED: a full-suite run reddened `SEEN RED (axis D)` at 1.94x against its 2.00x bar while the healthy cell above it would have skipped under the exact same load — a guard covering one third of the cells it names is INERT where it counts. ✅ **CLOSED THE SAME DAY, ALL THREE AXES**: `SEEN RED (axis A)`, `SEEN RED (axis B)` and `SEEN RED (axis D)` each now call `witness()` FIRST, identically to their healthy sibling, and skip on the SAME bar. PROVEN per axis, both directions, stable witness first: axis A sabotage 3.32x (margin 1.67) · axis B sabotage 3.48–4.05x (bar 2.83) · axis D sabotage 2.05x (bar 2.00, the closest — exactly the cell noise had already flipped once). Forced-unstable witness (`witnessInternals.sample` sabotaged in memory, reverted): all three render `UNMEASURED`, never green, never red. 🛑 No bar moved, no threshold widened — the fix is ONLY the missing witness call, per the doctrine above: a guard not seen failing on the cell it is meant to protect is a guard ASSUMED to work.
