---
rules: [{"pattern":"lock.js","scope":["ctxroute"],"exclude":["package-lock.json"]},{"pattern":"lock.test.js","scope":["ctxroute"]}]
mode: dumb
rank: 366
---
# lock.js — invariants

⚠️ REAL BUG ALREADY LIVED THROUGH (15/07/2026): `fs.mkdirSync(lockDir)` WITHOUT `{recursive:true}` on the PARENT folder fails with `ENOENT` (not `EEXIST`) on a fresh checkout where `state/` does not exist yet — invisible locally (folder already created by previous runs), broke in CI. The fix creates the parent chain upfront (`recursive:true`, idempotent, concurrency-safe) BEFORE the atomic acquisition attempt.
NEVER delete the `lock.test.js` "fresh checkout" test (folder `TMP_ROOT` never created before the test) — it is the only test that would have caught this bug.
`withLock` is FAIL-OPEN on timeout (`fallback`) — NEVER block the hook indefinitely over lock contention.
`CTXROUTE_LOCK_TIMEOUT_MS` = env RESERVED FOR TESTS (19/07/2026, paths.js doctrine): concurrency tests raise the timeout to prove ATOMICITY independently of load (2 s legitimately expiring under load = intended fail-open, not a bug). NEVER a user setting — prod = 2000 ms.
✅ **`STALE_MS` IS GONE — THE KERNEL ANSWERS NOW (2026-08-23). This line said "an INFERENCE ABOUT DEATH, NOT PATCHED" since 2026-08-21.** "Older than 5 s ⇒ the holder is dead" was a guess, and forcing a lock on it let a SECOND writer in while the first was merely DESCHEDULED — a lost update, silently, exactly when the machine is saturated. TLC exhibits it (`StateStaleForcing`, required RED for ever: the lock is TAKEN and a record is lost anyway). ⇒ The holder now RECORDS ITS PID in the lock directory and a contender ASKS THE OPERATING SYSTEM: `process.kill(pid, 0)` sends nothing — official Node v22 doc, read 2026-08-23: *"a platform independent way to test for the existence of a process"*. **`ESRCH` is a PROOF of death ⇒ forced INSTANTLY** (a crashed holder used to poison the lock for 5 s, during which every 2 s contender fell through fail-open and SKIPPED its write); **a pid that EXISTS ⇒ never forced**, whatever the mtime says. Proved by `StateKernelForcing` (green: TypeOK · NoLostRecord · AtMostOneInCrit) with `StateForcingFires` as its anti-vacuity — forcing must REALLY fire there, or the green would only mean "we never forced". Same rule as `kernel-bind.js`: ask what KNOWS.
🛑 **TWO RESIDUALS, DECLARED — a fix that hid them would be worth less than the defect.** ① **PID REUSE**: the same Node doc warns *"the signal may be delivered to some other process with the same PID"*, so a recycled pid answers "alive" for a dead holder and that lock is never forced — contenders fail open until the unrelated process exits. **No cross-OS pure-Node closure exists**: making a pid unique in time needs a process START TIME, and `os` has none (no boot id, no process enumeration — v22 API index, read 2026-08-23); `/proc/<pid>/stat` is Linux-only, and a native module is refused (public repo, installs anywhere). ② **THE TWO-SYSCALL WINDOW**: `mkdirSync` is the atomic step, so the pid lands on the NEXT syscall; a death in between leaves a lock nobody can identify. That case ALONE keeps a clock, renamed `UNIDENTIFIED_HOLDER_MS` so its scope cannot creep back, and a holder that CANNOT record itself RELEASES rather than hold — without which "no record" would stop meaning "died in the window".
📐 **AND THE INFERENCE IS NO LONGER INVISIBLE**: `rules/temporal-call.yml` only ever detected CALLS (`setTimeout`, `Atomics.wait`), never `if (Date.now() - x > y)` — so the one clock that judged a DEATH escaped the budget entirely. Clock-comparison atoms were added on 2026-08-23 with their witnesses; `src/lock.js` now declares **2** occurrences (the back-off and the residual ②), and the ratchet being an EQUALITY is what proves the new atom really bites. A second judge (`state-spec-gate`, CODE ANCHOR) holds `lock.js` to EXACTLY ONE clock comparison and requires it to be the NAMED one.
