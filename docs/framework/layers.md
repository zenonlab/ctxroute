---
rules: [{"pattern":"layers.json","scope":["ctxroute"]},{"pattern":"layers-gate.test.js","scope":["ctxroute"]}]
mode: smart
threshold: 30
---
# layers.json — THE CAPABILITIES × LAYERS TABLE (2026-08-06)
🛑 **WIDENING THIS TABLE IS ALMOST ALWAYS THE WRONG ANSWER.** A red gate says the FILE is in the wrong layer, or does work that does not belong to it. Adding a capability to silence the red disarms the guardrail **silently**, and nobody will ever see it — the repo is written by agents and reviewed by nobody. Fix the file, not the table.
⚠️ **WHY IT EXISTS**: 3 architecture defects in 3 days (transport in a single emitter · `process.exit` in 2 cores · `console.log` in guard-core) = **3 CELLS of this table**, not 3 discoveries. What a program can do is a **FINITE** list — same logic as the boolean OR/AND/NOT base of matching: a closed base, not an open list.
⚠️ **ANTI-INERT = THE GUARANTEE THAT HOLDS EVERYTHING ELSE.** The worst defect is not a red gate, it is a gate that is **GREEN AND SEES NOTHING**. Every capability therefore carries a `temoin` (a REAL line of code) written into the OS tmpdir, scanned, whose detection is REQUIRED. Derived from the manifest ⇒ a capability added tomorrow is covered the day it is written. 🛑 Capability without a witness = REFUSED. The witness must be the REAL form encountered, never a simplified textbook case.
⚠️ **THREE BLIND SPOTS MEASURED ON THE SAME DAY** — each time the rule "existed" and protected NOTHING: ① `shell: true` made a scan EMPTY under `/bin/sh` (red CI); ② the pattern `{ shell: true }` missed `{ encoding, shell: true, maxBuffer }`, the very form that caused ①; ③ **`ast-grep` RESPECTS `.gitignore`**, so witnesses written into `state/` were invisible. 🛑 If someone one day gitignores a SOURCE folder, this gate would go blind WITHOUT SAYING ANYTHING — only the "existence" part would catch it, never remove it.
⚠️ **`shell` FORBIDDEN IN ALL LAYERS, shells included**: it makes the command be interpreted by `cmd` (Windows) or `/bin/sh` (Linux/macOS) — the same code behaves differently depending on the machine. Call the binary DIRECTLY, never through a shell nor through `npx`.
⚠️ **`etat-devine` (2026-08-07)**: an empty literal passed as STATE to `gate.decide` is not "side-effect free", it is the ASSERTION "nothing was ever injected" — a PRODUCTION bug (orphan chunk re-emitted on every lock contention). 🛑 **Narrow scope on MEASUREMENT**: `x || {}` = 29 occurrences outside tests, ALL legitimate (missing parameter); identical syntax, inverse semantics. The distinction ("was the data READABLE?") is about FLOW, not syntax ⇒ **this table does NOT close the whole class**, only the call whose state is the framework's truth. Do NOT widen it to `|| {}`.
⚠️ **An object PROPERTY requires a `regle` (`kind: pair`), never a `pattern`** — otherwise the rule is inert.
⚠️ **PURE CORE = DERIVED from `stryker.conf.json` → `mutate`**, never copied (it already declares "ALL PURE modules"). Adding a pure module to Stryker protects it here automatically.
⚠️ **IMPORTS ARE NOT HERE**: `fs`/`path`/`child_process`/harness modules = `dependency-cruiser`, already in place. NEVER duplicate them (two tools for one invariant = divergence). This gate covers ONLY the GLOBALS. Tools REJECTED (measured): `eslint-plugin-boundaries` and `Sheriff` do MODULE boundaries, so nothing more, at the price of ESLint as a new dependency.
⚠️ **Official `ast-grep` doc**: `files`/`ignores` (Array&lt;Glob&gt;) + `severity` ∈ hint|info|warning|error|off. ⚠️ `ast-grep.github.io` **301-REDIRECTS** to `astgrep.com` — the old URL lingers in every tutorial.
⚠️ **WHAT IT DOES NOT DO**: find a logic bug, a false invariant, a bad product choice. That is the job of tests and mutation. It closes ONE class — the one that escapes everything else.
