---
rules: [{"pattern":"rendezvous-budget.json","scope":["ctxroute"]},{"pattern":"rendezvous-address-gate.test.js","scope":["ctxroute"]},{"pattern":"rendezvous-budget-pure.js","scope":["ctxroute"]},{"pattern":"rendezvous-budget-pure.test.js","scope":["ctxroute"]}]
mode: smart
threshold: 30
---
# rendezvous-budget.json — AN ADDRESS DECLARES ITSELF (2026-08-25)
🛑 **AN ADDRESS IS THE ONE VALUE WHOSE DIVERGENCE SAYS NOTHING.** A host, a port, a URL, a pipe or socket name, a route of our own protocol: two processes must spell it IDENTICALLY, and when they do not there is no exception, no log, no badge — the daemon listens here, the client knocks there, and the injection simply stops. Every such literal is DECLARED here with the rendezvous it names, the value it writes, its origin and its reason.
🔴 **BORN OF THREE DEFECTS OF ONE CLASS FOUND IN A SINGLE DAY, hours apart, each by a HUMAN pulling on a thread**: the rendezvous hashed from the CODE folder (frozen daemon and working-tree clients never met, ~12 h of silence) · the PORT held as a constant in `src/hooks/http-server.js` AND as `transport.port` in `wiring.json` · the HOST, the same defect one field over. The tests written that day are INSTANCE judges: they cover those three and stay GREEN when a FOURTH is written the same way. **That is the hole this closes.**
🔑 **THE CHECK AN INSTANCE JUDGE CANNOT DO IS AGREEMENT.** Two places holding one address is not forbidden — under socket activation the OS INSISTS on holding its own copy (`ListenStream=`, `SockNodeName`/`SockServiceName`) — what is forbidden is holding two DIFFERENT ones. `daemon-http-port` has FOUR sites (the resolution default, the systemd socket, the systemd service environment, the launchd plist) and until this file existed nothing on earth compared them.
🛑 **SEPARATION OF POWERS, AND A DEROGATION STATED RATHER THAN HIDDEN**: the JavaScript atoms live HERE and are RENDERED into an ast-grep rule AT GATE TIME (the shape `disk-writers.json` already uses for its method-anchored family), so detector and policy are one truth. What stops that from being an escape hatch is the WITNESS table — an atom detecting nothing is RED — so a silently weakened atom is caught by the mechanism that renders it.
⚠️ **AST ON THE JAVASCRIPT SIDE, NEVER REGEX**, and it is load-bearing: this repository EXPLAINS its own former port and host constants in prose, and three `service/` files describe the address they declare. A `regex:` inside an atom applies to the TEXT OF A NODE the parser already identified — the opposite of grepping a file. The data side has no parser, so it is NARROWED instead of loosened: comments stripped per format, perimeter limited to DECLARATIVE units. 📐 **MEASURED 2026-08-25: the wide form found 21 lines of which 8 named no address; the narrow form finds 6 with 0 false.**
📐 **THE INVENTORY, MEASURED BEFORE A LINE OF THE GATE WAS WRITTEN (the `doc-drift-gate` precedent — 936 literals counted first, 8 false reds avoided): 25 rendezvous literals over 12 files** — 19 JavaScript across `src/` and `tools/`, 6 in the declarative units and the manifest, 8 named rendezvous, exactly 2 shape collisions declared `not-an-address`. ✅ **RE-MEASURED AFTER THE ROUTES WERE GIVEN ONE OWNER (2026-08-25): 19 literals over 8 files** — 12 JavaScript, 7 data, the SAME 8 rendezvous and the SAME 2 collisions. A shrinking inventory is the only healthy direction here: what disappeared is duplication, never coverage.
✅ **AND THE MEASUREMENT FOUND A FOURTH SITE NOBODY HAD NAMED — the four ROUTES — CLOSED THE SAME DAY.** `/purge`, `/turn` and `/emit` were constants of the daemon AND hand-written strings in three client shells; `/pretool` was written in `src/hooks/state-client.js` AND in `transport.path`. 🛑 A misspelt route does NOT 404 — the daemon serves the GATE route for any path it does not recognise — so a purge would have purged nothing, a turn gone uncounted, a session queue been answered by the wrong route, **in silence**. ✅ One owner now (`src/protocol-routes-pure.js`, zero `fs`/`path`/`process`), imported by the daemon, by the four client shells and by `tools/wiring-generate.js`; `transport.path` left the manifest like the host and the port before it, and an endpoint or route RE-DECLARED there is a NAMED REFUSAL. 🛑 A client may NEVER read a route off `http-server.js`: that would pull a long-lived server's module graph into a hook spawned at every gesture.
⚠️ **THE RATCHET IS AN EQUALITY IN BOTH DIRECTIONS.** Undeclared literal = RED (an address nobody wrote down is an address nobody compared). Declaration matching nothing = RED, because a dormant permit gets re-inherited, reason included, by the next line of that shape. **An address that MOVED is therefore red on both ends, and that is the intended behaviour**: the whole class is one truth quietly acquiring a second home.
⚠️ **ORIGINS ARE A CLOSED LIST** (`resolved` · `kernel` · `protocol` · `supervisor` · `environment` · `not-an-address`) and the gate refuses any other word, exactly as the temporal budget admits only two motives. A word that fits everything justifies nothing.
⚠️ **PERIMETERS ARE DERIVED FROM `git ls-files` AND THEIR EXCLUSIONS ARE MEASURED, NOT FELT.** `test/` is out because a suite binds port 0 and connects to what the kernel handed back — ~40 declarations protecting nothing. The `.sh`/`.ps1` installers are out because they do not declare the address, they ASK the unit for it: reddening the derived form would punish the correct gesture.
⚠️ **ANTI-VACUITY, FOUR FLOORS AND A WITNESS PER ATOM**: two perimeters, two occurrence counts, plus a real line of code each atom must detect. This repository has been bitten three times by a gate green because it analysed zero files, and its worst defect has never been a red gate — it is a GREEN gate that sees nothing.
⚠️ **SEEN RED, BY IN-MEMORY SABOTAGE ONLY** (a fabricated fourth address in a file that does not exist · a misspelt route · a plist declaring 9001 against a default of 8787 · a declaration matching nothing), each with its GREEN control beside it. Never sabotage a real file: a first version of another gate here brought down 38 tests of parallel suites.
✅ **THE VERDICT IS A PURE MODULE SINCE 2026-08-25 — the declared weakness is CLOSED.** It lived in the TEST file, and Stryker does not mutate test code, so an inverted comparison in the judge would have stayed green for ever. `src/rendezvous-budget-pure.js` now carries the four parts (fail-closed · the count as an EQUALITY · the dormant permit · the meaning of a declaration) plus the AGREEMENT check, is imported STATICALLY by the gate, and is proven at **100 % mutation, 0 survivors** by `test/rendezvous-budget-pure.test.js`. 🔴 **AND THE MOVE ITSELF FOUND A DEFECT THE GATE COULD NOT SEE**: the agreement check read the rendezvous table by plain indexing, so a rendezvous named `toString` returned the function inherited from `Object.prototype` — TRUTHY — and the site was accused a SECOND time for writing its value against `undefined`. Both the sites and the rendezvous table are read through `Map`s now, and the class is gone by construction. ⚠️ The origin list stays a PARAMETER: it lives in the manifest beside the sentence justifying each word, and a copy in the module would be the very class this gate refuses.
⚠️ **BLIND CELLS, WITH THEIR TRUE FAILURE DIRECTION — SILENCE, i.e. FAIL-OPEN**: an address COMPOSED at runtime from pieces that do not look like addresses (no static detector closes it) · an address in a file outside both perimeters (the operator's `settings.json`, the Codex `config.toml` — a BOUNDARY covered by `doctor.js --settings`, not a hole) · an installer that stopped asking the unit and started hard-coding · a file still untracked by git.
