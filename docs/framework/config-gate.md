---
rules: [{"pattern":"ctxroute-config.json","scope":["ctxroute"],"rank":359},{"pattern":"config-gate.test.js","scope":["ctxroute"],"rank":360},{"pattern":"legacy-mcp-inject.test.js","scope":["ctxroute"],"rank":361},{"pattern":"ctxroute-config.schema.json","rank":567}]
mode: smart
threshold: 20
rank: 359
---
# ctxroute-config.json / config-gate.test.js — invariants

⚠️ REAL BUG (2026-07-15, present since the 1st commit): the committed config contained test FIXTURE residue (`filterMode:"whitelist"`, `filterList:["testserver999"]`) → framework running but injecting NOTHING for stripe/odoo, SILENTLY, for days. Cause: the integration tests wrote into the REAL file and "restored the original" — which was already polluted.
NEVER let a test write into `ctxroute-config.json`: tests pass `CTXROUTE_CONFIG_PATH` (throwaway tmpdir). That env var is RESERVED for tests.
NEVER delete/loosen `config-gate.test.js` — it is the dead-man switch: every server having a `docs/mcp/{server}.md` MUST be covered by the shipped config. A hook that never injects is indistinguishable from an absent hook.
Shipped config: `enabled` not false, global `mode` never `"dumb"` (debug fixture; a DOC in dumb stays legitimate — via ITS frontmatter, e.g. stripe.md).
⚠️ `servers` = subToolParam ONLY (never a per-server cadence). Per-DOC cadence lives in its frontmatter; per-CATEGORY cadence in `defaults.{file|mcp|skill|tool}` (2026-08-04) — keys DERIVED from the ADAPTERS' `id`, negative-check in both directions. Keys allowed in an MCP doc: `mode`/`threshold`/`driftUnit`/`note`/`enforce`.
⚠️ **`enforce` (2026-08-05)**: boolean allowed in `defaults.{source}` AND in a `skills` entry. **NO global tier** (deliberate: a global block would refuse the 1st action of every session). Setting it on a WHOLE category is DISCOURAGED.
⚠️ **`frames` (2026-08-07) = the BANDWIDTH of one action, default 12.** Capacity = `frames × (budgetInjection − envelope)` — measured: 12 × 7,661 = **91,932 c**. 🛑 **IT DRIVES NOTHING AT RUNTIME**: the harness starts exactly the processes declared in ITS wiring (`settings.json`), cold. This key is the SOURCE OF TRUTH that `doctor.js --settings` CONFRONTS with the real wiring — two places for one number diverge silently. ⚠️ Raising it has a price: one process per frame on EVERY tool call, even empty (~330 ms). ⚠️ **Gitignored ⇒ any test reading this file BREAKS CI** (clean clone): go through `CTXROUTE_CONFIG_PATH`, or `skipIf` — paid 2026-08-07, 3 reds on ubuntu.
⚠️ **GLOBAL FILTER BY TARGET (52, 2026-08-15)**: `filterMode`/`filterList` generalized from "MCP servers" to "TARGET" (server OR EXACT tool name or the `*` wildcard) — SAME words, zero vocabulary created (the asymmetry was class ㊴). SINGLE application point = `gate.js::excludedTargetsFor` (the MCP source NO LONGER filters — a source POSES, it excludes nothing: class ㊱/㊳); the pair cascades TOGETHER (`defaults.{source}.filterMode` > global; a `"none"` declared at the category tier UNSUBSCRIBES). 🛑 An exclusion is OBSERVABLE: badge `🚫 N doc(s) excluded` (1st frame) + a dedicated section of `explain.js` — a mute filter would be a hole. Pure predicate = `lib-pure.targetExcluded`; `isServerActive` remains the HISTORICAL semantics for the frozen oracle, do not merge them.
`ctxroute-config.schema.json` (2026-07-16) = the CLOSED vocabulary of the config (enums, strict keys). Drift test in config-gate.test.js: a config key outside the schema = RED. New config key ⇒ add it to the schema FIRST, otherwise the gate screams — that is intended.
⚠️ **GATE OF **FORM** SYMMETRY (㊺①, 2026-08-14)** — vocabulary gates probe the PRESENCE of a key in the 4 corpora, **NEVER its FORM**: `frontmatter.validate` (docs) and this schema (skills) are two declarations of the same form, and a mismatch would go UNNOTICED (class ㊴). The gate requires: `scope` declared IDENTICALLY at skill level and per-entry · **`oneOf` with EXACTLY 2 branches** (flat = OR · grouped = AND of OR) · non-empty strings and groups · `exclude` WITHOUT the grouped form. 🛑 `oneOf` and not `anyOf`: it is what REFUSES the MIXED form, and ambiguity is the real danger of this extension (`["a","b"]` written meaning "AND" gives an OR, hence a BROADER rule, silently). ⚠️ **HONEST SCOPE**: it does not PROVE the equivalence of the 2 validators (that would require executing the schema, hence ajv, hence a dependency) — it proves that both forms are declared on both sides and that no 3rd one exists.
