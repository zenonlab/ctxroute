---
match: [ctxroute-reset.js, store-purge-gate.test.js]
mode: dumb
---
# ctxroute-reset.js — PreCompact: the ONLY real context flush
✅ **THE INSTRUCTION BELOW IS NOW MECHANICAL (2026-08-07)**: `store-purge-gate.test.js` requires every declared store (`…PREFIX` in a file that uses `session-store`) to be in the purge loop — **derived from BOTH sides**, plus an inverse part (dead purge) and a negative-check. ⚠️ It raised a FALSE POSITIVE on the 1st run (`sources/skill.js`, an IDENTIFIER prefix and not a store one): hence the 2nd condition. 🛑 **WHAT IT DOES NOT PROVE**: that a reader TOLERATES the purge — that is semantic, therefore undecidable. Real regression of 08-07 (the canary lost its counter on PreCompact) ⇒ covered by a CASE test, not by this gate. Do not sell it beyond that.
⚠️ **THE PREFIX LIST IS EXHAUSTIVE OR IT IS WRONG.** 5 stores to purge: `doc-seen-` (dedup per doc) · `ctxroute-seen-` (legacy relic) · `turn-count-` (turn counter) · `plan-` (plan memoized per invocation) · `remainder-` (emission queue). **Forgetting one breaks nothing visible**: it produces docs never reinjected after compaction, or an orphan fragment — a SILENT defect, discovered sessions later. Every new store MUST be added here in the SAME action.
⚠️ **PURGING THE QUEUE IS CORRECT, not a loss**: compaction empties the real context, so what was waiting to reach it no longer has a destination and the docs will be re-decided from scratch by the cadence. Keeping the queue would deliver the END of a document whose BEGINNING has disappeared — an unreadable fragment.
⚠️ **`plan-` is ALWAYS swept by PREFIX**, never by exact path: its key carries an invocation suffix (`--inv-…`) that a targeted deletion would never find.
⚠️ **PER-AGENT SCOPE**: compaction INSIDE a sub-agent (`agent_id` present) = targeted purge of ITS scope; master compaction = purge by session prefix, sub-agents included. Worst-case fail-open = one reinjection too many, never a frozen state.
🔴 **THE SWEEP TAKES THE LOCK SINCE 2026-08-23 — it was the LAST durable writer that did not.** `/emit` and `/turn` were closed that morning; this one deleted state files with no mutual exclusion at all, on BOTH lanes. TLC exhibits the consequence (`specs/tla/State.tla`, run `StatePurgeWindow`): a writer whose SNAPSHOT PREDATES the deletion republishes it afterwards, a `doc-seen-` record is RESURRECTED, and the document it names is WITHHELD for the rest of the session — no error, no badge, nothing red. 🛑 **The address is ASKED FOR** (`store-resolve.lockDirForKey`), never composed here: two spellings are two locks and two locks are no lock. ⚠️ The five prefixes STRADDLE the two addresses, so the sweep is grouped by address and the sections run ONE AFTER THE OTHER — **never nested**, or a non-reentrant `mkdir` lock self-deadlocks and it reads as a slow compaction. ⚠️ Lock unavailable ⇒ THAT CLASS IS NOT PURGED and nothing is written: one doc not reinjected, never a blocked compaction. Cell: `daemon-purge.test.js`, "the PURGE WINDOW is closed" (seen red on the lock-less version).
⚠️ **TOTAL FAIL-OPEN** (invalid JSON, unreadable disk ⇒ silent exit 0): this hook must NEVER block a compaction. A failed reset costs one doc not reinjected; a reset that crashes costs the session.
