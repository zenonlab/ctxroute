---
rules: [{"pattern":"sources/","scope":["ctxroute"],"rank":536},{"pattern":"frontmatter.js","scope":["ctxroute"],"rank":537},{"pattern":"frontmatter.property.test.js","scope":["ctxroute"],"rank":538},{"pattern":"sources-file.property.test.js","scope":["ctxroute"],"rank":539},{"pattern":"file-differential.test.js","scope":["ctxroute"],"rank":540},{"pattern":"sources-file.test.js","scope":["ctxroute"],"rank":551},{"pattern":"sources-mcp.test.js","scope":["ctxroute"]},{"pattern":"sources-tool.test.js","scope":["ctxroute"]},{"pattern":"mcp-differential.test.js","scope":["ctxroute"]},{"pattern":"frontmatter.test.js","scope":["ctxroute"],"rank":552}]
mode: smart
threshold: 20
rank: 536
---
# sources/*.js + frontmatter.js — unified engine (✅ LIVE, merge COMPLETED 2026-07-17)

⚠️ IN PROD: the single gate `doc-inject.js` (matcher `*`) consumes sources/file.js (file) AND sources/mcp.js (MCP). History/decisions: `REFACTOR-PLAN.md` (status COMPLETED). Breaking a source = no more doc injected for the whole agent fleet.
⚠️ PURE (gates `sources-must-stay-pure` / `frontmatter-must-stay-pure`): zero fs/path/process. This is the CONDITION for mutating without equivalent mutants, not a testing comfort.
⚠️ NO HARNESS DIALECT (gate `sources-must-not-know-the-harness`): a source answers "which docs?", it decides NOTHING. `permissionDecision`/`hookSpecificOutput` = the GATE's job. That is what makes the Codex port trivial (write a gate, not an engine).
⚠️ `sources/file.js` = heir of `protect-files.js` (`cd &&` reconstruction, Codex `apply_patch` — patch in `input`/`patch`/`command`, `command` = REAL Codex shape ≥ 0.144 measured 2026-07-19 —, scope over ALL params, `norm()` backslash+case). Any "improvement" without rerunning the differential = silent regression over 532 rules nobody reviews.
✅ **`exclude` = ∀¬ OVER ALL PARAMS ∪ THE CONTEXT (㊼ FIXED on 2026-08-14).** (⚠️ DUAL IN QUANTIFIER, **NOT over the same universe** — corrected 19/08/2026: `scope` = ∃ over the PARAMS, `exclude` = ∀¬ over the params **∪ the triggering context**, which is STRICTLY larger. Saying "exact dual" was FALSE and it hid a real consequence: the tool NAME is reachable NEGATIVELY and not positively. Decision TAKEN 2026-08-20 by measurement (cell kept blind), REFACTOR-PLAN 59.) It was evaluated **candidate by candidate**, yet `bashCandidates` fabricates a pseudo-path PER WORD of a `cd X && …` (`ctxroute/node`) ⇒ **an invented fragment allowed it all by itself**: `cd ~/Desktop/ctxroute && node explain.js` let 53 KB in despite `exclude: ["explain.js"]`. 🛑 **ROOT CAUSE = THE QUANTIFIER, not the universe**: `¬(∃c : m ⊑ c) ≡ ∀c : m ⋢ c` — evaluated existentially, `exclude` was **a negation built like an affirmation**, therefore bypassable by the way an action is written. A "for all" cannot be bypassed by construction. ⚠️ **PRICE ACCEPTED, MEASURED on 4,702 real actions: 64 change (1.36 %), ALL one-directional (it can only REMOVE)** — 61 = the target, 3 = collateral (an `--exclude node_modules` in a command now counts as exclusion). Whoever wants finer writes a more precise pattern (`/node_modules/`). 🛑 **NEVER go back to per-candidate evaluation**: sealed by 2 property-based LAWS (`sources-file.property.test.js`) — MONOTONICITY (widening `exclude` can only REDUCE) and INDEPENDENCE FROM WRITING (one more word never re-allows). ⚠️ Their generators MUST produce the `cd X && …` form: without it there is only ONE candidate, the bug is unreachable and the laws pass BY VACUITY (measured by sabotaging the fix). ⚠️ **LESSON KEPT**: the comment "DELIBERATE asymmetry, not an oversight" turned this bug into a claimed choice and killed the question for weeks — it was even served to the maintainer as an explanation. **An explanatory note is not a gate.**
⚠️ **`scope` SEES THE WHOLE PAYLOAD, AT ANY DEPTH (㊵, fixed 2026-08-12).** It only read the 1st level (`Object.values(...).filter(typeof==='string')`) ⇒ **BLIND to the 16 MCP servers** (all of which put their args in `args{}`), while the doc promised everywhere "scope sees ALL params". The meaning of an operator must NEVER depend on the SHAPE of the payload. `textValues()` flattens recursively, **2 MEASURED bounds** (25,898 real calls: max depth 11, max text 12,060 c) ⇒ 20 levels / 262,144 c, and crossing them is **STATED** (`explain.js`): a mute bound would recreate the defect. 🛑 Non-text always ignored; no cycle detection (JSON.parse produces none = dead code).
✅ **`scope` HAS TWO FORMS — flat = OR, GROUPED = AND of OR (㊺①, 2026-08-14).** `["a","b"]` = a OR b (inherited, **UNTOUCHED** — differential: 0 change over 4,702 real actions) · `[["a","b"],["c"]]` = (a OR b) AND c. ⇒ "this file AND this project AND this flag", **inexpressible until 08-13**, can be written. **ZERO vocabulary word created**: it is a FORM of the existing key, the boolean base stays CLOSED. 🛑 **DEADLY TRAP, a mistake already made and caught in one minute**: a list WITHOUT any group is **ONE** group. Mapping it element by element would make it an AND and would FLIP the meaning of the fleet's 852 rules, silently ⇒ `scopeGroups` tests `some(Array.isArray)` BEFORE mapping, and law ④ (`sources-file.property.test.js`) proves `["a","b"] ≡ [["a","b"]]` on any action. 🛑 **MIXED FORM (`["a", ["b"]]`) = REFUSED** by `frontmatter.validate` AND by the skills schema: the danger of this extension was never the limit, it is AMBIGUITY. At runtime (unvalidated config) the reading is the most RESTRICTIVE — **an engine that guesses must always guess in the direction that does NOT inject**. ⚠️ `exclude` does NOT have this form (∀¬ over a single universe: an "AND of OR" makes no sense there). 📐 **SCOPE RE-MEASURED AND CHARACTERIZED: 128/256 = EXACTLY `{f | f(empty action)=false}`** — COMPLETE **for a FIXED observable universe**, up to the structural constraint "we only inject on FACTS". ⚠️ **THAT HYPOTHESIS IS LOAD-BEARING (19/08/2026)**: expressiveness = combinators × observables, and this number measures ONLY the combinators. Quoting it as "the language is complete" proves one factor and concludes on the product — the second factor has its own judge, `observable-reach-gate.test.js`.
⚠️ **`sources/mcp.js` NO LONGER FILTERS (52, 2026-08-15)**: `filterMode`/`filterList` moved to `gate.js` (target = server OR tool, cascade, observable exclusion). The source SETS the candidates — making it filter again = a source that resolves (class ㊱/㊳), and `sources-mcp.test.js` turns red.
✅ **53bis (2026-08-15) — A PATTERN LIVES IN A VALUE, NEVER ASTRIDE TWO.** The filters' universe was `morceaux.join(' ')`: a pattern WITH A SPACE (`"node build"`) matched astride two adjacent params — a text that exists in NO param (reproduced both ways: phantom exclusion + phantom scope; **found by the SPEC, not by a human** — a first). Fixed: PER-VALUE evaluation (`morceaux`), the context stays a SEPARATE value; the concatenated `texte` field is DELETED (a dead field always finds a reader again). **Measured price: 0 change over 7,355 real actions** (latent hole, never bit). Sealed: differential domain enriched with boundary forms (SEEN turning red before the fix) + 4 deterministic cases (Stryker suite), 100 % mutation.
✅ **55 (2026-08-15) — `scope`/`exclude` STARTING WITH `[[` are parsed as JSON in `parse()`**: via `parseList`, the grouped form `[["a"],["b"]]` became FLAT LITERALS (`'["a"]'`), VALID to the validator ⇒ a rule born DEAD in silence (the fleet's 1st doc to write it = the `live-production.md` guard, measured stillborn). 🛑 NEVER JSON-first on EVERY list: a flat one whose element is valid JSON (`[{}]`) would change reading — caught by the round-trip property the same day. Unreadable JSON on `[[` ⇒ RAW value ⇒ validate RED (never silent flat garbage). `exclude` follows for the OPPOSITE reason: validate must SEE `[["a"]]` in order to REFUSE it loudly. Triggers/`note` = never JSON.
✅ **`keys` = THE "WHERE TO LOOK" AXIS (19/08/2026) — and shipping it revealed FOUR holes, all of one family.** The operator chooses which parameter KEYS are visible; the others say WHAT to look for. Two forms: `["-command"]` = the three axes · `{match,scope,exclude}` = one universe per axis. Whitelist **REPLACES** the universe (so it may WIDEN) · a list of `-name` **REMOVES** from the default.
🔴 **① THE WHITELIST ONLY REPLACED FOR THE TRIGGER.** `keys: ["content"]` widened `match` and left `scope`/`exclude` blind ⇒ **ONE declaration, TWO meanings depending on the axis reading it**, silently. **780 divergences MEASURED** by the exhaustive differential the day the model learned the operator. Worse: the open half was the DANGEROUS one (a trigger CREATES injections), the closed half the SAFE one (a filter never injects on its own). ⇒ ㊿ is the **DEFAULT** universe of the filters, **never a floor**: `keys` exists precisely so an entry can overrule a global default FOR ITSELF, in writing. ⚠️ Re-traversed ONLY for the rule that NAMES a payload key — the fleet's 852 rules keep the shared traversal, zero cost (same pattern as `cheminsR`/`commandesR`, same reason: the general case must not pay for the rare one). 🛑 NEVER traverse the content by default: a large `content` would eat the `MAX_SIZE` bound and truncate the rest.
🔴 **② `exclude` WENT BACK TO EXISTENTIAL — ㊼ WORD FOR WORD.** The decision was taken **per candidate**; as long as the params carried the whole command, the complete universe hid the defect. `keys: {exclude:["-command"]}` removes it ⇒ **a single candidate free of the pattern re-authorised everything**, so the negation became bypassable again by how the gesture is written. **192 divergences MEASURED.** ✅ The decision is now taken **ONCE, over ALL biting candidates** (`mordants`) — a "for all" cannot be bypassed by construction. 🛑 NEVER go back to a per-candidate `shouldSkip`.
🔴 **③ `cwd` LIVED OUTSIDE EVERY KEY UNIVERSE** (pushed as a special case inside `extractFilePaths`) ⇒ `keys` could not reach it in any form — yet it is THE parameter that separates "I am WORKING here" from "I am QUOTING it". It is now a DECLARED path key of the profile; default behaviour unchanged, but narrowable. A hard-coded special case is not data.
🔴 **④ INERT ON THE SKILLS' `match` DIMENSION** (8 fleet entries out of 8) — `skillRules` rebuilds its rule field by field. ⇒ sealed by `operator-consumption-gate.test.js`, which PROBES every operator on every dimension instead of trusting a list.
⚠️ **WHAT THE SEQUENCE TEACHES**: the four holes lived under 959 green tests and a 100 % mutation score. None was found by a test — **the first by a behavioural probe, the next two by the INDEPENDENT MODEL** as soon as it was taught the operator. An operator shipped OUTSIDE the judges (`language-spec.js`, `spec-differential`, `language-atoms`, `language-completeness`) is an operator whose semantics nobody checks.
⚠️ ORDER = rule-major (rules → paths), dedup = FIRST rule wins. That is the parent→child order of the concatenation. A Set or a path-major order breaks the meaning without displaying anything.
⚠️ **`rank` = A Z-INDEX, NOT A TREE (2026-08-07)** — the engine has NO concept of a parent: just a total order over an integer (313 docs carry one). Hierarchy is **expressed with the language**, never coded into the core — that is why no hierarchy addition has ever moved the engine. 🛑 **IT SERVES TWO PURPOSES, AND THE TRANSPORT DOES NOT PRESERVE THEM THE SAME WAY**: **dedup** (1st rule wins) is decided at EMISSION so it is INTACT ✅ · **READING order** was UNOBSERVABLE in multi-frame — **FIXED on 2026-08-08**: each document carries `[DOC i/T]` next to its source tag, so the intended order can be glued back even if the frames arrive shuffled (symmetric hole of `CHUNK j/m`). ⚠️ The ordinal does not FORCE the harness to deliver in order — it makes it KNOWABLE. ⇒ keep the writing rule: each doc stays AUTONOMOUS, 1 line = 1 invariant, never "completes what precedes". Measured: 88 co-injected pairs, 0 conflict.
⚠️ **ONLY THE MCP CHANNEL carries a real hierarchy** (`global → specific`), and it survives disorder because it lives in the PATH (`docs/mcp/odoo.md` ⊃ `docs/mcp/odoo/delete_record.md`), not in a field.
⚠️ `match` accepts a string OR a list: measured, 98 of the 288 docs have several patterns. Accepting only a string rejects a third of the fleet.
⚠️ CADENCE: MCP doc = optional frontmatter `mode:`/`threshold:`/**`driftUnit:`** (the author proposes, the cascade disposes); the JSON carries ONLY globals + `defaults.{source}` (`servers` = subToolParam only). 4-stage cascade, SINGLE POINT gate.js: entry > `defaults.{source}` > global > framework.
⚠️ **`note:` (2026-08-04) = AUTHOR comment, the ONLY field the engine NEVER reads** — meant for whoever comes to MODIFY the doc ("why this mode/this scope"), invisible to injection since the whole frontmatter is stripped from the body (sealed by a test). 🛑 NEVER the why of an INVARIANT: that one stays in the body, otherwise the rule drifts. The day the engine read it, it would be config in disguise = a 2nd truth.
⚠️ **`enforce:` (2026-08-05) = boolean, REFUSES the action instead of informing.** Accepted in file docs AND MCP docs AND skill entries (same word everywhere). Cascade IDENTICAL to the others — EXCEPT that it has NO global stage (a global block would refuse the 1st action of every session). ⚠️ EXPLICIT `false` = cancels inheritance from `defaults.{source}`: without it, a category switched to enforce would be UN-OPT-OUT-ABLE. Non-boolean value = RED (never taken for a "yes"). It FOLLOWS the cadence: no mode forbidden, the anti-loop is ALTERNATION (cf. gate.md).
⚠️ **VOCABULARY SYMMETRY GATE (2026-08-05, `frontmatter.test.js`)**: every BEHAVIOR key (the vocabulary minus the matching operators) must exist in the **4 corpora** (file doc · MCP doc · skill entry · `defaults.{source}`), **otherwise RED** — unless there is an entry in `ASYMETRIES_JUSTIFIEES` with a written reason. Inverse part: a justification that has become false turns red too. **Symmetry by DEFAULT, exception DECLARED**: that is what makes a silent gap IMPOSSIBLE when keys are added. It paid off on the 1st run (`note` was missing in `defaults`). ⚠️ **`ASYMETRIES_JUSTIFIEES` has been EMPTY since `confirm` was removed (2026-08-05): the vocabulary is FULLY symmetric.** Adding an entry = a written decision, never a workaround.
⚠️ `tool:` = 4th trigger (2026-07-19, sources/tool.js + toolAdapter): EXACT name (===) of a NATIVE TOOL — closes the WebFetch/WebSearch blind spot (neither path nor mcp__, proven mute by spawn). NEVER a substring. toolAdapter REUSES acc.decls/bodies set by fileAdapter (zero corpus re-read — the file→tool order in ADAPTERS is a DEPENDENCY); docId dedup = file wins. scope/exclude via file.shouldSkip (single source).
✅ **`match` NOW SEES EVERY SHELL, not just Bash (㊽, 2026-08-14)** — a shell action is recognized by its **FORM** (presence of a `command`), never by a tool NAME. **MEASURED: 809 commands out of 4,396 (18 %) were INVISIBLE** — all PowerShell, all SSH; on a Windows machine, the main shell. Differential over 4,702 real actions: 128 change, **ZERO lost**. 🛑 NEVER go back to a tool list: it is born stale and fails in SILENCE. ⚠️ `match` stays limited to the **PATHS** declared by the profile + the command: to react to the CONTENT of a parameter (an MCP tool), use `tool` + `scope`. Recipe proven by real spawn on 2026-07-31 over 4 channels (POSIX shell, Windows shell, remote MCP tool, file read) — without it, every author rediscovers it at the cost of a session.
⚠️ **WILDCARD `tool: ["*"]` = any tool** (2026-07-31): a special VALUE, not an operator — the boolean base stays closed. **A wildcard WITHOUT `scope` or `exclude` is RED** (it would inject at every call). `*` + `exclude` = "all EXCEPT X" (on this axis, `exclude` applies to the TOOL NAME). Empty tool name ⇒ never matches.
⚠️ **`mcp:` is REJECTED in a doc under `docs/`** (2026-07-31): the MCP channel is triggered by the PATH `docs/mcp/{server}.md`, never by a key. Before, it was accepted AND inert — a validator that approves dead things points at the WRONG cause.
⚠️ **Mute doc ⇒ `node explain.js --doc <name> --tool X --input '{...}'`**: it gives the exact reason (pattern/scope/exclude/corpus/cadence). NEVER reimplement the engine to probe it — that is what cost a session on 07-31 (3 false probes, a false verdict "the engine does not read this param").
⚠️ `rules:` = 3rd trigger, inline JSON per entry `{pattern, scope?, exclude?}` — measured 07-16: 31/103 multi-rule docs have DIVERGENT scopes, unrepresentable with one scope per doc. `rules` + `match`/`scope`/`exclude` = contradiction RED. `rank`: measured (39 pairs, 0 conflict) then KEPT — parent/child abandoned (order partly accidental, cf. plan).
⚠️ MIGRATION COMPLETED (2026-07-27): frontmatters are the ONLY source of rules, lint-corpus INCLUDED. `protected-paths.json` = truth of the OLD engine (protect-files.js, replaced on 07-17) ⇒ INERT artifact: do NOT write to it anymore. Double writing + its 2 parity gates REMOVED. ⚠️ UNRELATED to Codex: its shells run on the NEW engine, compatibility intact.
⚠️ TOTAL parser: `parse()` NEVER throws. A throw = dead hook = NO doc injected anywhere anymore (a single malformed .md would break everything).
⚠️ Unknown frontmatter key = ERROR, never ignored: `mach:` instead of `match:` = doc dead in silence — the bug this refactor kills.
🛑 **A FROZEN ORACLE AGES — ORPHAN RULES are discarded, and ANNOUNCED (2026-08-12).** `protected-paths.json` has been INERT since 07-27 while the FLEET lives on: it cited 4 DELETED docs ⇒ **17 false divergences**, and that false red GROWS (~4 per deleted doc) until it makes the heaviest gate in the repo durably red, therefore dead. The oracle cannot emit an unreadable doc; `matchingDocs` returns the raw reference (that filtering lives in the ADAPTER) ⇒ we were comparing two LAYERS, not two engines. ⚠️ **Red differential ⇒ REPLAY the EXACT cases with the old code BEFORE blaming yourself**: that is what proved (0/2464) that the ㊵ fix was innocent.
Differential = `npm run test:differential` (~75 min, 2021 spawns): SWITCHOVER gate, NEVER a push gate. Green proves match equivalence, never that the moment is right to switch over.
✅ **A SHELL GESTURE EXPOSES THREE FACTS (2026-08-20)**: the raw text (`command`) · the directory a `cd X && …` designates (`commandCwd`) · the paths that `cd` RECONSTRUCTS word by word (`commandPaths`). The last two are DERIVED and live in a REGISTRY (`derived-observables.js`) — never one profile field per fact, which grew two twin engine branches in a single evening. This file LOOPS over that registry: a fact added tomorrow costs ZERO engine line. 🛑 `bashCandidates` still returns the WHOLE universe: it is `explain.js`'s public probe and the ∀¬ of `exclude` must keep seeing every value — a filter that stops seeing one is a negation weakened in silence (㊼ again). ⚠️ A derivation reads the FULL command list, never `commandesR`: dropping the raw half must not drop where the gesture WORKS, or the operator loses 47.7 % of real work (28,703 actions measured).
✅ **THE FILTER CASCADE LIVES HERE SINCE 2026-08-20** — `heriterFiltres` + `composerKeys`, right next to `keyDecision`. An entry composes with `defaults.{source}`: one `-` ⇒ ADJUST, bare names ⇒ REPLACE, the entry's addition wins. 🛑 ADJUST+ADJUST composes by CONCATENATION, never by resolving into a list: a resolved list reads as REPLACE, and the FILTERS' universe is every param minus the payload ones (㊿) — resolving would blind `scope`/`exclude` to a remote MCP's `args.foo`, in SILENCE.
✅ **`keys` MIXED FORM ADMITTED (2026-08-20)** — a `-` present ⇒ ADJUST the default universe · no `-` ⇒ REPLACE it. The refusal forced a hand-written enumeration of the whole universe to add ONE key, which stops following the profile the day it grows (class ㊽). ⚠️ In ADJUST mode `garde` keeps ㊿ EXPLICITLY (payload keys stay out unless NAMED): writing it as a bare `!bannies.includes(key)` only worked because the shared traversal had already dropped them — an invariant held by the CALLER, hence one that breaks when a caller changes, and a mixed form re-traverses.
