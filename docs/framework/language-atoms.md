---
match: language-atoms.test.js
scope: [ctxroute]
mode: smart
rank: 512
---
# language-atoms.test.js — THE ATOMS TABLE (㊻④, 2026-08-15)

🛑 **EVERY CELL (source × projection × operator) IS PROBED BY BEHAVIOR** on the REAL engine: visible ⟺ the decision changes between the action WITH the atom and the same action WITHOUT. Never declared by reading — a hand-written table would be a doc, and a doc drifts. ㊴ and ㊵ were empty cells of this table, found afterwards, one session each.
⚠️ **A BLIND cell ⇒ mandatory WRITTEN justification** (dated/numbered decision, never "the engine does not do it") — a blind cell without justification is a NAMED hole = RED.
🛑 **RED = the engine GAINED or LOST a view.** Gained: check it is INTENDED before flipping the cell to visible (a trigger that widens = the costliest noise class). Lost: semantic regression (= ㊵). In both cases DECIDE and write it down — NEVER "justify" to silence it.
⚠️ **`mcp/scope-exclude/absents` is JUSTIFIED, not a hole**: the capability is expressible through the `tool` axis (`tool: ["mcp__srv__x"]` + scope/exclude — the sub-tool IS a param). The path channel is a convenience; completeness lives in `tool`. Do not reopen without a real inexpressible case.
⚠️ A new projection of the action (new profile key, new channel) ⇒ **one LINE per operator here, IN THE SAME ACTION** — that is the net replacing "we will notice".
✅ **`keys` ENTERED THE TABLE 19/08/2026 — THE BLOCK WAS MISSING THE DAY IT SHIPPED.** The operator lived in the engine, the validator and the schema, and in NO judge. This table is what says an operator EXISTS on an axis; without those rows an axis could stay inert forever — **and one was**: `skill/match`, 8 fleet entries out of 8. Rows added: blacklist removes · whitelist replaces · one axis per axis · axis independence · **whitelist reaches a payload key** (false until 19/08: it widened the trigger only) · **`cwd` addressable** (false until 19/08: pushed as a special case, hence outside every key universe) · the skills' `match` dimension.
⚠️ Every cell narrows a key that carries the atom ALONE — that is what makes the flip attributable to `keys` and to nothing else.
